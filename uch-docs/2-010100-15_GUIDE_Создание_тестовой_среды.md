---
id: "2-010100-15"
name: "Создание_тестовой_среды"
type: "GUIDE"
level: 2
status: "active"
tags: []
created: "2026-02-03"
updated: "2026-02-03"
author: "kirillkravcov"
slug: "2-010100-15_GUIDE_Создание_тестовой_среды"
---


# 🧪 Гайд: Тестовая среда для UCH Blog

## 🎯 Зачем нужна тестовая среда

Тестовая среда позволяет:

- **Тестировать изменения** без риска для основного блога
- **Экспериментировать с дизайном** и функционалом
- **Демонстрировать идеи** до внедрения
- **Отрабатывать деплой** и обновления

## 🏗️ Подходы к тестированию

### Уровень 1: Быстрое тестирование (рекомендуется)

Использование отдельного порта и изолированных настроек.

### Уровень 2: Временная ветка Git

Создание feature-ветки для изменений.

### Уровень 3: Полная изоляция

Docker-контейнеры или виртуальные машины.

## 🚀 Быстрый старт: Тестирование на отдельном порту

### Шаг 1: Создаем тестовые файлы

Создаем тестовую директорию:

bashCopyDownload```
mkdir -p templates/test
```
Копируем шаблоны для тестирования:

bashCopyDownload```
cp templates/base.html templates/test/base_original.html
```
Создаем тестовый шаблон UCH темы:

bashCopyDownload```
cat > templates/test/base_uch_test.html << 'TEMPLATE_EOF'
<!DOCTYPE html>
<html lang="ru" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}UCH Test{% endblock %}</title>
    <link rel="stylesheet" href="/static/css/uch-theme.css">
</head>
<body>
    <nav>Навигация тестовой среды</nav>
    <main>{% block content %}Тестовый контент{% endblock %}</main>
    <footer>Тестовый футер</footer>
</body>
</html>
TEMPLATE_EOF
```
### Шаг 2: Создаем тестовые views

Создаем файл для тестовых представлений:

bashCopyDownload```
cat > uch/apps/blog/views_test.py << 'VIEWS_EOF'
from django.views.generic import TemplateView

class ThemePreviewView(TemplateView):
    template_name = 'test/theme_preview.html'
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['theme'] = self.kwargs.get('theme', 'uch')
        return context

class ArticlePreviewView(TemplateView):
    template_name = 'test/article_preview.html'
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['theme'] = self.kwargs.get('theme', 'uch')
        return context
VIEWS_EOF
```
### Шаг 3: Создаем тестовые URL

Создаем отдельный файл для тестовых URL:

bashCopyDownload```
cat > uch/urls_test.py << 'URLS_EOF'
from django.urls import path
from uch.apps.blog.views_test import ThemePreviewView, ArticlePreviewView

urlpatterns = [
    path('theme/<str:theme>/', ThemePreviewView.as_view(), name='theme_preview'),
    path('article/<str:theme>/', ArticlePreviewView.as_view(), name='article_preview'),
    path('', ThemePreviewView.as_view(), name='test_home'),
]
URLS_EOF
```
### Шаг 4: Обновляем основной urls.py

Добавляем тестовые пути в основной файл (сначала сделаем backup):

bashCopyDownload```
cp uch/urls.py uch/urls.py.backup
```
Редактируем `uch/urls.py`, добавив строку:

pythonCopyDownload```
path('test/', include('uch.urls_test')),  # Тестовые URL
```
Итоговый urls.py должен выглядеть примерно так:

pythonCopyDownload```
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('uch.apps.blog.urls')),
    path('test/', include('uch.urls_test')),  # Добавленная строка
]
```
### Шаг 5: Создаем тестовые шаблоны

Создаем шаблон предпросмотра темы:

bashCopyDownload```
cat > templates/test/theme_preview.html << 'PREVIEW_EOF'
{% if theme == 'uch' %}
    {% extends 'test/base_uch_test.html' %}
{% else %}
    {% extends 'test/base_original.html' %}
{% endif %}

{% block content %}
<div class="container">
    <h1>Предпросмотр темы: {{ theme }}</h1>
    <p>Это тестовая страница для предпросмотра дизайна.</p>
</div>
{% endblock %}
PREVIEW_EOF
```
### Шаг 6: Запускаем тестовый сервер

Останавливаем основной сервер если запущен:

bashCopyDownload```
pkill -f "python manage.py runserver"
```
Запускаем на порту 8001 (основной блог на 8000):

bashCopyDownload```
./venv/bin/python manage.py runserver 8001 &
```
### Шаг 7: Тестируем

Открываем в браузере:

- **Новая тема**: [http://127.0.0.1:8001/test/theme/uch/](http://127.0.0.1:8001/test/theme/uch/)
- **Старая тема**: [http://127.0.0.1:8001/test/theme/original/](http://127.0.0.1:8001/test/theme/original/)

## 🔧 Полная тестовая среда (рекомендуется)

### Создаем тестовые настройки

Создаем отдельный файл настроек:

bashCopyDownload```
cat > uch/settings_test.py << 'SETTINGS_EOF'
"""
Настройки для тестовой среды
"""
from .settings import *

# Используем тестовую базу данных
DATABASES['default']['NAME'] = 'db_test.sqlite3'

# Добавляем тестовые шаблоны в начало списка
TEMPLATES[0]['DIRS'] = [
    BASE_DIR / 'templates/test',
    BASE_DIR / 'templates',
]

# Отключаем кэширование шаблонов для разработки
TEMPLATES[0]['OPTIONS']['loaders'] = [
    'django.template.loaders.filesystem.Loader',
    'django.template.loaders.app_directories.Loader',
]

print("⚠️  Используются тестовые настройки")
SETTINGS_EOF
```
### Создаем скрипт для удобного тестирования

bashCopyDownload```
cat > test_environment.sh << 'SCRIPT_EOF'
#!/bin/bash
# Скрипт для запуска тестовой среды UCH Blog

echo "🚀 Запуск тестовой среды..."

# Останавливаем все Django процессы
pkill -f "python manage.py runserver"

# Запускаем основной блог на порту 8000
echo "🌐 Основной блог на порту 8000..."
./venv/bin/python manage.py runserver 8000 &

# Ждем секунду
sleep 1

# Запускаем тестовую среду на порту 8001
echo "🧪 Тестовая среда на порту 8001..."
./venv/bin/python manage.py runserver 8001 --settings=uch.settings_test &

# Ждем запуска
sleep 2

echo ""
echo "✅ Серверы запущены!"
echo ""
echo "📊 Доступные адреса:"
echo "   Основной блог:      http://127.0.0.1:8000/"
echo "   Тестовая среда:     http://127.0.0.1:8001/"
echo ""
echo "🧪 Тестовые страницы:"
echo "   Новая тема UCH:     http://127.0.0.1:8001/test/theme/uch/"
echo "   Оригинальная тема:  http://127.0.0.1:8001/test/theme/original/"
echo "   Тестовая статья:    http://127.0.0.1:8001/test/article/uch/"
echo ""
echo "🛑 Для остановки: ./stop_test.sh"
SCRIPT_EOF

chmod +x test_environment.sh
```
Создаем скрипт остановки:

bashCopyDownload```
cat > stop_test.sh << 'STOP_EOF'
#!/bin/bash
echo "🛑 Останавливаю тестовую среду..."
pkill -f "python manage.py runserver"
echo "✅ Все серверы остановлены"
STOP_EOF

chmod +x stop_test.sh
```
### Создаем тестовую базу данных

bashCopyDownload```
# Создаем чистую тестовую базу
cp db.sqlite3 db.sqlite3.backup_test
rm -f db_test.sqlite3

# Применяем миграции к тестовой базе
./venv/bin/python manage.py migrate --settings=uch.settings_test

# Создаем тестового пользователя
./venv/bin/python manage.py shell --settings=uch.settings_test << 'SHELL_EOF'
from django.contrib.auth.models import User
User.objects.create_superuser('test', 'test@test.com', 'test123')
print("✅ Создан тестовый пользователь: test / test123")
SHELL_EOF
```
## 🎯 Рабочий процесс тестирования

### Сценарий 1: Тестирование новой темы

bashCopyDownload```
# 1. Запускаем тестовую среду
./test_environment.sh

# 2. Работаем над файлами в static/css/uch-theme/
# 3. Смотрим изменения на http://127.0.0.1:8001/test/theme/uch/
# 4. Когда все готово, копируем изменения в основную тему
cp static/css/uch-theme.css static/css/uch-theme.css.backup
# ... копируем изменения ...

# 5. Останавливаем
./stop_test.sh
```
### Сценарий 2: Тестирование нового функционала

bashCopyDownload```
# 1. Создаем feature-ветку
git checkout -b feature/new-design

# 2. Запускаем тестовую среду
./test_environment.sh

# 3. Вносим изменения
# 4. Тестируем
# 5. Если все ок, коммитим и мержим
git add .
git commit -m "Add new UCH theme"
git checkout main
git merge feature/new-design

# 6. Останавливаем тестовую среду
./stop_test.sh
```
### Сценарий 3: Быстрое A/B тестирование

bashCopyDownload```
# Одновременно смотрим старую и новую версии:
open http://127.0.0.1:8000/          # Основной блог (старая тема)
open http://127.0.0.1:8001/test/theme/uch/  # Новая тема
```
## 🛡️ Меры предосторожности

### Перед тестированием всегда:

1. **Делаем backup** основных файлов:

bashCopyDownload```
cp db.sqlite3 db.sqlite3.backup_$(date +%Y%m%d)
cp -r templates templates.backup_$(date +%Y%m%d)
```
1. **Проверяем git статус**:

bashCopyDownload```
git status
git diff
```
1. **Используем виртуальное окружение**:

bashCopyDownload```
source venv/bin/activate
which python  # Должен показывать venv/bin/python
```
### Файлы для резервного копирования:

textCopyDownload```
db.sqlite3
templates/base.html
uch/urls.py
uch/settings.py
static/css/style.css
```
## 🔍 Проверка тестовой среды

### Чеклист рабочей тестовой среды:

- Сервер на порту 8001 запущен
- Тестовая база db_test.sqlite3 создана
- URL /test/ доступны
- Шаблоны в templates/test/ существуют
- Основной блог на порту 8000 работает
- Нет конфликтов портов

### Команды проверки:

bashCopyDownload```
# Проверяем процессы
ps aux | grep "runserver"

# Проверяем порты
lsof -i :8000
lsof -i :8001

# Проверяем тестовые URL
curl -I http://127.0.0.1:8001/test/
```
## 🧹 Очистка тестовой среды

### Безопасная очистка:

bashCopyDownload```
# Останавливаем серверы
./stop_test.sh

# Удаляем тестовую базу (опционально)
rm -f db_test.sqlite3

# Удаляем тестовые шаблоны (опционально)
rm -rf templates/test

# Восстанавливаем оригинальные настройки
cp uch/urls.py.backup uch/urls.py
```
### Полная очистка:

bashCopyDownload```
# Удаляем все тестовые файлы
find . -name "*test*" -type f | grep -v ".git" | xargs rm -f
rm -rf templates/test
```
## 📁 Структура тестовой среды

textCopyDownload```
uch-backend/
├── templates/
│   ├── test/                    # Тестовые шаблоны
│   │   ├── base_uch_test.html
│   │   ├── base_original.html
│   │   ├── theme_preview.html
│   │   └── article_preview.html
│   └── base.html               # Основной шаблон
├── uch/
│   ├── urls_test.py           # Тестовые URL
│   ├── settings_test.py       # Тестовые настройки
│   └── urls.py.backup         # Backup основного urls.py
├── db_test.sqlite3            # Тестовая база данных
├── test_environment.sh        # Скрипт запуска
├── stop_test.sh               # Скрипт остановки
└── uch/apps/blog/
    └── views_test.py          # Тестовые представления
```
## 🚨 Устранение неполадок

### Проблема: "Port already in use"

bashCopyDownload```
# Освобождаем порты
lsof -ti:8000 | xargs kill -9
lsof -ti:8001 | xargs kill -9
```
### Проблема: "TemplateDoesNotExist"

bashCopyDownload```
# Проверяем пути к шаблонам
ls -la templates/test/

# Проверяем настройки TEMPLATES['DIRS']
./venv/bin/python manage.py shell --settings=uch.settings_test << 'EOF'
from django.conf import settings
print(settings.TEMPLATES[0]['DIRS'])
EOF
```
### Проблема: "No module named 'uch.urls_test'"

bashCopyDownload```
# Проверяем существование файла
ls -la uch/urls_test.py

# Проверяем импорты в urls.py
grep "urls_test" uch/urls.py
```
## 📈 Расширенное тестирование

### Для тестирования CSS/JavaScript:

1. Используем **Live Reload** расширения браузера
2. Настраиваем **source maps** для отладки
3. Используем **браузерные dev tools**

### Для тестирования производительности:

bashCopyDownload```
# Тестируем скорость загрузки
curl -s -o /dev/null -w "Время: %{time_total}с\n" http://127.0.0.1:8001/test/

# Проверяем размер страницы
curl -s -w "Размер: %{size_download} байт\n" -o /dev/null http://127.0.0.1:8001/test/
```
### Для тестирования на разных устройствах:

1. Используем **режим разработчика** браузера
2. Тестируем на реальных мобильных устройствах
3. Проверяем в разных браузерах

*Документ создан: 2024-02-01*
*Проект: Universal Creative Hub*
*Версия: 1.0*
