# 📊 ПОЛНАЯ СТАТИСТИКА ДОКУМЕНТАЦИИ

**Дата:** $(date)

## 📈 ОБЩАЯ СТАТИСТИКА
- Всего .md файлов: $(find .. -name "*.md" -type f | wc -l)
- Всего .sh скриптов: $(find .. -name "*.sh" -type f | wc -l)

## 🔢 РАСПРЕДЕЛЕНИЕ ПО УРОВНЯМ
$(grep -h "level:" ../*.md 2>/dev/null | sort | uniq -c | sort -rn | head -10)

## 🏷️ РАСПРЕДЕЛЕНИЕ ПО ТИПАМ
$(grep -h "type:" ../*.md 2>/dev/null | sort | uniq -c | sort -rn | head -10)

## 📋 РАСПРЕДЕЛЕНИЕ ПО СТАТУСАМ
$(grep -h "status:" ../*.md 2>/dev/null | sort | uniq -c | sort -rn)

## ⚠️ ПРОБЛЕМЫ ДАННЫХ
- Несогласованные значения в level, type, status
- Комментарии в полях frontmatter
- Разные форматы данных
