
## 📄 ФАЙЛ: 6-001 Strudel Integration Spec v1.0.md

markdown

```
# Strudel Integration Specification v1.0

**Статус:** Актуально  
**Версия:** 1.0  
**Дата:** 2025-12-15  
**Автор:** [Universal Creative Hub]  
**Заменяет:** Все предыдущие неформальные решения

---

## 🎯 ЦЕЛЬ ДОКУМЕНТА

Единый источник истины для интеграции Strudel в UCH. Определяет:
- Какой API Strudel использовать
- Как управлять звуком
- Как избегать конфликтов
- Как тестировать изменения

---

## 📊 СТАТУС ИНТЕГРАЦИИ

### ✅ РАБОТАЕТ:
- Загрузка \`@strudel/web\` с CDN
- Воспроизведение паттернов через \`strudel.evaluate()\`
- Остановка через \`AudioContext.suspend()\`
- Интеграция с CoreOrchestrator как нода \`strudel-repl\`

### 🔴 НЕ РАБОТАЕТ / ИЗВЕСТНЫЕ ПРОБЛЕМЫ:
- \`strudel.hush()\` - вызывает ошибку (scheduler undefined)
- Автоматическая загрузка семплов (\`tr909\` и др.)
- HMR конфликты в dev-режиме
- Звук может задваиваться при конфликте с Tone.js

### ⚠️ ОГРАНИЧЕНИЯ:
- Используем только CDN версию (\`@strudel/web\`)
- Требует активации по клику (Autoplay policy)
- Лицензия AGPL-3.0 (требует раскрытия кода)

---

## 🏗️ АРХИТЕКТУРНАЯ СХЕМА

\`\`\`mermaid
graph TD
    A[StrudelREPLNode] --> B[Инициализация];
    B --> C{CDN загружен?};
    C -->|Да| D[initStrudel()];
    C -->|Нет| E[Показать ошибку];
    D --> F[window.strudel];
    F --> G[getAudioContext()];
    G --> H[AudioContext:suspended];
    
    I[Play кнопка] --> J[audioContext.resume()];
    J --> K[strudel.evaluate(code)];
    K --> L[Звук воспроизводится];
    
    M[Stop кнопка] --> N[audioContext.suspend()];
    N --> O[Звук остановлен];
    
    P[Unmount] --> Q[audioContext.close()];
    Q --> R[Ресурсы освобождены];
```

---

## 📋 КОНТРАКТ API

### РАЗРЕШЕННЫЕ МЕТОДЫ (ИСПОЛЬЗУЕМ):

javascript

```
// 1. ИНИЦИАЛИЗАЦИЯ
window.initStrudel()           // Создает window.strudel
window.strudel.getAudioContext() // Получить AudioContext

// 2. ВОСПРОИЗВЕДЕНИЕ
window.strudel.evaluate(code)  // Единственный способ запуска паттернов
// Примеры:
strudel.evaluate('note("c4 e4 g4")')
strudel.evaluate('sound("bd hh")')

// 3. ОСТАНОВКА
const ctx = strudel.getAudioContext()
ctx.suspend()                  // ЕДИНСТВЕННЫЙ работающий способ остановки

// 4. УПРАВЛЕНИЕ СЕМПЛАМИ (когда починим)
strudel.loadSamples?('tr909')  // Предзагрузка банков
```

### ЗАПРЕЩЕННЫЕ МЕТОДЫ (НЕ РАБОТАЮТ / ЛОМАЮТ):

javascript

```
// ❌ НЕ ИСПОЛЬЗОВАТЬ:
strudel.hush()                 // Ошибка: scheduler undefined
strudel.scheduler              // undefined
strudel.stop()                 // Не существует
strudel.pause()                // Не существует

// ❌ НЕ ПЫТАТЬСЯ:
audioContext.close()           // Только при unmount компонента
audioContext.currentTime = 0   // Не сбрасывает паттерны
```

### СОСТОЯНИЯ AudioContext:

typescript

```
// Правильная последовательность:
"suspended" --resume()--> "running" --suspend()--> "suspended" --close()--> "closed"

// Мониторинг состояния:
audioContext.onstatechange = () => {
  console.log('AudioContext state:', audioContext.state);
};
```

---

## 💾 КОД-ШАБЛОНЫ

### 1\. МИНИМАЛЬНЫЙ РАБОЧИЙ КОМПОНЕНТ:

typescript

```
// StrudelREPLNode.tsx - минимальная рабочая версия
export const StrudelREPLNode: React.FC<Props> = ({ nodeId, core }) => {
  const [isPlaying, setIsPlaying] = useState(false);
  const audioContextRef = useRef<AudioContext | null>(null);

  // 1. ИНИЦИАЛИЗАЦИЯ
  useEffect(() => {
    const init = async () => {
      if (!window.strudel) {
        await loadScript('https://unpkg.com/@strudel/web@latest');
        window.initStrudel?.();
      }
      
      audioContextRef.current = window.strudel?.getAudioContext();
      
      // Начинаем в suspended (требование autoplay policy)
      if (audioContextRef.current?.state === 'running') {
        await audioContextRef.current.suspend();
      }
    };
    
    init();
    
    // 2. ОЧИСТКА
    return () => {
      if (audioContextRef.current?.state !== 'closed') {
        audioContextRef.current?.close();
      }
    };
  }, []);

  // 3. PLAY
  const handlePlay = async () => {
    if (!audioContextRef.current || !window.strudel) return;
    
    await audioContextRef.current.resume();
    window.strudel.evaluate('note("c4 e4 g4")');
    setIsPlaying(true);
  };

  // 4. STOP (ЕДИНСТВЕННЫЙ РАБОЧИЙ СПОСОБ)
  const handleStop = async () => {
    if (!audioContextRef.current) return;
    
    await audioContextRef.current.suspend();
    setIsPlaying(false);
  };

  return (
    <div>
      <button onClick={handlePlay} disabled={isPlaying}>Play</button>
      <button onClick={handleStop} disabled={!isPlaying}>Stop</button>
    </div>
  );
};
```

### 2\. ЗАГРУЗКА СЕМПЛОВ (РЕШЕНИЕ ПРОБЛЕМЫ 3):

typescript

```
// Дополнить в useEffect после инициализации:
const loadSamples = async () => {
  if (window.strudel?.loadSamples) {
    try {
      await window.strudel.loadSamples('tr909');
      console.log('Семплы tr909 загружены');
    } catch (error) {
      console.error('Ошибка загрузки семплов:', error);
    }
  }
};
```

---

## 🚫 КОНФЛИКТЫ И КАК ИХ ИЗБЕЖАТЬ

### 1\. КОНФЛИКТ С Tone.js:

typescript

```
// ПРОВЕРКА перед инициализацией Strudel:
if (window.Tone && window.Tone.context) {
  console.warn('⚠️ Tone.js уже использует AudioContext. Риск конфликта.');
  
  // Решение: Использовать контекст Tone.js
  if (window.strudel?.setAudioContext) {
    window.strudel.setAudioContext(window.Tone.context);
  }
}
```

### 2\. МНОЖЕСТВЕННЫЕ AudioContext:

javascript

```
// ДИАГНОСТИКА (выполнить в консоли):
function checkAudioContexts() {
  const contexts = [];
  for (let key in window) {
    try {
      const obj = window[key];
      if (obj instanceof AudioContext || 
          (obj.constructor && obj.constructor.name === 'AudioContext')) {
        contexts.push({ key, state: obj.state });
      }
    } catch(e) {}
  }
  console.log('Найдено AudioContext:', contexts);
}
```

### 3\. HMR КОНФЛИКТ (ПРОБЛЕМА 1):

typescript

```
// Добавить в начало StrudelREPLNode.tsx:
if (import.meta.hot) {
  // Отключаем HMR для этого файла
  import.meta.hot.decline();
  
  // Или ручная очистка:
  import.meta.hot.dispose(() => {
    const ctx = window.strudel?.getAudioContext();
    if (ctx?.state !== 'closed') {
      ctx?.suspend().then(() => ctx?.close());
    }
  });
}
```

---

## 🔧 DIAGNOSTIC PLAYBOOK

### КОГДА ЗВУК НЕ РАБОТАЕТ:

### КОГДА ЗВУК ЗАДВАИВАЕТСЯ:

1. Проверить сколько AudioContext:
	javascript
	```
	checkAudioContexts(); // из раздела выше
	```
2. Проверить не запущен ли паттерн дважды
3. Проверить конфликт с Tone.js

---

## 🧪 ТЕСТ-КЕЙСЫ ДЛЯ ПРОВЕРКИ

### ОБЯЗАТЕЛЬНЫЕ ТЕСТЫ:

markdown

```
- [ ] **TEST-001**: Чистая загрузка Strudel
  - Открыть test-strudel-web.html
  - Нажать "Initialize" → видим window.strudel
  - Нажать "Play" → слышим note("c4")
  - Нажать "Stop" → звук останавливается

- [ ] **TEST-002**: Интеграция в UCH
  - Добавить StrudelREPLNode в студию
  - Запустить паттерн → звук есть
  - Остановить → звук останавливается
  - Добавить Tone.js ноду → оба работают

- [ ] **TEST-003**: HMR безопасность
  - В dev-режиме изменить StrudelREPLNode.tsx
  - Сохранить → нет вечной загрузки
  - Компонент обновляется

- [ ] **TEST-004**: Семплы
  - Запустить: \`s("[bd hh]*4").bank("tr909")\`
  - Ожидание: звук семплов (не синус)
```

---

## 📈 МЕТРИКИ УСПЕХА

### ТЕХНИЧЕСКИЕ:

- ✅ Звук воспроизводится за < 500мс после клика
- ✅ Остановка работает за < 100мс
- ✅ Память не утекает при создании/удалении нод
- ✅ HMR не ломает состояние

### ПОЛЬЗОВАТЕЛЬСКИЕ:

- ✅ Можно написать паттерн → нажать Play → услышать
- ✅ Можно нажать Stop → звук останавливается
- ✅ Можно использовать с другими аудио-нодами

---

## 🔄 ПРОЦЕСС ИЗМЕНЕНИЯ СПЕЦИФИКАЦИИ

1. Обнаружили проблему → записали в "НЕ РАБОТАЕТ"
2. Нашли решение → обновили "КОНТРАКТ API"
3. Протестировали → добавили в "ТЕСТ-КЕЙСЫ"
4. Утвердили → увеличили версию (v1.0 → v1.1)

---

## 🆘 ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### ОШИБКА: sound tr909\_bd not found!

**Решение:** Добавить `await strudel.loadSamples('tr909')` перед использованием

### ОШИБКА: Cannot read properties of undefined (reading 'stop')

**Причина:** Используете `strudel.hush()`  
**Решение:** Использовать только `audioContext.suspend()`

### ОШИБКА: Вечная загрузка при сохранении файла

**Причина:** HMR конфликт  
**Решение:** Добавить `import.meta.hot.decline()`

### ПРОБЛЕМА: Звук запускается сам при загрузке

**Причина:** AudioContext в состоянии 'running'  
**Решение:** При инициализации вызывать `audioContext.suspend()`

---

## 📁 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `4-129 Прогресс реализации Фаза 2.md` \- текущий статус

---