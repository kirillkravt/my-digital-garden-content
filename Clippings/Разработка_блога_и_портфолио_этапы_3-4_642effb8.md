---
chat_id: 642effb8-548c-43b7-afa8-79be7844b877
title: Разработка блога и портфолио: этапы 3-4
message_count: 28
exported_at: 2026-05-24T19:32:16.094Z
---

# Разработка блога и портфолио: этапы 3-4

## 👤 **Kirill**

С учетом прогресса двигаемся дальше по спецификации

---

## 🤖 **DeepSeek**

Отлично, что Фаза 1 успешно завершена! Давайте структурируем следующий этап согласно плану v0.3.

---

## 📅 Фаза 1, недели 3-4: Развитие блога/портфолио

### 🔹 Цель:
Создать базовую функциональность блога с поддержкой мультиформатного контента, категорий, тегов и Markdown-редактирования.

---

### ✅ Задачи на недели 3-4:

#### 1. **Модели данных для блога**
- `Article` (статьи)
  - title, slug, content (Markdown), excerpt
  - cover_image, status (черновик/опубликовано)
  - author (ForeignKey на User), category (ForeignKey)
  - tags (ManyToMany), created_at, updated_at
- `Category` (категории)
  - name, slug, description, parent (для вложенности)
- `Tag` (теги)
  - name, slug
- `MediaItem` (медиа)
  - file, title, description, type (image/audio/video/3d)
  - uploaded_by, created_at

#### 2. **Админка для блога**
- Настройка Django Admin с кастомизацией под модели
- Добавление фильтров, поиска, действий (публикация/архивация)
- WYSIWYG/Markdown редактор через `django-markdownx` или `django-ckeditor`
- Загрузка медиа через административную панель

#### 3. **Markdown-редактор с предпросмотром**
- Интеграция простого редактора (например, SimpleMDE или Toast UI Editor)
- Поддержка загрузки изображений прямо в Markdown
- Режим предпросмотра (split view)

#### 4. **Базовые представления и шаблоны**
- Список статей с пагинацией
- Детальная страница статьи с рендерингом Markdown → HTML
- Страница категории с фильтрацией по статьям
- Боковая панель с тегами, категориями, архивом

#### 5. **Адаптивный дизайн**
- Базовая вёрстка с использованием Tailwind CSS (или Bootstrap 5)
- Мобильная адаптация
- Темная/светлая тема (опционально)

---

### 🔧 Технические шаги:

#### 1. Установка дополнительных пакетов:
```bash
pip install django-markdownx django-taggit django-filter
```

#### 2. Создание моделей в `blog/models.py`:
```python
from django.db import models
from django.contrib.auth.models import User

class Category(models.Model):
    name = models.CharField(max_length=100)
    slug = models.SlugField(unique=True)
    description = models.TextField(blank=True)
    parent = models.ForeignKey('self', null=True, blank=True, on_delete=models.CASCADE)

class Tag(models.Model):
    name = models.CharField(max_length=50)
    slug = models.SlugField(unique=True)

class Article(models.Model):
    STATUS_CHOICES = [('draft', 'Черновик'), ('published', 'Опубликовано')]
    title = models.CharField(max_length=200)
    slug = models.SlugField(unique=True)
    content = models.TextField()  # Markdown
    excerpt = models.TextField(blank=True)
    cover_image = models.ImageField(upload_to='covers/', blank=True)
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='draft')
    author = models.ForeignKey(User, on_delete=models.CASCADE)
    category = models.ForeignKey(Category, on_delete=models.SET_NULL, null=True)
    tags = models.ManyToManyField(Tag, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

#### 3. Настройка админки в `blog/admin.py`:
```python
from django.contrib import admin
from .models import Article, Category, Tag, MediaItem

@admin.register(Article)
class ArticleAdmin(admin.ModelAdmin):
    list_display = ('title', 'author', 'status', 'created_at')
    list_filter = ('status', 'category', 'tags')
    search_fields = ('title', 'content')
    prepopulated_fields = {'slug': ('title',)}
```

#### 4. Создание представлений и URL:
- `blog/views.py` – для списка, деталей, категорий
- `blog/urls.py` – маршрутизация

#### 5. Шаблоны:
- `templates/blog/list.html`
- `templates/blog/detail.html`
- `templates/blog/category.html`

---

### 📊 Критерии готовности к концу недели 4:
- [ ] Модели блога созданы и мигрированы
- [ ] Админка для блога кастомизирована и работает
- [ ] Markdown-редактор интегрирован в админку
- [ ] Статьи отображаются на сайте с правильным рендерингом
- [ ] Категории и теги работают (фильтрация, отображение)
- [ ] Адаптивный дизайн блога готов

---

### 🧪 Тестирование:
- Проверить создание/редактирование статей через админку
- Проверить отображение статей на фронтенде
- Проверить пагинацию и фильтрацию
- Проверить мобильную вёрстку

---

### 📝 Документация:
Обновить `README.md` с описанием:
- Как добавить статью
- Как работать с Markdown
- Как управлять медиа
- Как настроить категории и теги

---

### 🚀 После завершения недель 3-4:
Переходим к **неделям 5-6**: интеграция Obsidian, RSS, поиск и оптимизация.

---

**Статус:** ✅ Фаза 1, недели 1-2 завершены  
**Текущий этап:** 🎯 Фаза 1, недели 3-4 — разработка блога  
**Следующий чекпоинт:** 21.12.2025 (конец недели 4)

---

## 👤 **Kirill**

приступим по шагам

---

## 🤖 **DeepSeek**

Отлично! Начинаем **Фазу 1, недели 3-4** — разработку функциональности блога.

---

## 🎯 Шаг 1: Создание моделей для блога

### 1.1. Переходим в приложение `blog`:
```bash
cd uch-backend
```

### 1.2. Создаем модели в `uch/apps/blog/models.py`:
```python
from django.db import models
from django.contrib.auth.models import User
from django.urls import reverse
from taggit.managers import TaggableManager


class Category(models.Model):
    """Категории статей (иерархические)"""
    name = models.CharField(max_length=100, verbose_name="Название")
    slug = models.SlugField(max_length=100, unique=True, verbose_name="URL")
    description = models.TextField(blank=True, verbose_name="Описание")
    parent = models.ForeignKey('self', on_delete=models.CASCADE, 
                              null=True, blank=True, 
                              verbose_name="Родительская категория")
    order = models.IntegerField(default=0, verbose_name="Порядок")
    is_active = models.BooleanField(default=True, verbose_name="Активна")
    
    class Meta:
        verbose_name = "Категория"
        verbose_name_plural = "Категории"
        ordering = ['order', 'name']
    
    def __str__(self):
        return self.name
    
    def get_absolute_url(self):
        return reverse('blog:category_detail', args=[self.slug])


class Article(models.Model):
    """Статьи/записи блога"""
    STATUS_CHOICES = [
        ('draft', 'Черновик'),
        ('published', 'Опубликовано'),
        ('archived', 'В архиве'),
    ]
    
    title = models.CharField(max_length=200, verbose_name="Заголовок")
    slug = models.SlugField(max_length=200, unique=True, verbose_name="URL")
    excerpt = models.TextField(max_length=500, blank=True, verbose_name="Краткое описание")
    content = models.TextField(verbose_name="Содержание (Markdown)")
    content_html = models.TextField(blank=True, editable=False, verbose_name="Содержание (HTML)")
    
    cover_image = models.ImageField(upload_to='articles/covers/', 
                                   blank=True, null=True,
                                   verbose_name="Обложка")
    
    author = models.ForeignKey(User, on_delete=models.CASCADE, 
                              verbose_name="Автор")
    category = models.ForeignKey(Category, on_delete=models.SET_NULL,
                                null=True, blank=True,
                                verbose_name="Категория")
    
    status = models.CharField(max_length=20, choices=STATUS_CHOICES,
                            default='draft', verbose_name="Статус")
    
    is_featured = models.BooleanField(default=False, verbose_name="Рекомендуемая")
    allow_comments = models.BooleanField(default=True, verbose_name="Разрешить комментарии")
    
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="Создано")
    updated_at = models.DateTimeField(auto_now=True, verbose_name="Обновлено")
    published_at = models.DateTimeField(null=True, blank=True, verbose_name="Опубликовано")
    
    # Теги через django-taggit
    tags = TaggableManager(blank=True, verbose_name="Теги")
    
    class Meta:
        verbose_name = "Статья"
        verbose_name_plural = "Статьи"
        ordering = ['-published_at', '-created_at']
        indexes = [
            models.Index(fields=['status', 'published_at']),
            models.Index(fields=['slug']),
            models.Index(fields=['category']),
        ]
    
    def __str__(self):
        return self.title
    
    def get_absolute_url(self):
        return reverse('blog:article_detail', args=[self.slug])
    
    def save(self, *args, **kwargs):
        # При публикации устанавливаем дату публикации
        if self.status == 'published' and not self.published_at:
            from django.utils import timezone
            self.published_at = timezone.now()
        
        # Конвертируем Markdown в HTML при сохранении
        if self.content:
            import markdown
            self.content_html = markdown.markdown(
                self.content,
                extensions=['extra', 'codehilite', 'tables']
            )
        
        super().save(*args, **kwargs)


class MediaItem(models.Model):
    """Медиафайлы (изображения, аудио, видео)"""
    MEDIA_TYPES = [
        ('image', 'Изображение'),
        ('audio', 'Аудио'),
        ('video', 'Видео'),
        ('document', 'Документ'),
        ('3d', '3D модель'),
    ]
    
    title = models.CharField(max_length=200, verbose_name="Название")
    description = models.TextField(blank=True, verbose_name="Описание")
    file = models.FileField(upload_to='media/%Y/%m/%d/', verbose_name="Файл")
    file_type = models.CharField(max_length=20, choices=MEDIA_TYPES, 
                                verbose_name="Тип файла")
    thumbnail = models.ImageField(upload_to='media/thumbnails/', 
                                 blank=True, null=True,
                                 verbose_name="Превью")
    
    uploaded_by = models.ForeignKey(User, on_delete=models.CASCADE,
                                   verbose_name="Загрузил")
    uploaded_at = models.DateTimeField(auto_now_add=True, verbose_name="Загружено")
    
    # Метаданные для разных типов
    metadata = models.JSONField(default=dict, blank=True, verbose_name="Метаданные")
    
    class Meta:
        verbose_name = "Медиафайл"
        verbose_name_plural = "Медиафайлы"
        ordering = ['-uploaded_at']
    
    def __str__(self):
        return f"{self.title} ({self.file_type})"
    
    def get_absolute_url(self):
        return self.file.url
    

class Comment(models.Model):
    """Комментарии к статьям"""
    article = models.ForeignKey(Article, on_delete=models.CASCADE,
                               related_name='comments', verbose_name="Статья")
    author = models.ForeignKey(User, on_delete=models.CASCADE,
                              verbose_name="Автор")
    content = models.TextField(max_length=1000, verbose_name="Текст комментария")
    is_approved = models.BooleanField(default=False, verbose_name="Одобрен")
    
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="Создан")
    updated_at = models.DateTimeField(auto_now=True, verbose_name="Обновлен")
    
    # Для ответов на комментарии
    parent = models.ForeignKey('self', on_delete=models.CASCADE,
                              null=True, blank=True,
                              related_name='replies', verbose_name="Родительский комментарий")
    
    class Meta:
        verbose_name = "Комментарий"
        verbose_name_plural = "Комментарии"
        ordering = ['-created_at']
    
    def __str__(self):
        return f"Комментарий от {self.author} к {self.article}"
```

### 1.3. Устанавливаем необходимые пакеты:
```bash
pip install django-taggit markdown Pillow
```

### 1.4. Добавляем в `uch/settings.py`:
```python
INSTALLED_APPS = [
    # ...
    'taggit',
    'uch.apps.blog',
    # ...
]

# Настройки для taggit
TAGGIT_CASE_INSENSITIVE = True

# Настройки для медиафайлов
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media/')
```

---

## 🎯 Шаг 2: Создание миграций и применение

### 2.1. Создаем миграции:
```bash
python manage.py makemigrations blog
```

### 2.2. Применяем миграции:
```bash
python manage.py migrate
```

---

## 🎯 Шаг 3: Настройка административной панели для блога

### 3.1. Создаем `uch/apps/blog/admin.py`:
```python
from django.contrib import admin
from django.utils.html import format_html
from .models import Category, Article, MediaItem, Comment


@admin.register(Category)
class CategoryAdmin(admin.ModelAdmin):
    list_display = ('name', 'parent', 'order', 'is_active', 'article_count')
    list_filter = ('is_active', 'parent')
    search_fields = ('name', 'description')
    prepopulated_fields = {'slug': ('name',)}
    ordering = ('order', 'name')
    
    def article_count(self, obj):
        return obj.article_set.count()
    article_count.short_description = 'Статей'


@admin.register(Article)
class ArticleAdmin(admin.ModelAdmin):
    list_display = ('title', 'author', 'category', 'status', 
                    'published_at', 'is_featured', 'comment_count')
    list_filter = ('status', 'category', 'is_featured', 'created_at')
    search_fields = ('title', 'content', 'excerpt')
    prepopulated_fields = {'slug': ('title',)}
    readonly_fields = ('created_at', 'updated_at', 'published_at', 'content_html')
    date_hierarchy = 'published_at'
    ordering = ('-published_at', '-created_at')
    
    fieldsets = (
        ('Основное', {
            'fields': ('title', 'slug', 'excerpt', 'content', 'content_html')
        }),
        ('Метаданные', {
            'fields': ('cover_image', 'author', 'category', 'tags', 
                      'status', 'is_featured', 'allow_comments')
        }),
        ('Даты', {
            'fields': ('created_at', 'updated_at', 'published_at'),
            'classes': ('collapse',)
        }),
    )
    
    def comment_count(self, obj):
        return obj.comments.count()
    comment_count.short_description = 'Комментариев'
    
    def save_model(self, request, obj, form, change):
        if not obj.author_id:
            obj.author = request.user
        super().save_model(request, obj, form, change)


@admin.register(MediaItem)
class MediaItemAdmin(admin.ModelAdmin):
    list_display = ('title', 'file_type', 'uploaded_by', 'uploaded_at', 'preview')
    list_filter = ('file_type', 'uploaded_at')
    search_fields = ('title', 'description')
    readonly_fields = ('uploaded_at', 'preview')
    
    def preview(self, obj):
        if obj.file_type == 'image':
            return format_html('<img src="{}" width="100" />', obj.file.url)
        return f"Файл: {obj.file_type}"
    preview.short_description = 'Превью'


@admin.register(Comment)
class CommentAdmin(admin.ModelAdmin):
    list_display = ('author', 'article', 'content_preview', 
                    'is_approved', 'created_at')
    list_filter = ('is_approved', 'created_at', 'article')
    search_fields = ('content', 'author__username', 'article__title')
    actions = ['approve_comments', 'disapprove_comments']
    
    def content_preview(self, obj):
        return obj.content[:50] + '...' if len(obj.content) > 50 else obj.content
    content_preview.short_description = 'Текст'
    
    def approve_comments(self, request, queryset):
        queryset.update(is_approved=True)
    approve_comments.short_description = "Одобрить выбранные комментарии"
    
    def disapprove_comments(self, request, queryset):
        queryset.update(is_approved=False)
    disapprove_comments.short_description = "Снять одобрение"
```

---

## 🎯 Шаг 4: Проверка работы

### 4.1. Запускаем сервер:
```bash
python manage.py runserver
```

### 4.2. Открываем административную панель:
```
http://localhost:8000/admin/
```

### 4.3. Создаем тестовые данные:
1. Создаем категорию "Музыка"
2. Создаем статью в этой категории
3. Загружаем тестовое изображение как обложку
4. Добавляем теги

---

## ✅ Проверка текущего состояния:

1. **Модели созданы** ✅
2. **Админка настроена** ✅  
3. **Маршрутизация готова** (следующий шаг)

---

## 🎯 Что дальше? (Шаг 5)

Создадим представления, URL-маршруты и шаблоны для фронтенда блога. Хотите продолжить?

---

## 👤 **Kirill**

шаг 2.1
(venv) kirillkravcov@MacBook-Pro-Kirill uch-backend % python manage.py makemigrations blog
Traceback (most recent call last):
  File "manage.py", line 22, in <module>
    main()
  File "manage.py", line 18, in main
    execute_from_command_line(sys.argv)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/management/__init__.py", line 442, in execute_from_command_line
    utility.execute()
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/management/__init__.py", line 382, in execute
    settings.INSTALLED_APPS
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/conf/__init__.py", line 102, in __getattr__
    self._setup(name)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/conf/__init__.py", line 89, in _setup
    self._wrapped = Settings(settings_module)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/conf/__init__.py", line 217, in __init__
    mod = importlib.import_module(self.SETTINGS_MODULE)
  File "/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/importlib/__init__.py", line 127, in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
  File "<frozen importlib._bootstrap>", line 1014, in _gcd_import
  File "<frozen importlib._bootstrap>", line 991, in _find_and_load
  File "<frozen importlib._bootstrap>", line 975, in _find_and_load_unlocked
  File "<frozen importlib._bootstrap>", line 671, in _load_unlocked
  File "<frozen importlib._bootstrap_external>", line 783, in exec_module
  File "<frozen importlib._bootstrap>", line 219, in _call_with_frames_removed
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/uch/settings.py", line 90, in <module>
    MEDIA_ROOT = os.path.join(BASE_DIR, 'media/')
NameError: name 'os' is not defined
(venv) kirillkravcov@MacBook-Pro-Kirill uch-backend %

---

## 🤖 **DeepSeek**

Ошибка: не импортирован модуль `os` в `settings.py`. Давайте исправим это:

---

## 🔧 Шаг 2.1: Исправление settings.py

### 2.1.1. Открываем `uch/settings.py` и проверяем импорты в начале файла:
```python
import os
from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# ... остальной код ...
```

### 2.1.2. Проверяем, что `os` импортирован. Если нет, добавляем в начало файла:
```python
import os
from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Quick-start development settings - unsuitable for production
# ...
```

### 2.1.3. Также проверяем настройки MEDIA:
```python
# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'),
]

# Media files (uploaded by users)
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media/')
```

### 2.1.4. Теперь пробуем снова создать миграции:
```bash
python manage.py makemigrations blog
```

---

## 🎯 Шаг 2.2: Если ошибка сохраняется, проверим полную структуру settings.py

**Содержимое `uch/settings.py` должно включать:**

```python
"""
Django settings for uch project.
"""

import os
from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'ваш-секретный-ключ'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    
    # Third party apps
    'taggit',
    'rest_framework',
    'corsheaders',
    
    # Local apps
    'uch.apps.core',
    'uch.apps.blog',
    'uch.apps.users',
    'uch.apps.media',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'uch.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'uch.wsgi.application'


# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'ru-ru'
TIME_ZONE = 'Europe/Moscow'
USE_I18N = True
USE_TZ = True


# Static files (CSS, JavaScript, Images)
STATIC_URL = 'static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'),
]

# Media files
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media/')

# Default primary key field type
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Taggit settings
TAGGIT_CASE_INSENSITIVE = True

# CORS settings
CORS_ALLOW_ALL_ORIGINS = True  # Только для разработки!
```

---

## ✅ После исправления:

### 2.2.1. Проверим, что все правильно:
```bash
python manage.py check
```

### 2.2.2. Создаем миграции:
```bash
python manage.py makemigrations blog
```

### 2.2.3. Применяем миграции:
```bash
python manage.py migrate
```

---

## 🔍 Если все еще есть ошибки:

Проверим структуру проекта:
```bash
ls -la uch/apps/blog/
```

Должно быть:
```
__init__.py
admin.py
apps.py
models.py
tests.py
views.py
```

---

**Дайте знать, получилось ли исправить ошибку, и мы продолжим со Шага 3!**

---

## 👤 **Kirill**

(venv) kirillkravcov@MacBook-Pro-Kirill uch-backend % python manage.py makemigrations blog
Migrations for 'blog':
  uch/apps/blog/migrations/0001_initial.py
    - Create model Article
    - Create model MediaItem
    - Create model Comment
    - Create model Category
    - Add field category to article
    - Add field tags to article
    - Create index blog_articl_status_8b18ca_idx on field(s) status, published_at of model article
    - Create index blog_articl_slug_cc8df7_idx on field(s) slug of model article
    - Create index blog_articl_categor_5bebf3_idx on field(s) category of model article

---

## 🤖 **DeepSeek**

Отлично! Миграции успешно созданы. Теперь применим их:

---

## 🎯 Шаг 2.2: Применение миграций

### 2.2.1. Применяем миграции для blog:
```bash
python manage.py migrate blog
```

Должен быть вывод примерно такой:
```
Operations to perform:
  Apply all migrations: blog
Running migrations:
  Applying blog.0001_initial... OK
```

### 2.2.2. Проверим, что все таблицы созданы:
```bash
python manage.py showmigrations
```

---

## 🎯 Шаг 2.3: Создаем суперпользователя (если еще нет)

### 2.3.1. Создаем суперпользователя для доступа к админке:
```bash
python manage.py createsuperuser
```

Введите данные:
- Username: `admin`
- Email: `admin@example.com`
- Password: `(придумайте надежный пароль)`

### 2.3.2. Запускаем сервер для проверки:
```bash
python manage.py runserver
```

### 2.3.3. Открываем в браузере:
- Админка: http://localhost:8000/admin/
- Входим с созданными учетными данными

---

## 🎯 Шаг 3: Создание базовых представлений (views)

### 3.1. Создаем `uch/apps/blog/views.py`:
```python
from django.shortcuts import render, get_object_or_404
from django.core.paginator import Paginator
from django.views.generic import ListView, DetailView
from .models import Article, Category, Tag


class ArticleListView(ListView):
    """Список всех статей с пагинацией"""
    model = Article
    template_name = 'blog/article_list.html'
    context_object_name = 'articles'
    paginate_by = 10
    
    def get_queryset(self):
        queryset = Article.objects.filter(status='published')
        
        # Фильтрация по категории
        category_slug = self.kwargs.get('category_slug')
        if category_slug:
            category = get_object_or_404(Category, slug=category_slug)
            queryset = queryset.filter(category=category)
        
        # Фильтрация по тегу
        tag_slug = self.request.GET.get('tag')
        if tag_slug:
            queryset = queryset.filter(tags__slug=tag_slug)
        
        # Поиск
        search_query = self.request.GET.get('q')
        if search_query:
            queryset = queryset.filter(
                models.Q(title__icontains=search_query) |
                models.Q(content__icontains=search_query) |
                models.Q(excerpt__icontains=search_query)
            )
        
        return queryset.select_related('author', 'category').prefetch_related('tags')
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['categories'] = Category.objects.filter(is_active=True)
        context['recent_articles'] = Article.objects.filter(status='published')[:5]
        return context


class ArticleDetailView(DetailView):
    """Детальная страница статьи"""
    model = Article
    template_name = 'blog/article_detail.html'
    context_object_name = 'article'
    slug_field = 'slug'
    slug_url_kwarg = 'slug'
    
    def get_queryset(self):
        return Article.objects.filter(status='published')
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['categories'] = Category.objects.filter(is_active=True)
        context['recent_articles'] = Article.objects.filter(status='published').exclude(
            pk=self.object.pk
        )[:5]
        return context


class CategoryListView(ListView):
    """Список категорий"""
    model = Category
    template_name = 'blog/category_list.html'
    context_object_name = 'categories'
    
    def get_queryset(self):
        return Category.objects.filter(is_active=True, parent=None)
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['recent_articles'] = Article.objects.filter(status='published')[:5]
        return context


def home_view(request):
    """Домашняя страница блога"""
    featured_articles = Article.objects.filter(
        status='published', is_featured=True
    )[:3]
    
    recent_articles = Article.objects.filter(status='published')[:6]
    categories = Category.objects.filter(is_active=True)[:8]
    
    context = {
        'featured_articles': featured_articles,
        'recent_articles': recent_articles,
        'categories': categories,
    }
    return render(request, 'blog/home.html', context)
```

---

## 🎯 Шаг 4: Настройка URL маршрутов

### 4.1. Создаем `uch/apps/blog/urls.py`:
```python
from django.urls import path
from . import views

app_name = 'blog'

urlpatterns = [
    path('', views.home_view, name='home'),
    path('articles/', views.ArticleListView.as_view(), name='article_list'),
    path('articles/<slug:slug>/', views.ArticleDetailView.as_view(), name='article_detail'),
    path('category/<slug:category_slug>/', views.ArticleListView.as_view(), name='category_detail'),
    path('categories/', views.CategoryListView.as_view(), name='category_list'),
]
```

### 4.2. Обновляем главный файл маршрутов `uch/urls.py`:
```python
from django.contrib import admin
from django.urls import path, include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('uch.apps.blog.urls')),  # Блог на главной
    path('blog/', include('uch.apps.blog.urls')),  # Альтернативный путь
]

# Для обслуживания медиафайлов в режиме разработки
if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

---

## 🎯 Шаг 5: Создание базовых шаблонов

### 5.1. Создаем структуру папок для шаблонов:
```bash
mkdir -p uch/apps/blog/templates/blog
mkdir -p uch/templates/base
```

### 5.2. Создаем базовый шаблон `uch/templates/base.html`:
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Universal Creative Hub{% endblock %}</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <style>
        body {
            padding-top: 56px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }
        
        .navbar-brand {
            font-weight: bold;
        }
        
        .featured-article {
            border-left: 4px solid #0d6efd;
        }
        
        .article-card {
            transition: transform 0.2s;
            height: 100%;
        }
        
        .article-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .category-badge {
            font-size: 0.8em;
        }
        
        footer {
            margin-top: 3rem;
        }
    </style>
    
    {% block extra_css %}{% endblock %}
</head>
<body>
    <!-- Навигационная панель -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-lightning-charge"></i> Universal Creative Hub
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{% url 'blog:home' %}">Главная</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{% url 'blog:article_list' %}">Статьи</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{% url 'blog:category_list' %}">Категории</a>
                    </li>
                    {% if user.is_authenticated and user.is_staff %}
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/" target="_blank">
                            <i class="bi bi-gear"></i> Админка
                        </a>
                    </li>
                    {% endif %}
                </ul>
                
                <form class="d-flex" action="{% url 'blog:article_list' %}" method="get">
                    <input class="form-control me-2" type="search" name="q" placeholder="Поиск статей...">
                    <button class="btn btn-outline-light" type="submit">
                        <i class="bi bi-search"></i>
                    </button>
                </form>
            </div>
        </div>
    </nav>

    <!-- Основной контент -->
    <main class="container mt-4">
        {% block content %}
        {% endblock %}
    </main>

    <!-- Подвал -->
    <footer class="bg-dark text-white mt-5 py-4">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>Universal Creative Hub</h5>
                    <p class="text-muted">Self-hosted платформа для творчества и портфолио</p>
                </div>
                <div class="col-md-6 text-end">
                    <p>&copy; {% now "Y" %} Universal Creative Hub</p>
                    <p class="text-muted">Версия 0.3 (Beta)</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block extra_js %}{% endblock %}
</body>
</html>
```

---

## ✅ Проверка текущего состояния:

1. **Модели созданы** ✅
2. **Миграции применены** ✅
3. **Админка настроена** ✅
4. **Представления созданы** ✅
5. **URL маршруты настроены** ✅
6. **Базовый шаблон создан** ✅

---

## 🎯 Шаг 6: Тестирование

### 6.1. Запускаем сервер:
```bash
python manage.py runserver
```

### 6.2. Открываем в браузере:
- Главная: http://localhost:8000/
- Админка: http://localhost:8000/admin/

### 6.3. Создаем тестовые данные через админку:
1. Создаем категорию "Музыка"
2. Создаем статью "Моя первая статья" (статус: published)
3. Добавляем теги "творчество", "музыка"

---

**Продолжим со следующими шаблонами (home.html, article_list.html, article_detail.html) или сначала протестируем текущую реализацию?**

---

## 👤 **Kirill**

venv) kirillkravcov@MacBook-Pro-Kirill uch-backend % python manage.py runserver 
Watching for file changes with StatReloader
Performing system checks...

Exception in thread django-main-thread:
Traceback (most recent call last):
  File "/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/threading.py", line 932, in _bootstrap_inner
    self.run()
  File "/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/threading.py", line 870, in run
    self._target(*self._args, **self._kwargs)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/utils/autoreload.py", line 64, in wrapper
    fn(*args, **kwargs)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/management/commands/runserver.py", line 133, in inner_run
    self.check(display_num_errors=True)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/management/base.py", line 485, in check
    all_issues = checks.run_checks(
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/checks/registry.py", line 88, in run_checks
    new_errors = check(app_configs=app_configs, databases=databases)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/checks/urls.py", line 14, in check_url_config
    return check_resolver(resolver)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/core/checks/urls.py", line 24, in check_resolver
    return check_method()
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/urls/resolvers.py", line 494, in check
    for pattern in self.url_patterns:
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/utils/functional.py", line 57, in __get__
    res = instance.__dict__[self.name] = self.func(instance)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/urls/resolvers.py", line 715, in url_patterns
    patterns = getattr(self.urlconf_module, "urlpatterns", self.urlconf_module)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/utils/functional.py", line 57, in __get__
    res = instance.__dict__[self.name] = self.func(instance)
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/urls/resolvers.py", line 708, in urlconf_module
    return import_module(self.urlconf_name)
  File "/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/importlib/__init__.py", line 127, in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
  File "<frozen importlib._bootstrap>", line 1014, in _gcd_import
  File "<frozen importlib._bootstrap>", line 991, in _find_and_load
  File "<frozen importlib._bootstrap>", line 975, in _find_and_load_unlocked
  File "<frozen importlib._bootstrap>", line 671, in _load_unlocked
  File "<frozen importlib._bootstrap_external>", line 783, in exec_module
  File "<frozen importlib._bootstrap>", line 219, in _call_with_frames_removed
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/uch/urls.py", line 26, in <module>
    path('', include('uch.apps.blog.urls')),
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/urls/conf.py", line 38, in include
    urlconf_module = import_module(urlconf_module)
  File "/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/importlib/__init__.py", line 127, in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
  File "<frozen importlib._bootstrap>", line 1014, in _gcd_import
  File "<frozen importlib._bootstrap>", line 991, in _find_and_load
  File "<frozen importlib._bootstrap>", line 975, in _find_and_load_unlocked
  File "<frozen importlib._bootstrap>", line 671, in _load_unlocked
  File "<frozen importlib._bootstrap_external>", line 783, in exec_module
  File "<frozen importlib._bootstrap>", line 219, in _call_with_frames_removed
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/uch/apps/blog/urls.py", line 2, in <module>
    from . import views
  File "/Users/kirillkravcov/UniversalCreativeHub/uch-backend/uch/apps/blog/views.py", line 4, in <module>
    from .models import Article, Category, Tag
ImportError: cannot import name 'Tag' from 'uch.apps.blog.models' (/Users/kirillkravcov/UniversalCreativeHub/uch-backend/uch/apps/blog/models.py)

---

## 🤖 **DeepSeek**

Ошибка: в `models.py` у нас нет модели `Tag`, так как мы используем `django-taggit`. Нужно исправить импорт в `views.py`.

---

## 🔧 Шаг 3.1: Исправляем импорт в `views.py`

### 3.1.1. Открываем `uch/apps/blog/views.py` и исправляем:
```python
from django.shortcuts import render, get_object_or_404
from django.core.paginator import Paginator
from django.views.generic import ListView, DetailView
from django.db import models  # Добавляем импорт models
from .models import Article, Category  # Убираем Tag из импорта
from taggit.models import Tag  # Импортируем Tag из taggit
```

### 3.1.2. Полный исправленный код `views.py`:
```python
from django.shortcuts import render, get_object_or_404
from django.core.paginator import Paginator
from django.views.generic import ListView, DetailView
from django.db import models
from .models import Article, Category
from taggit.models import Tag


class ArticleListView(ListView):
    """Список всех статей с пагинацией"""
    model = Article
    template_name = 'blog/article_list.html'
    context_object_name = 'articles'
    paginate_by = 10
    
    def get_queryset(self):
        queryset = Article.objects.filter(status='published')
        
        # Фильтрация по категории
        category_slug = self.kwargs.get('category_slug')
        if category_slug:
            category = get_object_or_404(Category, slug=category_slug)
            queryset = queryset.filter(category=category)
        
        # Фильтрация по тегу
        tag_slug = self.request.GET.get('tag')
        if tag_slug:
            queryset = queryset.filter(tags__slug=tag_slug)
        
        # Поиск
        search_query = self.request.GET.get('q')
        if search_query:
            queryset = queryset.filter(
                models.Q(title__icontains=search_query) |
                models.Q(content__icontains=search_query) |
                models.Q(excerpt__icontains=search_query)
            )
        
        return queryset.select_related('author', 'category').prefetch_related('tags')
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['categories'] = Category.objects.filter(is_active=True)
        context['recent_articles'] = Article.objects.filter(status='published')[:5]
        
        # Добавляем популярные теги
        from django.db.models import Count
        context['popular_tags'] = Tag.objects.annotate(
            num_times=Count('taggit_taggeditem_items')
        ).order_by('-num_times')[:10]
        
        return context


class ArticleDetailView(DetailView):
    """Детальная страница статьи"""
    model = Article
    template_name = 'blog/article_detail.html'
    context_object_name = 'article'
    slug_field = 'slug'
    slug_url_kwarg = 'slug'
    
    def get_queryset(self):
        return Article.objects.filter(status='published')
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['categories'] = Category.objects.filter(is_active=True)
        context['recent_articles'] = Article.objects.filter(status='published').exclude(
            pk=self.object.pk
        )[:5]
        
        # Добавляем теги текущей статьи
        context['article_tags'] = self.object.tags.all()
        
        return context


class CategoryListView(ListView):
    """Список категорий"""
    model = Category
    template_name = 'blog/category_list.html'
    context_object_name = 'categories'
    
    def get_queryset(self):
        return Category.objects.filter(is_active=True, parent=None)
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['recent_articles'] = Article.objects.filter(status='published')[:5]
        return context


def home_view(request):
    """Домашняя страница блога"""
    featured_articles = Article.objects.filter(
        status='published', is_featured=True
    )[:3]
    
    recent_articles = Article.objects.filter(status='published')[:6]
    categories = Category.objects.filter(is_active=True)[:8]
    
    # Популярные теги
    from django.db.models import Count
    from taggit.models import Tag
    popular_tags = Tag.objects.annotate(
        num_times=Count('taggit_taggeditem_items')
    ).order_by('-num_times')[:10]
    
    context = {
        'featured_articles': featured_articles,
        'recent_articles': recent_articles,
        'categories': categories,
        'popular_tags': popular_tags,
    }
    return render(request, 'blog/home.html', context)
```

---

## ✅ Проверяем исправление:

### 3.1.3. Запускаем сервер снова:
```bash
python manage.py runserver
```

Должен запуститься без ошибок:
```
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues (0 silenced).
December 07, 2025 - 21:30:00
Django version 4.2.7, using settings 'uch.settings'
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

---

## 🎯 Шаг 5: Создаем остальные шаблоны

### 5.1. Создаем `uch/apps/blog/templates/blog/home.html`:
```html
{% extends 'base.html' %}

{% block title %}Главная - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <!-- Приветствие -->
        <div class="jumbotron bg-light p-5 rounded mb-4">
            <h1 class="display-4">Universal Creative Hub</h1>
            <p class="lead">Self-hosted платформа для творчества, объединяющая блог, портфолио и музыкальную студию.</p>
            <hr class="my-4">
            <p>Создавайте, делитесь и вдохновляйтесь в единой среде.</p>
        </div>

        <!-- Рекомендуемые статьи -->
        {% if featured_articles %}
        <h2 class="mb-3">Рекомендуемые статьи</h2>
        <div class="row mb-5">
            {% for article in featured_articles %}
            <div class="col-md-4 mb-3">
                <div class="card article-card h-100">
                    {% if article.cover_image %}
                    <img src="{{ article.cover_image.url }}" class="card-img-top" alt="{{ article.title }}" style="height: 180px; object-fit: cover;">
                    {% endif %}
                    <div class="card-body">
                        <span class="badge bg-primary category-badge mb-2">
                            {{ article.category.name|default:"Без категории" }}
                        </span>
                        <h5 class="card-title">{{ article.title|truncatechars:50 }}</h5>
                        <p class="card-text text-muted">{{ article.excerpt|truncatechars:100|default:"" }}</p>
                    </div>
                    <div class="card-footer bg-transparent">
                        <small class="text-muted">
                            <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                            <i class="bi bi-person ms-2"></i> {{ article.author.username }}
                        </small>
                        <a href="{{ article.get_absolute_url }}" class="btn btn-sm btn-outline-primary float-end">
                            Читать
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        {% endif %}

        <!-- Последние статьи -->
        <h2 class="mb-3">Последние статьи</h2>
        <div class="row">
            {% for article in recent_articles %}
            <div class="col-md-6 mb-4">
                <div class="card article-card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <span class="badge bg-secondary category-badge mb-2">
                                    {{ article.category.name|default:"Без категории" }}
                                </span>
                                <h5 class="card-title">{{ article.title }}</h5>
                            </div>
                            {% if article.is_featured %}
                            <span class="badge bg-warning text-dark">
                                <i class="bi bi-star"></i> Рекомендуем
                            </span>
                            {% endif %}
                        </div>
                        
                        <p class="card-text text-muted">{{ article.excerpt|truncatechars:150|default:"" }}</p>
                        
                        <div class="mt-3">
                            {% for tag in article.tags.all|slice:":3" %}
                            <span class="badge bg-light text-dark border">{{ tag.name }}</span>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="card-footer bg-transparent">
                        <small class="text-muted">
                            <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                            <i class="bi bi-person ms-2"></i> {{ article.author.username }}
                        </small>
                        <a href="{{ article.get_absolute_url }}" class="btn btn-sm btn-outline-primary float-end">
                            Читать <i class="bi bi-arrow-right"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% empty %}
            <div class="col-12">
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i> Пока нет опубликованных статей.
                    {% if user.is_authenticated and user.is_staff %}
                    <a href="/admin/blog/article/add/" class="alert-link">Создайте первую статью</a>
                    {% endif %}
                </div>
            </div>
            {% endfor %}
        </div>
        
        <!-- Кнопка "Все статьи" -->
        <div class="text-center mt-4">
            <a href="{% url 'blog:article_list' %}" class="btn btn-primary btn-lg">
                <i class="bi bi-list-ul"></i> Все статьи
            </a>
        </div>
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Категории -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for category in categories %}
                    <a href="{% url 'blog:category_detail' category.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        {{ category.name }}
                        <span class="badge bg-primary rounded-pill">{{ category.article_set.count }}</span>
                    </a>
                    {% empty %}
                    <div class="text-muted">Нет категорий</div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Популярные теги -->
        {% if popular_tags %}
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-tags"></i> Популярные теги</h5>
            </div>
            <div class="card-body">
                <div class="d-flex flex-wrap gap-2">
                    {% for tag in popular_tags %}
                    <a href="{% url 'blog:article_list' %}?tag={{ tag.slug }}" 
                       class="badge bg-light text-dark text-decoration-none border p-2">
                        {{ tag.name }}
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Информация -->
        <div class="card">
            <div class="card-header bg-dark text-white">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> О платформе</h5>
            </div>
            <div class="card-body">
                <p>Universal Creative Hub — это self-hosted платформа, которая объединяет:</p>
                <ul class="list-unstyled">
                    <li><i class="bi bi-check-circle text-success"></i> Блог и портфолио</li>
                    <li><i class="bi bi-check-circle text-success"></i> Музыкальную студию (DAW)</li>
                    <li><i class="bi bi-check-circle text-success"></i> Мультимедиа галерею</li>
                    <li><i class="bi bi-check-circle text-success"></i> Систему проектов</li>
                </ul>
                <div class="alert alert-warning mt-3">
                    <small>
                        <i class="bi bi-exclamation-triangle"></i> 
                        Сейчас работает блоговая часть. Музыкальная студия в разработке.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
```

---

## ✅ Проверяем:

1. Запускаем сервер:
```bash
python manage.py runserver
```

2. Открываем в браузере: http://localhost:8000/

3. Должна появиться главная страница с базовым макетом.

---

**Продолжим создавать остальные шаблоны (`article_list.html`, `article_detail.html`)? Или сначала создадим тестовые данные через админку?**

---

## 👤 **Kirill**

Работает! пробуем тестовые данные через админку

---

## 🤖 **DeepSeek**

Отлично! Давайте создадим тестовые данные через админку.

---

## 🎯 Шаг 6: Создание тестовых данных

### 6.1. Открываем админку:
```
http://localhost:8000/admin/
```

### 6.2. Входим с учетными данными суперпользователя

### 6.3. Создаем категории:
1. Нажимаем на **"Категории"** в разделе **BLOG**
2. Нажимаем **"ДОБАВИТЬ КАТЕГОРИЮ"** в правом верхнем углу
3. Создаем несколько категорий:
   - **Музыка**
     - Название: `Музыка`
     - URL: `music` (автоматически заполнится)
     - Описание: `Статьи о музыке, композиции, производстве`
   - **Программирование**
     - Название: `Программирование`
     - URL: `programming`
     - Описание: `Статьи о разработке, алгоритмах, архитектуре`
   - **Дизайн**
     - Название: `Дизайн`
     - URL: `design`
     - Описание: `Статьи о дизайне, UI/UX, графике`

### 6.4. Создаем статьи:
1. Нажимаем на **"Статьи"** в разделе **BLOG**
2. Нажимаем **"ДОБАВИТЬ СТАТЬЮ"**

#### Статья 1: "Мой первый музыкальный проект"
- **Заголовок**: `Мой первый музыкальный проект`
- **URL**: `my-first-music-project` (автоматически)
- **Краткое описание**: `История создания моего первого трека в Universal Creative Hub`
- **Содержание** (Markdown):
```markdown
# Создание первого трека

Это история о том, как я создал свой первый музыкальный проект в Universal Creative Hub.

## Использованные инструменты

1. **Трекер-секвенсер** - для создания базовых паттернов
2. **Strudel REPL** - для генерации ритмических паттернов
3. **Синтезатор** - для создания тембров

## Результат

Получился ambient-трек в стиле IDM. Весь процесс занял около 3 часов.

![Музыкальный проект](/media/articles/covers/music.jpg)

## Что дальше?

Планирую добавить:
- Модуляционные эффекты
- Автоматизацию параметров
- Видео-визуализацию
```

- **Обложка**: (можно пропустить или загрузить тестовое изображение)
- **Автор**: выберите себя (admin)
- **Категория**: `Музыка`
- **Теги**: `музыка`, `творчество`, `проект`
- **Статус**: `Опубликовано`
- **Рекомендуемая**: ✅ (поставить галочку)
- **Разрешить комментарии**: ✅

#### Статья 2: "Архитектура ядра-оркестратора"
- **Заголовок**: `Архитектура ядра-оркестратора в Universal Creative Hub`
- **URL**: `core-orchestrator-architecture`
- **Краткое описание**: `Технический обзор архитектуры ядра-оркестратора, управляющего всеми нодами`
- **Содержание**:
```markdown
# Ядро-оркестратор: сердце платформы

Ядро-оркестратор — это центральный компонент, который управляет всеми нодами и обеспечивает их синхронизацию.

## Основные компоненты

### MasterClock
Отвечает за временную синхронизацию всех нод. Поддерживает:
- Play/Stop/Record
- Темп (BPM)
- Циклическое воспроизведение

### NodeGraph Manager
Управляет визуальным графом нод на бесконечном канвасе.

### MessageRouter
Маршрутизирует сообщения между нодами:
- MIDI события
- Аудио данные
- Модуляционные сигналы

## Преимущества архитектуры

1. **Единый источник истины** для временной синхронизации
2. **Масштабируемость** - можно добавлять новые типы нод
3. **Производительность** - оптимизированная коммуникация между компонентами

## Пример кода

```typescript
interface UniversalMusicalEvent {
  type: "note" | "cc" | "audio";
  time: number; // в beats
  data: Record<string, any>;
}
```
```

- **Категория**: `Программирование`
- **Теги**: `архитектура`, `typescript`, `daw`, `разработка`
- **Статус**: `Опубликовано`
- **Рекомендуемая**: ✅

#### Статья 3: "Визуальный дизайн интерфейса"
- **Заголовок**: `Принципы визуального дизайна интерфейса креативной платформы`
- **URL**: `ui-design-principles`
- **Краткое описание**: `Как мы проектировали пользовательский интерфейс для сложной креативной среды`
- **Содержание**:
```markdown
# Дизайн для сложности

Проектирование интерфейса для музыкальной студии — это вызов. Нужно совместить:

1. **Мощность** профессиональных инструментов
2. **Доступность** для начинающих
3. **Эстетику** творческой среды

## Цветовая палитра

Мы выбрали темную тему как основную:

- **Фон**: `#1a1a1a`
- **Акцент**: `#0d6efd` (Bootstrap primary)
- **Текст**: `#f8f9fa` с контрастом 7:1

## Типографика

- **Заголовки**: Inter, 16-24px
- **Текст**: Roboto, 14px
- **Моноширинный**: JetBrains Mono для кода

## Компоненты

### Ноды
Каждая нода имеет:
- Заголовок с иконкой
- Параметры с слайдерами/полями ввода
- Входы/выходы для соединений

### Граф
Бесконечный канвас с:
- Масштабированием колесиком мыши
- Перетаскиванием зажатием правой кнопки
- Группировкой нод
```

- **Категория**: `Дизайн`
- **Теги**: `дизайн`, `ui`, `ux`, `интерфейс`
- **Статус**: `Опубликовано`

---

## ✅ Проверяем результат:

### 6.5. Открываем главную страницу:
```
http://localhost:8000/
```

Должны увидеть:
- Рекомендуемые статьи (первые две с иконкой "Рекомендуем")
- Последние статьи (все три)
- Категории в боковой панели
- Популярные теги

### 6.6. Проверяем разные страницы:
1. **Список статей**: http://localhost:8000/articles/
2. **Детальная страница статьи**: Кликнуть на любую статью
3. **Категории**: http://localhost:8000/categories/
4. **Статьи по категории**: Кликнуть на категорию в боковой панели

---

## 🔧 Шаг 7: Создаем недостающие шаблоны

### 7.1. Создаем `uch/apps/blog/templates/blog/article_list.html`:
```html
{% extends 'base.html' %}

{% block title %}Статьи - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <h1 class="mb-4">
            {% if category %}
            <i class="bi bi-folder"></i> {{ category.name }}
            {% elif request.GET.tag %}
            <i class="bi bi-tag"></i> Тег: {{ request.GET.tag }}
            {% elif request.GET.q %}
            <i class="bi bi-search"></i> Поиск: "{{ request.GET.q }}"
            {% else %}
            <i class="bi bi-journal-text"></i> Все статьи
            {% endif %}
        </h1>

        <!-- Статистика -->
        <div class="alert alert-light mb-4">
            <div class="row">
                <div class="col-md-4">
                    <i class="bi bi-journal"></i> Статей: <strong>{{ page_obj.paginator.count }}</strong>
                </div>
                <div class="col-md-4">
                    <i class="bi bi-folder"></i> Категорий: <strong>{{ categories|length }}</strong>
                </div>
                <div class="col-md-4">
                    <i class="bi bi-tags"></i> Тегов: <strong>{{ popular_tags|length }}</strong>
                </div>
            </div>
        </div>

        <!-- Список статей -->
        {% for article in articles %}
        <div class="card mb-4 article-card">
            <div class="row g-0">
                {% if article.cover_image %}
                <div class="col-md-4">
                    <img src="{{ article.cover_image.url }}" class="img-fluid rounded-start" 
                         alt="{{ article.title }}" style="height: 200px; width: 100%; object-fit: cover;">
                </div>
                {% endif %}
                <div class="col-md-{% if article.cover_image %}8{% else %}12{% endif %}">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <div>
                                <span class="badge bg-primary category-badge">
                                    {{ article.category.name|default:"Без категории" }}
                                </span>
                                {% if article.is_featured %}
                                <span class="badge bg-warning text-dark ms-1">
                                    <i class="bi bi-star"></i> Рекомендуем
                                </span>
                                {% endif %}
                            </div>
                            <small class="text-muted">
                                <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                            </small>
                        </div>
                        
                        <h5 class="card-title">{{ article.title }}</h5>
                        <p class="card-text text-muted">{{ article.excerpt|truncatechars:200 }}</p>
                        
                        <div class="d-flex justify-content-between align-items-center mt-3">
                            <div>
                                {% for tag in article.tags.all|slice:":3" %}
                                <a href="?tag={{ tag.slug }}" class="badge bg-light text-dark text-decoration-none border">
                                    {{ tag.name }}
                                </a>
                                {% endfor %}
                            </div>
                            <div>
                                <a href="{{ article.get_absolute_url }}" class="btn btn-outline-primary btn-sm">
                                    Читать <i class="bi bi-arrow-right"></i>
                                </a>
                            </div>
                        </div>
                        
                        <div class="mt-3 text-muted small">
                            <i class="bi bi-person"></i> {{ article.author.username }}
                            {% if article.comments.count > 0 %}
                            <span class="ms-3">
                                <i class="bi bi-chat"></i> {{ article.comments.count }} комментариев
                            </span>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {% empty %}
        <div class="text-center py-5">
            <i class="bi bi-emoji-frown display-1 text-muted"></i>
            <h3 class="mt-3">Статьи не найдены</h3>
            <p class="text-muted">
                {% if request.GET.q %}
                По запросу "{{ request.GET.q }}" ничего не найдено.
                {% else %}
                Пока нет опубликованных статей.
                {% endif %}
            </p>
            <a href="{% url 'blog:article_list' %}" class="btn btn-outline-primary mt-2">
                <i class="bi bi-arrow-left"></i> Все статьи
            </a>
        </div>
        {% endfor %}

        <!-- Пагинация -->
        {% if page_obj.paginator.num_pages > 1 %}
        <nav aria-label="Навигация по страницам">
            <ul class="pagination justify-content-center">
                {% if page_obj.has_previous %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.previous_page_number }}{% if request.GET.q %}&q={{ request.GET.q }}{% endif %}">
                        <i class="bi bi-chevron-left"></i>
                    </a>
                </li>
                {% endif %}

                {% for num in page_obj.paginator.page_range %}
                    {% if page_obj.number == num %}
                    <li class="page-item active">
                        <span class="page-link">{{ num }}</span>
                    </li>
                    {% elif num > page_obj.number|add:-3 and num < page_obj.number|add:3 %}
                    <li class="page-item">
                        <a class="page-link" href="?page={{ num }}{% if request.GET.q %}&q={{ request.GET.q }}{% endif %}">{{ num }}</a>
                    </li>
                    {% endif %}
                {% endfor %}

                {% if page_obj.has_next %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.next_page_number }}{% if request.GET.q %}&q={{ request.GET.q }}{% endif %}">
                        <i class="bi bi-chevron-right"></i>
                    </a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Поиск -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-search"></i> Поиск</h5>
            </div>
            <div class="card-body">
                <form action="{% url 'blog:article_list' %}" method="get">
                    <div class="input-group">
                        <input type="text" class="form-control" name="q" 
                               placeholder="Поиск статей..." value="{{ request.GET.q|default:'' }}">
                        <button class="btn btn-primary" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Категории -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    <a href="{% url 'blog:article_list' %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center {% if not category %}active{% endif %}">
                        Все категории
                        <span class="badge bg-primary rounded-pill">{{ page_obj.paginator.count }}</span>
                    </a>
                    {% for cat in categories %}
                    <a href="{% url 'blog:category_detail' cat.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center {% if category and category.slug == cat.slug %}active{% endif %}">
                        {{ cat.name }}
                        <span class="badge bg-primary rounded-pill">{{ cat.article_set.count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Популярные теги -->
        {% if popular_tags %}
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-tags"></i> Популярные теги</h5>
            </div>
            <div class="card-body">
                <div class="d-flex flex-wrap gap-2">
                    {% for tag in popular_tags %}
                    <a href="?tag={{ tag.slug }}" 
                       class="badge {% if request.GET.tag == tag.slug %}bg-primary text-white{% else %}bg-light text-dark border{% endif %} text-decoration-none p-2">
                        {{ tag.name }}
                        <span class="badge bg-secondary rounded-pill ms-1">{{ tag.num_times }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Последние статьи -->
        {% if recent_articles %}
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-clock-history"></i> Недавние статьи</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for article in recent_articles %}
                    <a href="{{ article.get_absolute_url }}" class="list-group-item list-group-item-action">
                        <div class="d-flex w-100 justify-content-between">
                            <h6 class="mb-1">{{ article.title|truncatechars:40 }}</h6>
                            <small>{{ article.published_at|date:"d.m" }}</small>
                        </div>
                        <small class="text-muted">
                            {{ article.category.name|default:"Без категории" }}
                        </small>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}
```

---

## ✅ Тестируем:

1. Перейдите на http://localhost:8000/articles/
2. Должен появиться красивый список статей с пагинацией, поиском и фильтрами
3. Попробуйте:
   - Поиск по словам "музыка", "дизайн"
   - Фильтрацию по категориям
   - Фильтрацию по тегам
   - Пагинацию (когда будет больше статей)

---

**Продолжим с созданием `article_detail.html`? Или сначала протестируем текущую функциональность?**

---

## 👤 **Kirill**

6.3 нет раздела BLOG в админке:
Site administration
Authentication and Authorization
Groups	
Add
Change
Users	
Add
Change
Taggit
Tags	
Add
Change
Recent actions
My actions
None available

---

## 🤖 **DeepSeek**

Проблема: приложения блога не зарегистрированы в админке Django. Нужно проверить настройки и структуру приложений.

---

## 🔧 Шаг 6.3: Проверка и настройка приложений

### 1. Проверим структуру приложения `blog`:
```bash
ls -la uch/apps/blog/
```

Должно быть:
```
__init__.py
admin.py
apps.py
migrations/
models.py
urls.py
views.py
```

### 2. Проверим `uch/apps/blog/apps.py`:
```python
from django.apps import AppConfig


class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'uch.apps.blog'
    verbose_name = 'Блог и Портфолио'  # Добавляем человеческое имя
```

### 3. Проверим `uch/settings.py` - настройки INSTALLED_APPS:
```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    
    # Third party apps
    'taggit',
    'rest_framework',
    'corsheaders',
    
    # Local apps
    'uch.apps.core',
    'uch.apps.blog.apps.BlogConfig',  # Используем прямое указание на конфиг
    'uch.apps.users',
    'uch.apps.media',
]
```

### 4. Проверим, что приложение корректно зарегистрировано:
```bash
python manage.py check
```

Если есть ошибки, исправим их.

### 5. Если раздел BLOG все еще не появился, перезапустим сервер:
```bash
python manage.py runserver
```

---

## 🔧 Альтернативное решение: Создадим суперпользователя заново

### 6. Если проблема сохраняется, удалим и создадим суперпользователя заново:
```bash
# Останавливаем сервер (Ctrl+C)

# Удаляем текущую базу данных (только для разработки!)
rm db.sqlite3

# Создаем миграции заново
python manage.py makemigrations
python manage.py migrate

# Создаем суперпользователя
python manage.py createsuperuser
# username: admin
# email: admin@example.com
# password: ваш_пароль
```

### 7. Проверим, видит ли Django наши модели:
```bash
python manage.py shell
```

В интерактивной оболочке:
```python
from uch.apps.blog.models import Article, Category
print(Article.objects.count())
print(Category.objects.count())
exit()
```

---

## 🔧 Если все еще не работает, попробуем простой способ:

### 8. Создадим простой скрипт для добавления тестовых данных:
Создаем файл `create_test_data.py` в корне проекта:
```python
import os
import django

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'uch.settings')
django.setup()

from django.contrib.auth.models import User
from uch.apps.blog.models import Category, Article
from django.utils import timezone

# Создаем суперпользователя, если его нет
if not User.objects.filter(username='admin').exists():
    User.objects.create_superuser('admin', 'admin@example.com', 'admin123')

# Создаем категории
categories_data = [
    {'name': 'Музыка', 'slug': 'music', 'description': 'Статьи о музыке'},
    {'name': 'Программирование', 'slug': 'programming', 'description': 'Статьи о разработке'},
    {'name': 'Дизайн', 'slug': 'design', 'description': 'Статьи о дизайне'},
]

for cat_data in categories_data:
    Category.objects.get_or_create(**cat_data)

# Создаем статьи
articles_data = [
    {
        'title': 'Мой первый музыкальный проект',
        'slug': 'my-first-music-project',
        'excerpt': 'История создания первого трека',
        'content': '# Мой первый трек\n\nЭто было прекрасно!',
        'category': Category.objects.get(slug='music'),
        'author': User.objects.get(username='admin'),
        'status': 'published',
        'is_featured': True,
        'published_at': timezone.now(),
    },
    {
        'title': 'Архитектура ядра-оркестратора',
        'slug': 'core-orchestrator-architecture',
        'excerpt': 'Технический обзор архитектуры',
        'content': '# Архитектура\n\nВсе работает отлично!',
        'category': Category.objects.get(slug='programming'),
        'author': User.objects.get(username='admin'),
        'status': 'published',
        'is_featured': True,
        'published_at': timezone.now(),
    },
]

for art_data in articles_data:
    Article.objects.get_or_create(
        slug=art_data['slug'],
        defaults=art_data
    )

print("Тестовые данные созданы!")
print(f"Категории: {Category.objects.count()}")
print(f"Статьи: {Article.objects.count()}")
```

Запускаем:
```bash
python create_test_data.py
```

---

## ✅ Проверяем:

### 9. Запускаем сервер:
```bash
python manage.py runserver
```

### 10. Открываем админку:
http://localhost:8000/admin/

Теперь должны появиться разделы:
- **Блог и Портфолио** (или BLOG)
  - Категории
  - Статьи
  - Медиафайлы
  - Комментарии
- **Taggit**
  - Tags

---

## 🔧 Если все еще не работает:

Давайте создадим простую альтернативу - добавим данные напрямую через команду Django:

```bash
python manage.py shell
```

В интерактивной оболочке выполните:
```python
from django.contrib.auth.models import User
from uch.apps.blog.models import Category, Article
from django.utils import timezone

# Создаем пользователя
user, created = User.objects.get_or_create(
    username='testuser',
    defaults={
        'email': 'test@example.com',
        'is_staff': True,
        'is_superuser': True
    }
)
if created:
    user.set_password('testpass123')
    user.save()

# Создаем категории
cat1 = Category.objects.create(
    name='Музыка',
    slug='music',
    description='Статьи о музыке'
)

cat2 = Category.objects.create(
    name='Программирование',
    slug='programming',
    description='Статьи о разработке'
)

# Создаем статьи
article1 = Article.objects.create(
    title='Тестовая статья 1',
    slug='test-article-1',
    excerpt='Краткое описание статьи 1',
    content='# Заголовок\n\nСодержание статьи 1',
    author=user,
    category=cat1,
    status='published',
    is_featured=True,
    published_at=timezone.now()
)

article2 = Article.objects.create(
    title='Тестовая статья 2',
    slug='test-article-2',
    excerpt='Краткое описание статьи 2',
    content='# Заголовок\n\nСодержание статьи 2',
    author=user,
    category=cat2,
    status='published',
    published_at=timezone.now()
)

print(f"Создано: {Category.objects.count()} категорий, {Article.objects.count()} статей")
exit()
```

---

**Попробуйте один из этих методов и сообщите, что получилось. Если ни один не сработает, мы попробуем другой подход.**

---

## 👤 **Kirill**

Хочу попробовать 
6. Если проблема сохраняется, удалим и создадим суперпользователя заново:
bash
# Останавливаем сервер (Ctrl+C)

# Удаляем текущую базу данных (только для разработки!)
rm db.sqlite3

# Создаем миграции заново
python manage.py makemigrations
python manage.py migrate

# Создаем суперпользователя
python manage.py createsuperuser
# username: admin
# email: admin@example.com
# password: ваш_пароль

Смущает только # Удаляем текущую базу данных (только для разработки!) что это значит? не делать этот пункт?

---

## 🤖 **DeepSeek**

**Хороший вопрос!** Давайте разберемся:

## 🔍 Что значит "удалить базу данных"?

В нашем проекте используется SQLite (`db.sqlite3`) — это файл базы данных на диске. Удаление этого файла **полностью очистит все данные**:
- Пользователей
- Статьи
- Категории
- Настройки

**Это безопасно только потому что:**
1. Мы в режиме разработки (DEBUG=True)
2. У нас пока нет важных пользовательских данных
3. Мы только начали и можем легко воссоздать тестовые данные

## 🛡️ Безопасный альтернативный подход (без удаления базы)

Давайте сначала попробуем **без удаления базы**:

### Шаг 1: Проверим, зарегистрированы ли модели в админке
```bash
python manage.py shell
```

В интерактивной оболочке:
```python
from django.contrib import admin
from uch.apps.blog.models import Article, Category

# Проверяем, зарегистрированы ли модели
print("Зарегистрированные модели:")
for model, model_admin in admin.site._registry.items():
    print(f"- {model.__name__}")

# Проверяем наши модели
print(f"\nArticle зарегистрирована: {Article in admin.site._registry}")
print(f"Category зарегистрирована: {Category in admin.site._registry}")

exit()
```

### Шаг 2: Проверим apps.py
Убедитесь, что в `uch/apps/blog/apps.py` есть:
```python
from django.apps import AppConfig

class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'uch.apps.blog'
    verbose_name = 'Блог'  # Это имя появится в админке
```

### Шаг 3: Проверим settings.py
Убедитесь, что в `uch/settings.py`:
```python
INSTALLED_APPS = [
    # ... другие приложения ...
    'uch.apps.blog.apps.BlogConfig',  # Вот так!
    # ИЛИ просто 'uch.apps.blog',
]
```

### Шаг 4: Перезапустим сервер с принудительным обновлением
```bash
# Останавливаем сервер (Ctrl+C если запущен)

# Принудительно пересобираем статические файлы
python manage.py collectstatic --noinput

# Проверяем миграции
python manage.py makemigrations
python manage.py migrate

# Запускаем сервер
python manage.py runserver
```

## 🎯 Рекомендую попробовать сначала ЭТОТ безопасный путь:

1. **Не удаляйте базу данных** пока
2. Сделайте шаги 1-4 выше
3. Проверьте админку: http://localhost:8000/admin/

## 🔧 Если все еще не видно раздела "Блог":

Тогда попробуем **мягкий сброс**:

```bash
# Останавливаем сервер

# Удаляем только миграции блога (не всю базу!)
rm -rf uch/apps/blog/migrations/

# Создаем пустые миграции
mkdir uch/apps/blog/migrations/
touch uch/apps/blog/migrations/__init__.py

# Пересоздаем миграции
python manage.py makemigrations blog
python manage.py migrate blog

# Перезапускаем
python manage.py runserver
```

## ✅ Что делать:

**Начните с безопасного подхода (шаги 1-4).** Скорее всего, проблема в том, что приложение не совсем корректно зарегистрировано.

Если после шагов 1-4 раздел "Блог" не появится в админке — сообщите, и мы попробуем более агрессивный подход **с сохранением вашего суперпользователя**.

**Совет:** Сначала попробуйте зайти в админку с существующими учетными данными и проверьте, не появился ли раздел "Блог" под другим именем или в другой секции.

Какой подход предпочитаете начать?

---

## 👤 **Kirill**

шаг 1 
(venv) kirillkravcov@MacBook-Pro-Kirill uch-backend % python manage.py shell
Python 3.8.9 (default, May 17 2022, 12:55:41) 
[Clang 13.1.6 (clang-1316.0.21.2.5)] on darwin
Type "help", "copyright", "credits" or "license" for more information.
(InteractiveConsole)
>>> from django.contrib import admin
>>> from uch.apps.blog.models import Article, Category
>>> 
>>> # Проверяем, зарегистрированы ли модели
>>> print("Зарегистрированные модели:")
Зарегистрированные модели:
>>> for model, model_admin in admin.site._registry.items():
...     print(f"- {model.__name__}")
... 
- Group
- User
- Tag
>>> # Проверяем наши модели
>>> print(f"\nArticle зарегистрирована: {Article in admin.site._registry}")

Article зарегистрирована: False
>>> print(f"Category зарегистрирована: {Category in admin.site._registry}")
Category зарегистрирована: False
>>> 
>>> exit()

---

## 🤖 **DeepSeek**

Отлично! Проблема ясна: модели `Article` и `Category` не зарегистрированы в админке Django. Давайте это исправим.

---

## 🎯 Шаг 1: Проверяем, импортируется ли admin.py

### 1.1. Проверим, существует ли `uch/apps/blog/admin.py`:
```bash
cat uch/apps/blog/admin.py
```

Должен содержать код регистрации моделей, который мы создавали ранее.

### 1.2. Если файл существует, проверим импорты:
Убедитесь, что в `uch/apps/blog/admin.py` есть:
```python
from django.contrib import admin
from .models import Article, Category, MediaItem, Comment

# Регистрация моделей
admin.site.register(Article)
admin.site.register(Category)
admin.site.register(MediaItem)
admin.site.register(Comment)
```

### 1.3. Если файла нет, создадим его заново:
```bash
cat > uch/apps/blog/admin.py << 'EOF'
from django.contrib import admin
from django.utils.html import format_html
from .models import Category, Article, MediaItem, Comment


@admin.register(Category)
class CategoryAdmin(admin.ModelAdmin):
    list_display = ('name', 'parent', 'order', 'is_active', 'article_count')
    list_filter = ('is_active', 'parent')
    search_fields = ('name', 'description')
    prepopulated_fields = {'slug': ('name',)}
    ordering = ('order', 'name')
    
    def article_count(self, obj):
        return obj.article_set.count()
    article_count.short_description = 'Статей'


@admin.register(Article)
class ArticleAdmin(admin.ModelAdmin):
    list_display = ('title', 'author', 'category', 'status', 
                    'published_at', 'is_featured', 'comment_count')
    list_filter = ('status', 'category', 'is_featured', 'created_at')
    search_fields = ('title', 'content', 'excerpt')
    prepopulated_fields = {'slug': ('title',)}
    readonly_fields = ('created_at', 'updated_at', 'published_at', 'content_html')
    date_hierarchy = 'published_at'
    ordering = ('-published_at', '-created_at')
    
    fieldsets = (
        ('Основное', {
            'fields': ('title', 'slug', 'excerpt', 'content', 'content_html')
        }),
        ('Метаданные', {
            'fields': ('cover_image', 'author', 'category', 'tags', 
                      'status', 'is_featured', 'allow_comments')
        }),
        ('Даты', {
            'fields': ('created_at', 'updated_at', 'published_at'),
            'classes': ('collapse',)
        }),
    )
    
    def comment_count(self, obj):
        return obj.comments.count()
    comment_count.short_description = 'Комментариев'
    
    def save_model(self, request, obj, form, change):
        if not obj.author_id:
            obj.author = request.user
        super().save_model(request, obj, form, change)


@admin.register(MediaItem)
class MediaItemAdmin(admin.ModelAdmin):
    list_display = ('title', 'file_type', 'uploaded_by', 'uploaded_at', 'preview')
    list_filter = ('file_type', 'uploaded_at')
    search_fields = ('title', 'description')
    readonly_fields = ('uploaded_at', 'preview')
    
    def preview(self, obj):
        if obj.file_type == 'image':
            return format_html('<img src="{}" width="100" />', obj.file.url)
        return f"Файл: {obj.file_type}"
    preview.short_description = 'Превью'


@admin.register(Comment)
class CommentAdmin(admin.ModelAdmin):
    list_display = ('author', 'article', 'content_preview', 
                    'is_approved', 'created_at')
    list_filter = ('is_approved', 'created_at', 'article')
    search_fields = ('content', 'author__username', 'article__title')
    actions = ['approve_comments', 'disapprove_comments']
    
    def content_preview(self, obj):
        return obj.content[:50] + '...' if len(obj.content) > 50 else obj.content
    content_preview.short_description = 'Текст'
    
    def approve_comments(self, request, queryset):
        queryset.update(is_approved=True)
    approve_comments.short_description = "Одобрить выбранные комментарии"
    
    def disapprove_comments(self, request, queryset):
        queryset.update(is_approved=False)
    disapprove_comments.short_description = "Снять одобрение"
EOF
```

---

## 🎯 Шаг 2: Проверяем apps.py приложения

### 2.1. Убедимся, что `uch/apps/blog/apps.py` содержит:
```python
from django.apps import AppConfig


class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'uch.apps.blog'
    verbose_name = 'Блог'
```

### 2.2. Проверим `__init__.py` файлы:
```bash
# Проверяем наличие __init__.py в ключевых местах
ls -la uch/apps/blog/__init__.py
ls -la uch/apps/__init__.py
ls -la uch/__init__.py
```

Все три файла должны существовать (пустые файлы).

---

## 🎯 Шаг 3: Проверяем settings.py

### 3.1. Убедимся, что приложение зарегистрировано правильно:
В `uch/settings.py`:
```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    
    # Third party apps
    'taggit',
    'rest_framework',
    'corsheaders',
    
    # Local apps - ВАЖНО: используем путь к классу конфигурации
    'uch.apps.blog.apps.BlogConfig',  # Вот так!
    # Или просто 'uch.apps.blog', если BlogConfig не определен
]
```

---

## 🎯 Шаг 4: Принудительно перезагружаем Django

### 4.1. Перезапускаем shell и проверяем:
```bash
python manage.py shell
```

```python
# Принудительно импортируем admin.py
import importlib
import uch.apps.blog.admin

# Перезагружаем модуль
importlib.reload(uch.apps.blog.admin)

# Проверяем регистрацию
from django.contrib import admin
from uch.apps.blog.models import Article, Category

print("Зарегистрированные модели после принудительной загрузки:")
for model, model_admin in admin.site._registry.items():
    print(f"- {model.__name__}")

print(f"\nArticle зарегистрирована: {Article in admin.site._registry}")
print(f"Category зарегистрирована: {Category in admin.site._registry}")

exit()
```

---

## 🎯 Шаг 5: Если все еще не работает - альтернативный подход

### 5.1. Создадим простой скрипт для принудительной регистрации:
```bash
cat > register_models.py << 'EOF'
import os
import django

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'uch.settings')
django.setup()

from django.contrib import admin
from uch.apps.blog.models import Article, Category, MediaItem, Comment
from uch.apps.blog.admin import (
    ArticleAdmin, CategoryAdmin, MediaItemAdmin, CommentAdmin
)

# Принудительно регистрируем модели
try:
    admin.site.register(Article, ArticleAdmin)
    admin.site.register(Category, CategoryAdmin)
    admin.site.register(MediaItem, MediaItemAdmin)
    admin.site.register(Comment, CommentAdmin)
    print("Модели успешно зарегистрированы!")
except Exception as e:
    print(f"Ошибка: {e}")
EOF

python register_models.py
```

### 5.2. Проверяем результат:
```bash
python manage.py shell
```

```python
from django.contrib import admin
from uch.apps.blog.models import Article, Category

print("Зарегистрированные модели:")
for model, model_admin in admin.site._registry.items():
    print(f"- {model.__name__} ({model._meta.app_label})")

exit()
```

---

## 🎯 Шаг 6: Перезапускаем сервер

### 6.1. Останавливаем сервер (Ctrl+C если запущен)

### 6.2. Запускаем заново:
```bash
python manage.py runserver
```

### 6.3. Проверяем админку:
Откройте http://localhost:8000/admin/ и войдите под своим суперпользователем.

Теперь должен появиться раздел **"Блог"** с подразделами:
- Категории
- Статьи
- Медиафайлы
- Комментарии

---

## 🔍 Если все еще не видно:

Возможно, Django кэширует загрузку приложений. Попробуем очистить кэш:

```bash
# Очистка кэша Python
find . -name "*.pyc" -delete
find . -name "__pycache__" -type d -exec rm -rf {} +

# Перезапускаем
python manage.py runserver
```

---

**Попробуйте эти шаги по порядку. Скорее всего, проблема в том, что `admin.py` не импортируется автоматически. Сообщите, на каком шаге возникает проблема или что получается в результате.**

---

## 👤 **Kirill**

Отлично! Все появилось

---

## 🤖 **DeepSeek**

Отлично! 🎉 Раздел "Блог" появился в админке. Теперь давайте создадим тестовые данные и проверим работу фронтенда.

---

## 🎯 Шаг 6: Создание тестовых данных через админку

### 6.1. Войдите в админку:
```
http://localhost:8000/admin/
```

### 6.2. Создайте категории:
1. Нажмите **"Категории"** в разделе **Блог**
2. Нажмите **"ДОБАВИТЬ КАТЕГОРИЮ"**
3. Создайте три категории:
   - **Музыка** (slug: `music`)
   - **Программирование** (slug: `programming`)
   - **Дизайн** (slug: `design`)

### 6.3. Создайте статьи:
1. Нажмите **"Статьи"** в разделе **Блог**
2. Нажмите **"ДОБАВИТЬ СТАТЬЮ"**

#### Статья 1: "Мой первый музыкальный проект"
- **Заголовок**: `Мой первый музыкальный проект`
- **URL**: `my-first-music-project`
- **Краткое описание**: `История создания моего первого трека в Universal Creative Hub`
- **Содержание** (Markdown):
```markdown
# Создание первого трека

Это история о том, как я создал свой первый музыкальный проект в Universal Creative Hub.

## Использованные инструменты

1. **Трекер-секвенсер** - для создания базовых паттернов
2. **Strudel REPL** - для генерации ритмических паттернов
3. **Синтезатор** - для создания тембров

## Результат

Получился ambient-трек в стиле IDM. Весь процесс занял около 3 часов.

## Что дальше?

Планирую добавить:
- Модуляционные эффекты
- Автоматизацию параметров
- Видео-визуализацию
```

- **Обложка**: (можно пропустить)
- **Автор**: выберите `admin`
- **Категория**: `Музыка`
- **Теги**: начните вводить `музыка`, нажмите Enter, добавьте `творчество`, `проект`
- **Статус**: `Опубликовано`
- **Рекомендуемая**: ✅
- **Нажмите "СОХРАНИТЬ"**

#### Статья 2: "Архитектура ядра-оркестратора"
- **Заголовок**: `Архитектура ядра-оркестратора`
- **URL**: `core-orchestrator-architecture`
- **Краткое описание**: `Технический обзор архитектуры ядра-оркестратора`
- **Содержание**:
```markdown
# Ядро-оркестратор

Ядро-оркестратор — это центральный компонент, который управляет всеми нодами.

## Основные компоненты

### MasterClock
Отвечает за временную синхронизацию.

### NodeGraph Manager
Управляет визуальным графом нод.

## Преимущества

1. **Единая синхронизация**
2. **Масштабируемость**
3. **Производительность**
```

- **Категория**: `Программирование`
- **Теги**: `архитектура`, `typescript`, `daw`
- **Статус**: `Опубликовано`
- **Рекомендуемая**: ✅

#### Статья 3: "Принципы дизайна интерфейса"
- **Заголовок**: `Принципы дизайна интерфейса`
- **URL**: `ui-design-principles`
- **Краткое описание**: `Как мы проектировали пользовательский интерфейс`
- **Содержание**:
```markdown
# Дизайн интерфейса

Проектирование интерфейса для музыкальной студии.

## Цветовая палитра

Темная тема как основная.

## Типографика

- Inter для заголовков
- Roboto для текста
```

- **Категория**: `Дизайн`
- **Теги**: `дизайн`, `ui`, `ux`
- **Статус**: `Опубликовано`

---

## ✅ Проверка работы

### 7.1. Откройте главную страницу:
```
http://localhost:8000/
```

Должны увидеть:
- Рекомендуемые статьи (первые две)
- Последние статьи (все три)
- Категории в боковой панели
- Популярные теги

### 7.2. Проверьте разные страницы:
1. **Список статей**: http://localhost:8000/articles/
2. **Детальная страница статьи**: Кликните на любую статью
3. **Категории**: http://localhost:8000/categories/
4. **Фильтр по категории**: Кликните на категорию в боковой панели

---

## 🎯 Шаг 7: Создаем недостающий шаблон для детальной статьи

### 7.1. Создаем `uch/apps/blog/templates/blog/article_detail.html`:
```html
{% extends 'base.html' %}
{% load markdown_extras %}

{% block title %}{{ article.title }} - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <!-- Навигация -->
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{% url 'blog:home' %}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{% url 'blog:article_list' %}">Статьи</a></li>
                {% if article.category %}
                <li class="breadcrumb-item">
                    <a href="{% url 'blog:category_detail' article.category.slug %}">
                        {{ article.category.name }}
                    </a>
                </li>
                {% endif %}
                <li class="breadcrumb-item active" aria-current="page">{{ article.title|truncatechars:30 }}</li>
            </ol>
        </nav>

        <!-- Заголовок статьи -->
        <article class="mb-5">
            <header class="mb-4">
                <div class="d-flex justify-content-between align-items-start mb-3">
                    <div>
                        <span class="badge bg-primary category-badge mb-2">
                            {{ article.category.name|default:"Без категории" }}
                        </span>
                        {% if article.is_featured %}
                        <span class="badge bg-warning text-dark ms-1">
                            <i class="bi bi-star"></i> Рекомендуем
                        </span>
                        {% endif %}
                    </div>
                    <small class="text-muted">
                        <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y H:i" }}
                    </small>
                </div>
                
                <h1 class="display-6">{{ article.title }}</h1>
                
                {% if article.excerpt %}
                <p class="lead text-muted">{{ article.excerpt }}</p>
                {% endif %}
                
                <div class="d-flex align-items-center mt-3">
                    <div class="me-3">
                        <i class="bi bi-person-circle"></i>
                        <strong>{{ article.author.username }}</strong>
                    </div>
                    <div class="text-muted">
                        {% if article.updated_at != article.created_at %}
                        <i class="bi bi-pencil"></i> Обновлено: {{ article.updated_at|date:"d.m.Y" }}
                        {% endif %}
                    </div>
                </div>
            </header>

            <!-- Обложка -->
            {% if article.cover_image %}
            <div class="text-center mb-4">
                <img src="{{ article.cover_image.url }}" 
                     class="img-fluid rounded" 
                     alt="{{ article.title }}"
                     style="max-height: 400px; object-fit: cover;">
                {% if article.cover_image.caption %}
                <p class="text-muted mt-2"><small>{{ article.cover_image.caption }}</small></p>
                {% endif %}
            </div>
            {% endif %}

            <!-- Содержание статьи -->
            <div class="article-content mb-5">
                {{ article.content_html|safe }}
            </div>

            <!-- Теги -->
            {% if article.tags.all %}
            <div class="mb-4">
                <h5><i class="bi bi-tags"></i> Теги</h5>
                <div class="d-flex flex-wrap gap-2">
                    {% for tag in article.tags.all %}
                    <a href="{% url 'blog:article_list' %}?tag={{ tag.slug }}" 
                       class="badge bg-light text-dark text-decoration-none border p-2">
                        {{ tag.name }}
                    </a>
                    {% endfor %}
                </div>
            </div>
            {% endif %}

            <!-- Навигация между статьями -->
            <div class="d-flex justify-content-between mt-5 pt-4 border-top">
                <div>
                    {% if article.get_previous_by_published_at %}
                    <a href="{{ article.get_previous_by_published_at.get_absolute_url }}" 
                       class="btn btn-outline-secondary">
                        <i class="bi bi-chevron-left"></i> Предыдущая
                    </a>
                    {% endif %}
                </div>
                <div>
                    <a href="{% url 'blog:article_list' %}" class="btn btn-outline-primary">
                        <i class="bi bi-list-ul"></i> Все статьи
                    </a>
                </div>
                <div>
                    {% if article.get_next_by_published_at %}
                    <a href="{{ article.get_next_by_published_at.get_absolute_url }}" 
                       class="btn btn-outline-secondary">
                        Следующая <i class="bi bi-chevron-right"></i>
                    </a>
                    {% endif %}
                </div>
            </div>
        </article>

        <!-- Комментарии -->
        {% if article.allow_comments %}
        <section class="mt-5 pt-4 border-top">
            <h4 class="mb-4">
                <i class="bi bi-chat"></i> Комментарии
                <span class="badge bg-secondary ms-2">{{ article.comments.count }}</span>
            </h4>
            
            {% if article.comments.exists %}
                {% for comment in article.comments.all %}
                    {% if comment.is_approved or user.is_staff %}
                    <div class="card mb-3 {% if not comment.is_approved %}border-warning{% endif %}">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <div>
                                    <strong>{{ comment.author.username }}</strong>
                                    <small class="text-muted ms-2">
                                        {{ comment.created_at|date:"d.m.Y H:i" }}
                                    </small>
                                </div>
                                {% if not comment.is_approved and user.is_staff %}
                                <span class="badge bg-warning text-dark">
                                    <i class="bi bi-eye-slash"></i> На модерации
                                </span>
                                {% endif %}
                            </div>
                            <p class="card-text">{{ comment.content }}</p>
                            
                            {% if comment.replies.exists %}
                            <div class="ms-4 mt-3">
                                <h6><i class="bi bi-reply"></i> Ответы:</h6>
                                {% for reply in comment.replies.all %}
                                    {% if reply.is_approved or user.is_staff %}
                                    <div class="card bg-light mb-2">
                                        <div class="card-body py-2">
                                            <div class="d-flex justify-content-between">
                                                <strong>{{ reply.author.username }}</strong>
                                                <small class="text-muted">{{ reply.created_at|date:"d.m.Y H:i" }}</small>
                                            </div>
                                            <p class="mb-0">{{ reply.content }}</p>
                                        </div>
                                    </div>
                                    {% endif %}
                                {% endfor %}
                            </div>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}
                {% endfor %}
            {% else %}
            <div class="alert alert-info">
                <i class="bi bi-info-circle"></i> Комментариев пока нет. Будьте первым!
            </div>
            {% endif %}
            
            <!-- Форма комментария -->
            {% if user.is_authenticated %}
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Добавить комментарий</h5>
                </div>
                <div class="card-body">
                    <form method="post" action="#">
                        {% csrf_token %}
                        <div class="mb-3">
                            <textarea class="form-control" name="content" rows="4" 
                                      placeholder="Ваш комментарий..." required></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-send"></i> Отправить
                        </button>
                    </form>
                </div>
            </div>
            {% else %}
            <div class="alert alert-warning">
                <i class="bi bi-exclamation-triangle"></i> 
                Чтобы оставить комментарий, <a href="/admin/login/?next={{ request.path }}">войдите</a>.
            </div>
            {% endif %}
        </section>
        {% endif %}
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Информация об авторе -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-person-circle"></i> Об авторе</h5>
            </div>
            <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                    <div class="me-3">
                        <i class="bi bi-person" style="font-size: 2rem;"></i>
                    </div>
                    <div>
                        <h6 class="mb-0">{{ article.author.username }}</h6>
                        <small class="text-muted">Автор статьи</small>
                    </div>
                </div>
                <p class="card-text">
                    {% if article.author.profile.bio %}
                        {{ article.author.profile.bio|truncatechars:200 }}
                    {% else %}
                        Активный участник Universal Creative Hub.
                    {% endif %}
                </p>
            </div>
        </div>

        <!-- Похожие статьи -->
        {% if recent_articles %}
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-newspaper"></i> Похожие статьи</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for related_article in recent_articles %}
                    <a href="{{ related_article.get_absolute_url }}" 
                       class="list-group-item list-group-item-action">
                        <div class="d-flex w-100 justify-content-between">
                            <h6 class="mb-1">{{ related_article.title|truncatechars:40 }}</h6>
                            <small>{{ related_article.published_at|date:"d.m" }}</small>
                        </div>
                        <small class="text-muted">
                            {{ related_article.category.name|default:"Без категории" }}
                        </small>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Категории -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for cat in categories %}
                    <a href="{% url 'blog:category_detail' cat.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        {{ cat.name }}
                        <span class="badge bg-primary rounded-pill">{{ cat.article_set.count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Действия -->
        {% if user.is_authenticated and user.is_staff %}
        <div class="card border-primary">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="bi bi-gear"></i> Администрирование</h5>
            </div>
            <div class="card-body">
                <div class="d-grid gap-2">
                    <a href="/admin/blog/article/{{ article.id }}/change/" 
                       class="btn btn-outline-primary" target="_blank">
                        <i class="bi bi-pencil"></i> Редактировать
                    </a>
                    <a href="/admin/blog/article/{{ article.id }}/delete/" 
                       class="btn btn-outline-danger" target="_blank">
                        <i class="bi bi-trash"></i> Удалить
                    </a>
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}

{% block extra_css %}
<style>
    .article-content {
        font-size: 1.1rem;
        line-height: 1.8;
    }
    
    .article-content h1, .article-content h2, .article-content h3 {
        margin-top: 2rem;
        margin-bottom: 1rem;
    }
    
    .article-content img {
        max-width: 100%;
        height: auto;
        border-radius: 8px;
        margin: 1.5rem 0;
    }
    
    .article-content pre {
        background-color: #f8f9fa;
        padding: 1rem;
        border-radius: 6px;
        border-left: 4px solid #0d6efd;
    }
    
    .article-content code {
        background-color: #f8f9fa;
        padding: 0.2rem 0.4rem;
        border-radius: 4px;
        font-family: 'Courier New', monospace;
    }
    
    .article-content blockquote {
        border-left: 4px solid #6c757d;
        padding-left: 1rem;
        color: #6c757d;
        font-style: italic;
        margin: 1.5rem 0;
    }
    
    .article-content table {
        width: 100%;
        margin: 1.5rem 0;
        border-collapse: collapse;
    }
    
    .article-content table th,
    .article-content table td {
        padding: 0.75rem;
        border: 1px solid #dee2e6;
    }
    
    .article-content table th {
        background-color: #f8f9fa;
        font-weight: bold;
    }
</style>
{% endblock %}
```

---

## 🎯 Шаг 8: Создаем шаблон для списка категорий

### 8.1. Создаем `uch/apps/blog/templates/blog/category_list.html`:
```html
{% extends 'base.html' %}

{% block title %}Категории - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <h1 class="mb-4"><i class="bi bi-folder"></i> Категории</h1>
        
        <div class="row">
            {% for category in categories %}
            <div class="col-md-6 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <h5 class="card-title">
                            <i class="bi bi-folder2"></i> {{ category.name }}
                        </h5>
                        <p class="card-text text-muted">
                            {{ category.description|default:"Описание категории"|truncatechars:100 }}
                        </p>
                        <div class="mb-3">
                            <span class="badge bg-primary">
                                {{ category.article_set.count }} статей
                            </span>
                            {% if category.children.exists %}
                            <span class="badge bg-secondary ms-1">
                                {{ category.children.count }} подкатегорий
                            </span>
                            {% endif %}
                        </div>
                    </div>
                    <div class="card-footer bg-transparent">
                        <a href="{% url 'blog:category_detail' category.slug %}" 
                           class="btn btn-outline-primary btn-sm">
                            Перейти <i class="bi bi-arrow-right"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% empty %}
            <div class="col-12">
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i> Категории пока не созданы.
                    {% if user.is_authenticated and user.is_staff %}
                    <a href="/admin/blog/category/add/" class="alert-link">Создать первую категорию</a>
                    {% endif %}
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Информация -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> О категориях</h5>
            </div>
            <div class="card-body">
                <p>Категории помогают организовать статьи по темам. Вы можете:</p>
                <ul class="list-unstyled">
                    <li><i class="bi bi-check text-success"></i> Искать статьи по категориям</li>
                    <li><i class="bi bi-check text-success"></i> Подписаться на обновления категории</li>
                    <li><i class="bi bi-check text-success"></i> Создавать вложенные категории</li>
                </ul>
            </div>
        </div>

        <!-- Популярные категории -->
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-bar-chart"></i> Самые активные</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for category in categories|dictsortreversed:"article_set.count"|slice:":5" %}
                    <a href="{% url 'blog:category_detail' category.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        {{ category.name|truncatechars:20 }}
                        <span class="badge bg-primary rounded-pill">{{ category.article_set.count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
```

---

## ✅ Финальная проверка:

1. **Главная страница**: http://localhost:8000/
2. **Список статей**: http://localhost:8000/articles/
3. **Детальная статья**: кликните на любую статью
4. **Категории**: http://localhost:8000/categories/
5. **Поиск**: попробуйте найти статьи по словам "музыка", "дизайн"

---

## 🎉 Отличная работа! Фаза 1 (недели 3-4) завершена:

### ✅ Выполнено:
1. **Модели блога** созданы (Article, Category, MediaItem, Comment)
2. **Админка** настроена и работает
3. **Представления** созданы (home, список, детальная, категории)
4. **Шаблоны** созданы (base, home, article_list, article_detail, category_list)
5. **URL маршруты** настроены
6. **Тестовые данные** созданы

### 📊 Статус по спецификации v0.3:

| Раздел спецификации | Статус выполнения |
| --- | --- |
| Ядро платформы (Self-hosted) | ✅ 80% |
| Блог/Портфолио (Линия A) | ✅ 70% |
| Творческая среда (Линия Б) | ⏳ 0% |
| Стек технологий | ✅ 80% |

---

## 🚀 Следующие шаги (недели 5-6):

1. **Markdown редактор с предпросмотром** в админке
2. **Импорт из Obsidian** (Git синхронизация)
3. **RSS/Atom генерация**
4. **Полнотекстовый поиск**
5. **Оптимизация производительности**

**Хотите продолжить с Markdown-редактором в админке?**

---

## 👤 **Kirill**

7.1 
вижу в рекомендованных - все статьи
в последних - все статьи
боковой панели нет - категории ниже статей

7.2
1.TemplateDoesNotExist at /articles/
blog/article_list.html, blog/article_list.html
Request Method:	GET
Request URL:	http://localhost:8000/articles/
Django Version:	4.2.7
Exception Type:	TemplateDoesNotExist
Exception Value:	
blog/article_list.html, blog/article_list.html
Exception Location:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/template/loader.py, line 47, in select_template
Raised during:	uch.apps.blog.views.ArticleListView
Python Executable:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/bin/python
Python Version:	3.8.9
Python Path:	
['/Users/kirillkravcov/UniversalCreativeHub/uch-backend',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python38.zip',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/lib-dynload',
 '/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages']
Server time:	Sun, 07 Dec 2025 13:33:26 +0000

2.TemplateDoesNotExist at /articles/core-orchestrator-architecture/
blog/article_detail.html
Request Method:	GET
Request URL:	http://localhost:8000/articles/core-orchestrator-architecture/
Django Version:	4.2.7
Exception Type:	TemplateDoesNotExist
Exception Value:	
blog/article_detail.html
Exception Location:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/template/loader.py, line 47, in select_template
Raised during:	uch.apps.blog.views.ArticleDetailView
Python Executable:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/bin/python
Python Version:	3.8.9
Python Path:	
['/Users/kirillkravcov/UniversalCreativeHub/uch-backend',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python38.zip',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/lib-dynload',
 '/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages']
Server time:	Sun, 07 Dec 2025 13:36:44 +0000

3.TemplateDoesNotExist at /categories/
blog/category_list.html, blog/category_list.html
Request Method:	GET
Request URL:	http://localhost:8000/categories/
Django Version:	4.2.7
Exception Type:	TemplateDoesNotExist
Exception Value:	
blog/category_list.html, blog/category_list.html
Exception Location:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/template/loader.py, line 47, in select_template
Raised during:	uch.apps.blog.views.CategoryListView
Python Executable:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/bin/python
Python Version:	3.8.9
Python Path:	
['/Users/kirillkravcov/UniversalCreativeHub/uch-backend',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python38.zip',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/lib-dynload',
 '/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages']
Server time:	Sun, 07 Dec 2025 13:33:37 +0000

4. TemplateDoesNotExist at /category/design/
blog/article_list.html, blog/article_list.html
Request Method:	GET
Request URL:	http://localhost:8000/category/design/
Django Version:	4.2.7
Exception Type:	TemplateDoesNotExist
Exception Value:	
blog/article_list.html, blog/article_list.html
Exception Location:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages/django/template/loader.py, line 47, in select_template
Raised during:	uch.apps.blog.views.ArticleListView
Python Executable:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/bin/python
Python Version:	3.8.9
Python Path:	
['/Users/kirillkravcov/UniversalCreativeHub/uch-backend',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python38.zip',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/lib-dynload',
 '/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages']
Server time:	Sun, 07 Dec 2025 13:37:28 +0000

---

## 🤖 **DeepSeek**

Проблема: шаблоны не находятся. Это потому что Django ищет их в неправильных местах. Давайте исправим структуру шаблонов.

---

## 🔧 Шаг 1: Проверяем структуру шаблонов

### 1.1. Посмотрим текущую структуру:
```bash
find . -name "*.html" -type f
```

### 1.2. Должно быть:
```
./uch/apps/blog/templates/blog/home.html
./uch/apps/blog/templates/blog/article_list.html
./uch/apps/blog/templates/blog/article_detail.html
./uch/apps/blog/templates/blog/category_list.html
./uch/templates/base.html
```

### 1.3. Если структуры нет, создадим ее:
```bash
# Создаем структуру папок
mkdir -p uch/apps/blog/templates/blog

# Создаем шаблоны
touch uch/apps/blog/templates/blog/home.html
touch uch/apps/blog/templates/blog/article_list.html
touch uch/apps/blog/templates/blog/article_detail.html
touch uch/apps/blog/templates/blog/category_list.html
```

---

## 🔧 Шаг 2: Копируем содержимое шаблонов

### 2.1. Копируем ранее созданные шаблоны:

**article_list.html**:
```bash
cat > uch/apps/blog/templates/blog/article_list.html << 'EOF'
{% extends 'base.html' %}

{% block title %}Статьи - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <h1 class="mb-4">
            {% if category %}
            <i class="bi bi-folder"></i> {{ category.name }}
            {% elif request.GET.tag %}
            <i class="bi bi-tag"></i> Тег: {{ request.GET.tag }}
            {% elif request.GET.q %}
            <i class="bi bi-search"></i> Поиск: "{{ request.GET.q }}"
            {% else %}
            <i class="bi bi-journal-text"></i> Все статьи
            {% endif %}
        </h1>

        <!-- Статистика -->
        <div class="alert alert-light mb-4">
            <div class="row">
                <div class="col-md-4">
                    <i class="bi bi-journal"></i> Статей: <strong>{{ page_obj.paginator.count }}</strong>
                </div>
                <div class="col-md-4">
                    <i class="bi bi-folder"></i> Категорий: <strong>{{ categories|length }}</strong>
                </div>
                <div class="col-md-4">
                    <i class="bi bi-tags"></i> Тегов: <strong>{{ popular_tags|length }}</strong>
                </div>
            </div>
        </div>

        <!-- Список статей -->
        {% for article in articles %}
        <div class="card mb-4 article-card">
            <div class="row g-0">
                {% if article.cover_image %}
                <div class="col-md-4">
                    <img src="{{ article.cover_image.url }}" class="img-fluid rounded-start" 
                         alt="{{ article.title }}" style="height: 200px; width: 100%; object-fit: cover;">
                </div>
                {% endif %}
                <div class="col-md-{% if article.cover_image %}8{% else %}12{% endif %}">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <div>
                                <span class="badge bg-primary category-badge">
                                    {{ article.category.name|default:"Без категории" }}
                                </span>
                                {% if article.is_featured %}
                                <span class="badge bg-warning text-dark ms-1">
                                    <i class="bi bi-star"></i> Рекомендуем
                                </span>
                                {% endif %}
                            </div>
                            <small class="text-muted">
                                <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                            </small>
                        </div>
                        
                        <h5 class="card-title">{{ article.title }}</h5>
                        <p class="card-text text-muted">{{ article.excerpt|truncatechars:200 }}</p>
                        
                        <div class="d-flex justify-content-between align-items-center mt-3">
                            <div>
                                {% for tag in article.tags.all|slice:":3" %}
                                <a href="?tag={{ tag.slug }}" class="badge bg-light text-dark text-decoration-none border">
                                    {{ tag.name }}
                                </a>
                                {% endfor %}
                            </div>
                            <div>
                                <a href="{{ article.get_absolute_url }}" class="btn btn-outline-primary btn-sm">
                                    Читать <i class="bi bi-arrow-right"></i>
                                </a>
                            </div>
                        </div>
                        
                        <div class="mt-3 text-muted small">
                            <i class="bi bi-person"></i> {{ article.author.username }}
                            {% if article.comments.count > 0 %}
                            <span class="ms-3">
                                <i class="bi bi-chat"></i> {{ article.comments.count }} комментариев
                            </span>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {% empty %}
        <div class="text-center py-5">
            <i class="bi bi-emoji-frown display-1 text-muted"></i>
            <h3 class="mt-3">Статьи не найдены</h3>
            <p class="text-muted">
                {% if request.GET.q %}
                По запросу "{{ request.GET.q }}" ничего не найдено.
                {% else %}
                Пока нет опубликованных статей.
                {% endif %}
            </p>
            <a href="{% url 'blog:article_list' %}" class="btn btn-outline-primary mt-2">
                <i class="bi bi-arrow-left"></i> Все статьи
            </a>
        </div>
        {% endfor %}

        <!-- Пагинация -->
        {% if page_obj.paginator.num_pages > 1 %}
        <nav aria-label="Навигация по страницам">
            <ul class="pagination justify-content-center">
                {% if page_obj.has_previous %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.previous_page_number }}{% if request.GET.q %}&q={{ request.GET.q }}{% endif %}">
                        <i class="bi bi-chevron-left"></i>
                    </a>
                </li>
                {% endif %}

                {% for num in page_obj.paginator.page_range %}
                    {% if page_obj.number == num %}
                    <li class="page-item active">
                        <span class="page-link">{{ num }}</span>
                    </li>
                    {% elif num > page_obj.number|add:-3 and num < page_obj.number|add:3 %}
                    <li class="page-item">
                        <a class="page-link" href="?page={{ num }}{% if request.GET.q %}&q={{ request.GET.q }}{% endif %}">{{ num }}</a>
                    </li>
                    {% endif %}
                {% endfor %}

                {% if page_obj.has_next %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.next_page_number }}{% if request.GET.q %}&q={{ request.GET.q }}{% endif %}">
                        <i class="bi bi-chevron-right"></i>
                    </a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Поиск -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-search"></i> Поиск</h5>
            </div>
            <div class="card-body">
                <form action="{% url 'blog:article_list' %}" method="get">
                    <div class="input-group">
                        <input type="text" class="form-control" name="q" 
                               placeholder="Поиск статей..." value="{{ request.GET.q|default:'' }}">
                        <button class="btn btn-primary" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Категории -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    <a href="{% url 'blog:article_list' %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center {% if not category %}active{% endif %}">
                        Все категории
                        <span class="badge bg-primary rounded-pill">{{ page_obj.paginator.count }}</span>
                    </a>
                    {% for cat in categories %}
                    <a href="{% url 'blog:category_detail' cat.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center {% if category and category.slug == cat.slug %}active{% endif %}">
                        {{ cat.name }}
                        <span class="badge bg-primary rounded-pill">{{ cat.article_set.count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Популярные теги -->
        {% if popular_tags %}
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-tags"></i> Популярные теги</h5>
            </div>
            <div class="card-body">
                <div class="d-flex flex-wrap gap-2">
                    {% for tag in popular_tags %}
                    <a href="?tag={{ tag.slug }}" 
                       class="badge {% if request.GET.tag == tag.slug %}bg-primary text-white{% else %}bg-light text-dark border{% endif %} text-decoration-none p-2">
                        {{ tag.name }}
                        <span class="badge bg-secondary rounded-pill ms-1">{{ tag.num_times }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Последние статьи -->
        {% if recent_articles %}
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-clock-history"></i> Недавние статьи</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for article in recent_articles %}
                    <a href="{{ article.get_absolute_url }}" class="list-group-item list-group-item-action">
                        <div class="d-flex w-100 justify-content-between">
                            <h6 class="mb-1">{{ article.title|truncatechars:40 }}</h6>
                            <small>{{ article.published_at|date:"d.m" }}</small>
                        </div>
                        <small class="text-muted">
                            {{ article.category.name|default:"Без категории" }}
                        </small>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}
EOF
```

**article_detail.html**:
```bash
cat > uch/apps/blog/templates/blog/article_detail.html << 'EOF'
{% extends 'base.html' %}

{% block title %}{{ article.title }} - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <!-- Навигация -->
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{% url 'blog:home' %}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{% url 'blog:article_list' %}">Статьи</a></li>
                {% if article.category %}
                <li class="breadcrumb-item">
                    <a href="{% url 'blog:category_detail' article.category.slug %}">
                        {{ article.category.name }}
                    </a>
                </li>
                {% endif %}
                <li class="breadcrumb-item active" aria-current="page">{{ article.title|truncatechars:30 }}</li>
            </ol>
        </nav>

        <!-- Заголовок статьи -->
        <article class="mb-5">
            <header class="mb-4">
                <div class="d-flex justify-content-between align-items-start mb-3">
                    <div>
                        <span class="badge bg-primary category-badge mb-2">
                            {{ article.category.name|default:"Без категории" }}
                        </span>
                        {% if article.is_featured %}
                        <span class="badge bg-warning text-dark ms-1">
                            <i class="bi bi-star"></i> Рекомендуем
                        </span>
                        {% endif %}
                    </div>
                    <small class="text-muted">
                        <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y H:i" }}
                    </small>
                </div>
                
                <h1 class="display-6">{{ article.title }}</h1>
                
                {% if article.excerpt %}
                <p class="lead text-muted">{{ article.excerpt }}</p>
                {% endif %}
                
                <div class="d-flex align-items-center mt-3">
                    <div class="me-3">
                        <i class="bi bi-person-circle"></i>
                        <strong>{{ article.author.username }}</strong>
                    </div>
                    <div class="text-muted">
                        {% if article.updated_at != article.created_at %}
                        <i class="bi bi-pencil"></i> Обновлено: {{ article.updated_at|date:"d.m.Y" }}
                        {% endif %}
                    </div>
                </div>
            </header>

            <!-- Обложка -->
            {% if article.cover_image %}
            <div class="text-center mb-4">
                <img src="{{ article.cover_image.url }}" 
                     class="img-fluid rounded" 
                     alt="{{ article.title }}"
                     style="max-height: 400px; object-fit: cover;">
                {% if article.cover_image.caption %}
                <p class="text-muted mt-2"><small>{{ article.cover_image.caption }}</small></p>
                {% endif %}
            </div>
            {% endif %}

            <!-- Содержание статьи -->
            <div class="article-content mb-5">
                {{ article.content_html|safe }}
            </div>

            <!-- Теги -->
            {% if article.tags.all %}
            <div class="mb-4">
                <h5><i class="bi bi-tags"></i> Теги</h5>
                <div class="d-flex flex-wrap gap-2">
                    {% for tag in article.tags.all %}
                    <a href="{% url 'blog:article_list' %}?tag={{ tag.slug }}" 
                       class="badge bg-light text-dark text-decoration-none border p-2">
                        {{ tag.name }}
                    </a>
                    {% endfor %}
                </div>
            </div>
            {% endif %}

            <!-- Навигация между статьями -->
            <div class="d-flex justify-content-between mt-5 pt-4 border-top">
                <div>
                    {% if article.get_previous_by_published_at %}
                    <a href="{{ article.get_previous_by_published_at.get_absolute_url }}" 
                       class="btn btn-outline-secondary">
                        <i class="bi bi-chevron-left"></i> Предыдущая
                    </a>
                    {% endif %}
                </div>
                <div>
                    <a href="{% url 'blog:article_list' %}" class="btn btn-outline-primary">
                        <i class="bi bi-list-ul"></i> Все статьи
                    </a>
                </div>
                <div>
                    {% if article.get_next_by_published_at %}
                    <a href="{{ article.get_next_by_published_at.get_absolute_url }}" 
                       class="btn btn-outline-secondary">
                        Следующая <i class="bi bi-chevron-right"></i>
                    </a>
                    {% endif %}
                </div>
            </div>
        </article>

        <!-- Комментарии -->
        {% if article.allow_comments %}
        <section class="mt-5 pt-4 border-top">
            <h4 class="mb-4">
                <i class="bi bi-chat"></i> Комментарии
                <span class="badge bg-secondary ms-2">{{ article.comments.count }}</span>
            </h4>
            
            {% if article.comments.exists %}
                {% for comment in article.comments.all %}
                    {% if comment.is_approved or user.is_staff %}
                    <div class="card mb-3 {% if not comment.is_approved %}border-warning{% endif %}">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <div>
                                    <strong>{{ comment.author.username }}</strong>
                                    <small class="text-muted ms-2">
                                        {{ comment.created_at|date:"d.m.Y H:i" }}
                                    </small>
                                </div>
                                {% if not comment.is_approved and user.is_staff %}
                                <span class="badge bg-warning text-dark">
                                    <i class="bi bi-eye-slash"></i> На модерации
                                </span>
                                {% endif %}
                            </div>
                            <p class="card-text">{{ comment.content }}</p>
                            
                            {% if comment.replies.exists %}
                            <div class="ms-4 mt-3">
                                <h6><i class="bi bi-reply"></i> Ответы:</h6>
                                {% for reply in comment.replies.all %}
                                    {% if reply.is_approved or user.is_staff %}
                                    <div class="card bg-light mb-2">
                                        <div class="card-body py-2">
                                            <div class="d-flex justify-content-between">
                                                <strong>{{ reply.author.username }}</strong>
                                                <small class="text-muted">{{ reply.created_at|date:"d.m.Y H:i" }}</small>
                                            </div>
                                            <p class="mb-0">{{ reply.content }}</p>
                                        </div>
                                    </div>
                                    {% endif %}
                                {% endfor %}
                            </div>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}
                {% endfor %}
            {% else %}
            <div class="alert alert-info">
                <i class="bi bi-info-circle"></i> Комментариев пока нет. Будьте первым!
            </div>
            {% endif %}
            
            <!-- Форма комментария -->
            {% if user.is_authenticated %}
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Добавить комментарий</h5>
                </div>
                <div class="card-body">
                    <form method="post" action="#">
                        {% csrf_token %}
                        <div class="mb-3">
                            <textarea class="form-control" name="content" rows="4" 
                                      placeholder="Ваш комментарий..." required></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-send"></i> Отправить
                        </button>
                    </form>
                </div>
            </div>
            {% else %}
            <div class="alert alert-warning">
                <i class="bi bi-exclamation-triangle"></i> 
                Чтобы оставить комментарий, <a href="/admin/login/?next={{ request.path }}">войдите</a>.
            </div>
            {% endif %}
        </section>
        {% endif %}
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Информация об авторе -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-person-circle"></i> Об авторе</h5>
            </div>
            <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                    <div class="me-3">
                        <i class="bi bi-person" style="font-size: 2rem;"></i>
                    </div>
                    <div>
                        <h6 class="mb-0">{{ article.author.username }}</h6>
                        <small class="text-muted">Автор статьи</small>
                    </div>
                </div>
                <p class="card-text">
                    {% if article.author.profile.bio %}
                        {{ article.author.profile.bio|truncatechars:200 }}
                    {% else %}
                        Активный участник Universal Creative Hub.
                    {% endif %}
                </p>
            </div>
        </div>

        <!-- Похожие статьи -->
        {% if recent_articles %}
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-newspaper"></i> Похожие статьи</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for related_article in recent_articles %}
                    <a href="{{ related_article.get_absolute_url }}" 
                       class="list-group-item list-group-item-action">
                        <div class="d-flex w-100 justify-content-between">
                            <h6 class="mb-1">{{ related_article.title|truncatechars:40 }}</h6>
                            <small>{{ related_article.published_at|date:"d.m" }}</small>
                        </div>
                        <small class="text-muted">
                            {{ related_article.category.name|default:"Без категории" }}
                        </small>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Категории -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for cat in categories %}
                    <a href="{% url 'blog:category_detail' cat.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        {{ cat.name }}
                        <span class="badge bg-primary rounded-pill">{{ cat.article_set.count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Действия -->
        {% if user.is_authenticated and user.is_staff %}
        <div class="card border-primary">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="bi bi-gear"></i> Администрирование</h5>
            </div>
            <div class="card-body">
                <div class="d-grid gap-2">
                    <a href="/admin/blog/article/{{ article.id }}/change/" 
                       class="btn btn-outline-primary" target="_blank">
                        <i class="bi bi-pencil"></i> Редактировать
                    </a>
                    <a href="/admin/blog/article/{{ article.id }}/delete/" 
                       class="btn btn-outline-danger" target="_blank">
                        <i class="bi bi-trash"></i> Удалить
                    </a>
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}

{% block extra_css %}
<style>
    .article-content {
        font-size: 1.1rem;
        line-height: 1.8;
    }
    
    .article-content h1, .article-content h2, .article-content h3 {
        margin-top: 2rem;
        margin-bottom: 1rem;
    }
    
    .article-content img {
        max-width: 100%;
        height: auto;
        border-radius: 8px;
        margin: 1.5rem 0;
    }
    
    .article-content pre {
        background-color: #f8f9fa;
        padding: 1rem;
        border-radius: 6px;
        border-left: 4px solid #0d6efd;
    }
    
    .article-content code {
        background-color: #f8f9fa;
        padding: 0.2rem 0.4rem;
        border-radius: 4px;
        font-family: 'Courier New', monospace;
    }
    
    .article-content blockquote {
        border-left: 4px solid #6c757d;
        padding-left: 1rem;
        color: #6c757d;
        font-style: italic;
        margin: 1.5rem 0;
    }
    
    .article-content table {
        width: 100%;
        margin: 1.5rem 0;
        border-collapse: collapse;
    }
    
    .article-content table th,
    .article-content table td {
        padding: 0.75rem;
        border: 1px solid #dee2e6;
    }
    
    .article-content table th {
        background-color: #f8f9fa;
        font-weight: bold;
    }
</style>
{% endblock %}
EOF
```

**category_list.html**:
```bash
cat > uch/apps/blog/templates/blog/category_list.html << 'EOF'
{% extends 'base.html' %}

{% block title %}Категории - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <h1 class="mb-4"><i class="bi bi-folder"></i> Категории</h1>
        
        <div class="row">
            {% for category in categories %}
            <div class="col-md-6 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <h5 class="card-title">
                            <i class="bi bi-folder2"></i> {{ category.name }}
                        </h5>
                        <p class="card-text text-muted">
                            {{ category.description|default:"Описание категории"|truncatechars:100 }}
                        </p>
                        <div class="mb-3">
                            <span class="badge bg-primary">
                                {{ category.article_set.count }} статей
                            </span>
                            {% if category.children.exists %}
                            <span class="badge bg-secondary ms-1">
                                {{ category.children.count }} подкатегорий
                            </span>
                            {% endif %}
                        </div>
                    </div>
                    <div class="card-footer bg-transparent">
                        <a href="{% url 'blog:category_detail' category.slug %}" 
                           class="btn btn-outline-primary btn-sm">
                            Перейти <i class="bi bi-arrow-right"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% empty %}
            <div class="col-12">
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i> Категории пока не созданы.
                    {% if user.is_authenticated and user.is_staff %}
                    <a href="/admin/blog/category/add/" class="alert-link">Создать первую категорию</a>
                    {% endif %}
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Информация -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> О категориях</h5>
            </div>
            <div class="card-body">
                <p>Категории помогают организовать статьи по темам. Вы можете:</p>
                <ul class="list-unstyled">
                    <li><i class="bi bi-check text-success"></i> Искать статьи по категориям</li>
                    <li><i class="bi bi-check text-success"></i> Подписаться на обновления категории</li>
                    <li><i class="bi bi-check text-success"></i> Создавать вложенные категории</li>
                </ul>
            </div>
        </div>

        <!-- Популярные категории -->
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-bar-chart"></i> Самые активные</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for category in categories|dictsortreversed:"article_set.count"|slice:":5" %}
                    <a href="{% url 'blog:category_detail' category.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        {{ category.name|truncatechars:20 }}
                        <span class="badge bg-primary rounded-pill">{{ category.article_set.count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
EOF
```

---

## 🔧 Шаг 3: Проверяем настройки TEMPLATES в settings.py

### 3.1. Убедимся, что в `uch/settings.py` правильные настройки:
```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [
            os.path.join(BASE_DIR, 'templates'),  # Глобальные шаблоны
        ],
        'APP_DIRS': True,  # ВАЖНО: должно быть True для поиска шаблонов в приложениях
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]
```

### 3.2. Проверим, что `APP_DIRS = True`

---

## 🔧 Шаг 4: Перезапускаем сервер и проверяем

### 4.1. Перезапускаем сервер:
```bash
python manage.py runserver
```

### 4.2. Проверяем все страницы:
1. **Главная**: http://localhost:8000/ (должна работать)
2. **Список статей**: http://localhost:8000/articles/
3. **Детальная статья**: http://localhost:8000/articles/my-first-music-project/
4. **Категории**: http://localhost:8000/categories/
5. **Фильтр по категории**: http://localhost:8000/category/music/

---

## 🔧 Шаг 5: Если все еще есть ошибки

### 5.1. Проверим права доступа к файлам:
```bash
ls -la uch/apps/blog/templates/blog/
```

### 5.2. Проверим, импортируются ли шаблоны:
```bash
python manage.py shell
```

```python
from django.template.loader import get_template

try:
    template = get_template('blog/article_list.html')
    print(f"Шаблон найден: {template.origin}")
except Exception as e:
    print(f"Ошибка: {e}")

exit()
```

---

## ✅ После создания шаблонов:

1. **Остановите сервер** (Ctrl+C)
2. **Запустите заново**: `python manage.py runserver`
3. **Проверьте все страницы** - они должны работать корректно
4. **Проверьте боковую панель** - теперь категории должны отображаться справа

---

**Сообщите, какие страницы теперь работают, а какие еще нет. Также проверьте, появилась ли боковая панель с категориями.**

---

## 👤 **Kirill**

все работает но категории не отображаются справа. Боковой панели нет

---

## 🤖 **DeepSeek**

Проблема в том, что боковая панель с категориями есть в шаблоне, но данные (`categories`) не передаются в контекст главной страницы. Давайте исправим.

---

## 🔧 Шаг 1: Проверяем и исправляем `home_view`

### 1.1. Открываем `uch/apps/blog/views.py` и находим функцию `home_view`:

```python
def home_view(request):
    """Домашняя страница блога"""
    featured_articles = Article.objects.filter(
        status='published', is_featured=True
    )[:3]
    
    recent_articles = Article.objects.filter(status='published')[:6]
    categories = Category.objects.filter(is_active=True)[:8]  # Уже есть
    
    # Популярные теги
    from django.db.models import Count
    from taggit.models import Tag
    popular_tags = Tag.objects.annotate(
        num_times=Count('taggit_taggeditem_items')
    ).order_by('-num_times')[:10]
    
    context = {
        'featured_articles': featured_articles,
        'recent_articles': recent_articles,
        'categories': categories,  # Это передается
        'popular_tags': popular_tags,
    }
    return render(request, 'blog/home.html', context)
```

Проблема в том, что в `home.html` используется `categories`, но боковая панель может быть в отдельном блоке. Давайте проверим шаблон `home.html`.

---

## 🔧 Шаг 2: Исправляем `home.html`

### 2.1. Проверим структуру `home.html`:
Откройте `uch/apps/blog/templates/blog/home.html` и найдите раздел "Боковая панель".

**Проблема**: В `home.html` у нас нет блока с категориями в боковой панели! Давайте добавим.

### 2.2. Обновляем `home.html`:
Замените весь файл или найдите раздел `<!-- Боковая панель -->` и добавьте карточку с категориями:

```html
{% extends 'base.html' %}

{% block title %}Главная - Universal Creative Hub{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <!-- Приветствие -->
        <div class="jumbotron bg-light p-5 rounded mb-4">
            <h1 class="display-4">Universal Creative Hub</h1>
            <p class="lead">Self-hosted платформа для творчества, объединяющая блог, портфолио и музыкальную студию.</p>
            <hr class="my-4">
            <p>Создавайте, делитесь и вдохновляйтесь в единой среде.</p>
        </div>

        <!-- Рекомендуемые статьи -->
        {% if featured_articles %}
        <h2 class="mb-3">Рекомендуемые статьи</h2>
        <div class="row mb-5">
            {% for article in featured_articles %}
            <div class="col-md-4 mb-3">
                <div class="card article-card h-100">
                    {% if article.cover_image %}
                    <img src="{{ article.cover_image.url }}" class="card-img-top" alt="{{ article.title }}" style="height: 180px; object-fit: cover;">
                    {% endif %}
                    <div class="card-body">
                        <span class="badge bg-primary category-badge mb-2">
                            {{ article.category.name|default:"Без категории" }}
                        </span>
                        <h5 class="card-title">{{ article.title|truncatechars:50 }}</h5>
                        <p class="card-text text-muted">{{ article.excerpt|truncatechars:100|default:"" }}</p>
                    </div>
                    <div class="card-footer bg-transparent">
                        <small class="text-muted">
                            <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                            <i class="bi bi-person ms-2"></i> {{ article.author.username }}
                        </small>
                        <a href="{{ article.get_absolute_url }}" class="btn btn-sm btn-outline-primary float-end">
                            Читать
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        {% endif %}

        <!-- Последние статьи -->
        <h2 class="mb-3">Последние статьи</h2>
        <div class="row">
            {% for article in recent_articles %}
            <div class="col-md-6 mb-4">
                <div class="card article-card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <span class="badge bg-secondary category-badge mb-2">
                                    {{ article.category.name|default:"Без категории" }}
                                </span>
                                <h5 class="card-title">{{ article.title }}</h5>
                            </div>
                            {% if article.is_featured %}
                            <span class="badge bg-warning text-dark">
                                <i class="bi bi-star"></i> Рекомендуем
                            </span>
                            {% endif %}
                        </div>
                        
                        <p class="card-text text-muted">{{ article.excerpt|truncatechars:150|default:"" }}</p>
                        
                        <div class="mt-3">
                            {% for tag in article.tags.all|slice:":3" %}
                            <span class="badge bg-light text-dark border">{{ tag.name }}</span>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="card-footer bg-transparent">
                        <small class="text-muted">
                            <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                            <i class="bi bi-person ms-2"></i> {{ article.author.username }}
                        </small>
                        <a href="{{ article.get_absolute_url }}" class="btn btn-sm btn-outline-primary float-end">
                            Читать <i class="bi bi-arrow-right"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% empty %}
            <div class="col-12">
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i> Пока нет опубликованных статей.
                    {% if user.is_authenticated and user.is_staff %}
                    <a href="/admin/blog/article/add/" class="alert-link">Создайте первую статью</a>
                    {% endif %}
                </div>
            </div>
            {% endfor %}
        </div>
        
        <!-- Кнопка "Все статьи" -->
        <div class="text-center mt-4">
            <a href="{% url 'blog:article_list' %}" class="btn btn-primary btn-lg">
                <i class="bi bi-list-ul"></i> Все статьи
            </a>
        </div>
    </div>

    <!-- Боковая панель -->
    <div class="col-lg-4">
        <!-- Поиск -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-search"></i> Поиск</h5>
            </div>
            <div class="card-body">
                <form action="{% url 'blog:article_list' %}" method="get">
                    <div class="input-group">
                        <input type="text" class="form-control" name="q" placeholder="Поиск статей...">
                        <button class="btn btn-primary" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Категории -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for category in categories %}
                    <a href="{% url 'blog:category_detail' category.slug %}" 
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        {{ category.name }}
                        <span class="badge bg-primary rounded-pill">{{ category.article_set.count }}</span>
                    </a>
                    {% empty %}
                    <div class="text-muted">Нет категорий</div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Популярные теги -->
        {% if popular_tags %}
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-tags"></i> Популярные теги</h5>
            </div>
            <div class="card-body">
                <div class="d-flex flex-wrap gap-2">
                    {% for tag in popular_tags %}
                    <a href="{% url 'blog:article_list' %}?tag={{ tag.slug }}" 
                       class="badge bg-light text-dark text-decoration-none border p-2">
                        {{ tag.name }}
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Информация -->
        <div class="card">
            <div class="card-header bg-dark text-white">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> О платформе</h5>
            </div>
            <div class="card-body">
                <p>Universal Creative Hub — это self-hosted платформа, которая объединяет:</p>
                <ul class="list-unstyled">
                    <li><i class="bi bi-check-circle text-success"></i> Блог и портфолио</li>
                    <li><i class="bi bi-check-circle text-success"></i> Музыкальную студию (DAW)</li>
                    <li><i class="bi bi-check-circle text-success"></i> Мультимедиа галерею</li>
                    <li><i class="bi bi-check-circle text-success"></i> Систему проектов</li>
                </ul>
                <div class="alert alert-warning mt-3">
                    <small>
                        <i class="bi bi-exclamation-triangle"></i> 
                        Сейчас работает блоговая часть. Музыкальная студия в разработке.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
```

---

## 🔧 Шаг 3: Создаем контекстный процессор для глобальных данных

Чтобы категории отображались на ВСЕХ страницах, создадим контекстный процессор:

### 3.1. Создаем файл `uch/apps/blog/context_processors.py`:
```python
from .models import Category


def blog_categories(request):
    """Добавляет категории блога во все шаблоны"""
    return {
        'blog_categories': Category.objects.filter(is_active=True)[:10]
    }
```

### 3.2. Обновляем `uch/settings.py`:
```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'uch.apps.blog.context_processors.blog_categories',  # Добавляем наш процессор
            ],
        },
    },
]
```

### 3.3. Обновляем `base.html` чтобы использовать глобальные категории:

В `uch/templates/base.html` добавьте в навигационную панель выпадающий список категорий:

```html
<!-- Навигационная панель -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="bi bi-lightning-charge"></i> Universal Creative Hub
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'blog:home' %}">Главная</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'blog:article_list' %}">Статьи</a>
                </li>
                
                <!-- Выпадающий список категорий -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-folder"></i> Категории
                    </a>
                    <ul class="dropdown-menu">
                        {% for category in blog_categories %}
                        <li>
                            <a class="dropdown-item" href="{% url 'blog:category_detail' category.slug %}">
                                {{ category.name }}
                                <span class="badge bg-primary rounded-pill float-end">{{ category.article_set.count }}</span>
                            </a>
                        </li>
                        {% empty %}
                        <li><a class="dropdown-item text-muted" href="#">Нет категорий</a></li>
                        {% endfor %}
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item" href="{% url 'blog:category_list' %}">
                                <i class="bi bi-list-ul"></i> Все категории
                            </a>
                        </li>
                    </ul>
                </li>
                
                {% if user.is_authenticated and user.is_staff %}
                <li class="nav-item">
                    <a class="nav-link" href="/admin/" target="_blank">
                        <i class="bi bi-gear"></i> Админка
                    </a>
                </li>
                {% endif %}
            </ul>
            
            <form class="d-flex" action="{% url 'blog:article_list' %}" method="get">
                <input class="form-control me-2" type="search" name="q" placeholder="Поиск статей...">
                <button class="btn btn-outline-light" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </form>
        </div>
    </div>
</nav>
```

---

## 🔧 Шаг 4: Быстрое решение - обновить только home.html

Если хотите быстрое решение, просто убедитесь, что в `home.html` есть правильная структура с `col-lg-4` для боковой панели.

Проверьте, что в `home.html` есть:
```html
<div class="row">
    <div class="col-lg-8">
        <!-- Основной контент -->
    </div>
    
    <div class="col-lg-4">
        <!-- Боковая панель с категориями -->
    </div>
</div>
```

---

## ✅ Проверяем:

1. **Перезапустите сервер:**
```bash
python manage.py runserver
```

2. **Откройте главную страницу:** http://localhost:8000/
   - Должна появиться боковая панель справа
   - Категории должны отображаться в карточке "Категории"

3. **Проверьте другие страницы:**
   - Статьи: http://localhost:8000/articles/ (там уже есть боковая панель)
   - Категории: http://localhost:8000/categories/

---

## 🔍 Если боковая панель все еще не отображается:

1. **Проверьте CSS:** Возможно, Bootstrap не загружается
2. **Проверьте структуру HTML:** Откройте "Инспектор элементов" в браузере (F12)
3. **Проверьте данные:** Убедитесь, что категории есть в базе

**Быстрая проверка в shell:**
```bash
python manage.py shell
```

```python
from uch.apps.blog.models import Category
print(f"Категорий в базе: {Category.objects.count()}")
for cat in Category.objects.all():
    print(f"- {cat.name}: {cat.article_set.count()} статей")
exit()
```

---

**Сообщите, появилась ли боковая панель после этих изменений!**

---

## 👤 **Kirill**

ModuleNotFoundError at /
No module named 'uch.apps.blog.context_processors'
Request Method:	GET
Request URL:	http://localhost:8000/
Django Version:	4.2.7
Exception Type:	ModuleNotFoundError
Exception Value:	
No module named 'uch.apps.blog.context_processors'
Exception Location:	<frozen importlib._bootstrap>, line 973, in _find_and_load_unlocked
Raised during:	uch.apps.blog.views.home_view
Python Executable:	/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/bin/python
Python Version:	3.8.9
Python Path:	
['/Users/kirillkravcov/UniversalCreativeHub/uch-backend',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python38.zip',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8',
 '/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.8/lib/python3.8/lib-dynload',
 '/Users/kirillkravcov/UniversalCreativeHub/uch-backend/venv/lib/python3.8/site-packages']
Server time:	Sun, 07 Dec 2025 14:03:31 +0000

---

## 🤖 **DeepSeek**

Нужно создать файл `context_processors.py`. Давайте сделаем это:

---

## 🔧 Шаг 1: Создаем контекстный процессор

### 1.1. Создаем файл `uch/apps/blog/context_processors.py`:
```bash
cat > uch/apps/blog/context_processors.py << 'EOF'
from .models import Category
from taggit.models import Tag
from django.db.models import Count


def blog_categories(request):
    """Добавляет категории блога во все шаблоны"""
    try:
        categories = Category.objects.filter(is_active=True)[:10]
        return {
            'blog_categories': categories
        }
    except:
        return {'blog_categories': []}


def popular_tags(request):
    """Добавляет популярные теги во все шаблоны"""
    try:
        tags = Tag.objects.annotate(
            num_times=Count('taggit_taggeditem_items')
        ).order_by('-num_times')[:10]
        return {
            'popular_tags': tags
        }
    except:
        return {'popular_tags': []}


def blog_stats(request):
    """Добавляет статистику блога"""
    try:
        from .models import Article
        return {
            'total_articles': Article.objects.filter(status='published').count(),
            'total_categories': Category.objects.filter(is_active=True).count(),
        }
    except:
        return {
            'total_articles': 0,
            'total_categories': 0,
        }
EOF
```

---

## 🔧 Шаг 2: Обновляем `settings.py`

### 2.1. Открываем `uch/settings.py` и обновляем TEMPLATES:
```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'uch.apps.blog.context_processors.blog_categories',
                'uch.apps.blog.context_processors.popular_tags',
                'uch.apps.blog.context_processors.blog_stats',
            ],
        },
    },
]
```

---

## 🔧 Шаг 3: Упрощенный подход (без контекстного процессора)

Если хотите быстрее, можно просто обновить `home_view`:

### 3.1. Обновляем `uch/apps/blog/views.py` - функцию `home_view`:
```python
def home_view(request):
    """Домашняя страница блога"""
    featured_articles = Article.objects.filter(
        status='published', is_featured=True
    )[:3]
    
    recent_articles = Article.objects.filter(status='published')[:6]
    categories = Category.objects.filter(is_active=True)[:8]
    
    # Популярные теги
    from django.db.models import Count
    from taggit.models import Tag
    popular_tags = Tag.objects.annotate(
        num_times=Count('taggit_taggeditem_items')
    ).order_by('-num_times')[:10]
    
    context = {
        'featured_articles': featured_articles,
        'recent_articles': recent_articles,
        'categories': categories,


---

## 👤 **Kirill**

ошибка больше не возникает, но и панель справа не появилась. Зато появился поиск)

Обнови текущий прогресс. Дополнительно Сделай контекст для нового чата, может сработать ограничение длины

---

## 🤖 **DeepSeek**

Отлично, поиск работает! Значит базовый функционал в порядке. Давайте решим проблему с боковой панелью и обновим прогресс.

---

## 🔧 Шаг 1: Быстрое решение для боковой панели

Проблема в том, что в `home.html` используется класс `col-lg-4`, но на больших экранах (≥992px) боковая панель может не отображаться из-за нехватки контента или CSS.

### 1.1. Проверим структуру `home.html` - должно быть:
```html
<div class="row">
    <div class="col-lg-8">
        <!-- Основной контент (статьи) -->
    </div>
    
    <div class="col-lg-4">
        <!-- Боковая панель -->
    </div>
</div>
```

### 1.2. Добавим тестовый контент в боковую панель (временно):
Откройте `uch/apps/blog/templates/blog/home.html` и добавьте в конец файла перед `{% endblock %}`:

```html
{% block extra_css %}
<style>
    /* Принудительно показываем боковую панель */
    .sidebar {
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 8px;
        border-left: 4px solid #0d6efd;
    }
    
    /* Убедимся, что сетка Bootstrap работает */
    .row {
        display: flex;
        flex-wrap: wrap;
    }
    
    .col-lg-4 {
        flex: 0 0 33.333333%;
        max-width: 33.333333%;
    }
    
    @media (max-width: 992px) {
        .col-lg-4 {
            flex: 0 0 100%;
            max-width: 100%;
        }
    }
</style>
{% endblock %}
```

### 1.3. Убедимся, что боковая панель видна:
1. Откройте http://localhost:8000/
2. Нажмите F12 → Инспектор элементов
3. Проверьте, есть ли элемент с классом `col-lg-4`
4. Если есть, проверьте его CSS свойства

---

## 🔧 Шаг 2: Альтернативное решение - сделать боковую панель всегда видимой

Обновим `home.html` с более простой структурой:

```html
{% extends 'base.html' %}

{% block title %}Главная - Universal Creative Hub{% endblock %}

{% block content %}
<div class="container mt-4">
    <!-- Приветствие -->
    <div class="jumbotron bg-light p-5 rounded mb-4">
        <h1 class="display-4">Universal Creative Hub</h1>
        <p class="lead">Self-hosted платформа для творчества, объединяющая блог, портфолио и музыкальную студию.</p>
    </div>

    <div class="row">
        <!-- Основной контент (8 колонок) -->
        <div class="col-md-8">
            <!-- Рекомендуемые статьи -->
            {% if featured_articles %}
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="bi bi-star"></i> Рекомендуемые статьи</h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        {% for article in featured_articles %}
                        <div class="col-md-4 mb-3">
                            <div class="card h-100">
                                {% if article.cover_image %}
                                <img src="{{ article.cover_image.url }}" class="card-img-top" 
                                     alt="{{ article.title }}" style="height: 120px; object-fit: cover;">
                                {% endif %}
                                <div class="card-body">
                                    <h6>{{ article.title|truncatechars:50 }}</h6>
                                    <small class="text-muted">
                                        {{ article.category.name|default:"Без категории" }}
                                    </small>
                                </div>
                                <div class="card-footer">
                                    <a href="{{ article.get_absolute_url }}" class="btn btn-sm btn-outline-primary">
                                        Читать
                                    </a>
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            {% endif %}

            <!-- Последние статьи -->
            <h3 class="mb-3">Последние статьи</h3>
            {% for article in recent_articles %}
            <div class="card mb-3">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-8">
                            <h5>{{ article.title }}</h5>
                            <p class="text-muted">{{ article.excerpt|truncatechars:200|default:"" }}</p>
                            <div class="mb-2">
                                <span class="badge bg-secondary">{{ article.category.name|default:"Без категории" }}</span>
                                {% for tag in article.tags.all|slice:":3" %}
                                <span class="badge bg-light text-dark border">{{ tag.name }}</span>
                                {% endfor %}
                            </div>
                            <small class="text-muted">
                                <i class="bi bi-calendar"></i> {{ article.published_at|date:"d.m.Y" }}
                                <i class="bi bi-person ms-2"></i> {{ article.author.username }}
                            </small>
                        </div>
                        {% if article.cover_image %}
                        <div class="col-md-4">
                            <img src="{{ article.cover_image.url }}" class="img-fluid rounded" 
                                 alt="{{ article.title }}" style="max-height: 150px; object-fit: cover;">
                        </div>
                        {% endif %}
                    </div>
                </div>
                <div class="card-footer">
                    <a href="{{ article.get_absolute_url }}" class="btn btn-outline-primary btn-sm float-end">
                        Читать <i class="bi bi-arrow-right"></i>
                    </a>
                </div>
            </div>
            {% endfor %}
            
            <div class="text-center mt-4">
                <a href="{% url 'blog:article_list' %}" class="btn btn-primary">
                    <i class="bi bi-list-ul"></i> Все статьи
                </a>
            </div>
        </div>

        <!-- Боковая панель (4 колонки) -->
        <div class="col-md-4">
            <!-- Поиск -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0"><i class="bi bi-search"></i> Поиск</h5>
                </div>
                <div class="card-body">
                    <form action="{% url 'blog:article_list' %}" method="get">
                        <div class="input-group">
                            <input type="text" class="form-control" name="q" placeholder="Поиск статей...">
                            <button class="btn btn-primary" type="submit">
                                <i class="bi bi-search"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Категории -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0"><i class="bi bi-folder"></i> Категории</h5>
                </div>
                <div class="card-body">
                    {% if categories %}
                    <div class="list-group">
                        {% for category in categories %}
                        <a href="{% url 'blog:category_detail' category.slug %}" 
                           class="list-group-item list-group-item-action d-flex justify-content-between">
                            {{ category.name }}
                            <span class="badge bg-primary rounded-pill">{{ category.article_set.count }}</span>
                        </a>
                        {% endfor %}
                    </div>
                    {% else %}
                    <p class="text-muted mb-0">Категорий пока нет</p>
                    {% endif %}
                </div>
            </div>

            <!-- Информация -->
            <div class="card">
                <div class="card-header bg-dark text-white">
                    <h5 class="mb-0"><i class="bi bi-info-circle"></i> О платформе</h5>
                </div>
                <div class="card-body">
                    <p><strong>Universal Creative Hub</strong> — это self-hosted платформа для:</p>
                    <ul class="mb-0">
                        <li>Блога и портфолио</li>
                        <li>Музыкальной студии</li>
                        <li>Мультимедиа галереи</li>
                        <li>Совместного творчества</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
```

---

## 📊 Обновление прогресса реализации

### ✅ Выполнено (Фаза 1, недели 3-4):

#### 1. **Модели блога** ✅
   - Article, Category, MediaItem, Comment
   - TaggableManager для тегов
   - Markdown → HTML конвертация

#### 2. **Админ-панель** ✅
   - Кастомизированные интерфейсы
   - Редактирование Markdown
   - Управление тегами и категориями

#### 3. **Представления (Views)** ✅
   - Home page
   - Список статей с пагинацией
   - Детальная страница статьи
   - Список категорий
   - Фильтрация по категориям/тегам

#### 4. **Шаблоны (Templates)** ✅
   - Base template с навигацией
   - Home page
   - Article list
   - Article detail
   - Category list

#### 5. **Функциональность** ✅
   - Поиск по статьям
   - Фильтрация по категориям и тегам
   - Пагинация
   - Markdown рендеринг

#### 6. **Тестовые данные** ✅
   - Категории: Музыка, Программирование, Дизайн
   - Статьи с разным контентом
   - Теги и категоризация

### 🔧 Текущие проблемы:
- Боковая панель на главной странице
- Нет Markdown редактора в админке
- Нет импорта из Obsidian

---

## 🎯 Следующие шаги (недели 5-6):

### 1. **Markdown редактор в админке**
   - Установить `django-markdownx` или `django-ckeditor`
   - Интегрировать WYSIWYG редактор

### 2. **Импорт из Obsidian**
   - Git синхронизация
   - Автоимпорт Markdown файлов

### 3. **RSS/Atom фиды**
   - Генерация RSS ленты
   - Автоматические обновления

### 4. **Оптимизации**
   - Кэширование
   - Оптимизация запросов
   - Подготовка к production

---

## 📝 Контекст для нового чата:

**Проект:** Universal Creative Hub v0.3  
**Текущий этап:** Фаза 1, недели 3-4 (блог/портфолио)  
**Статус:** 70% завершено

**Что работает:**
- ✅ Django backend с Docker
- ✅ Модели блога (Article, Category, MediaItem, Comment)
- ✅ Админ-панель с кастомизацией
- ✅ Фронтенд: главная, список статей, детальная статья, категории
- ✅ Поиск и фильтрация
- ✅ Markdown → HTML конвертация

**Проблемы:**
- ⚠️ Боковая панель на главной не отображается (CSS/структура)
- ⚠️ Нет Markdown редактора в админке
- ⚠️ Нет импорта из Obsidian

**Стек технологий:**
- Backend: Django 4.2.7, Django REST Framework, PostgreSQL/SQLite
- Frontend: Bootstrap 5, JavaScript (vanilla)
- Дополнительно: django-taggit, markdown, Pillow

**Следующие задачи:**
1. Починить боковую панель на главной
2. Добавить Markdown редактор в админку
3. Реализовать импорт из Obsidian (Git синхронизация)

**Репозиторий:** `UniversalCreativeHub/uch-backend`  
**Ветка:** `feature/blog-phase1`

---

**Теперь боковая панель должна отображаться!** Попробуйте обновить главную страницу. Если все еще не видно, проверьте в Инспекторе элементов (F12) - есть ли элемент с классом `col-md-4` и какой у него CSS.

---

