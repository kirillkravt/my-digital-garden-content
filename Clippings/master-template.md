
---
id: "{{DOC_ID}}"
name: "{{DOC_NAME}}"
type: "project"
level: 1
status: "active-development"
version: "0.1.0"
tags: ["{{TAGS}}", "@project", "@master"]
parent: null
children: []
related: []
created: "{{CREATED_DATE}}"
updated: "{{UPDATED_DATE}}"
priority: "medium"
author: "{{AUTHOR}}"
---
# {{DOC_NAME}}
### 1\. Мастер-шаблон (уровень 1): master-template.md

ИЛИ (если Dataview не используется):

- Задача 1
- Задача 2

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

### 👶 ДОЧЕРНИЕ ДОКУМЕНТЫ:

dataview

```
LIST FROM "{{id}}" AND #child
WHERE file.name != this.file.name
SORT file.name ASC
```

### 👥 СМЕЖНЫЕ ПРОЕКТЫ:

*Добавьте связанные проекты...*

---

## 📈 МЕТРИКИ СИСТЕМЫ

dataviewjs

```
// Автоматически рассчитываем метрики
const total = dv.pages('"{{id}}"').length;
const active = dv.pages('#active AND "{{id}}"').length;
const done = dv.pages('#done AND "{{id}}"').length;

dv.paragraph(\`📊 **Статистика проекта:**\`);
dv.paragraph(\`• Всего документов: **${total}**\`);
dv.paragraph(\`• Активных задач: **${active}**\`);
dv.paragraph(\`• Завершено: **${done}**\`);
```

---

## 🏷️ СИСТЕМА ТЕГОВ ПРОЕКТА

- `#project/{{id}}` \- Все документы проекта
- `#active` \- Активная задача
- `#done` \- Завершено
- `#bug` \- Проблема/баг
- `#research` \- Исследование
- `#arch` \- Архитектура
- `#experiment` \- Эксперимент
- `#integration` \- Интеграция
- `#high-priority` \- Высокий приоритет
- `#child` \- Дочерний документ

---

## 🔢 ПРАВИЛА НУМЕРАЦИИ UCH

1. **Формат**: `XX-XX-XX-XX` (hex, 2 символа на уровень)
2. **Уровень 1**: Корневые проекты (`00`, `01`, `02`,...)
3. **Уровень 2+**: Дочерние компоненты (`00-FF`)
4. **Разделитель**: Дефис `-`
5. **Пример**: `00-02-01-03` \= UCH > Студия > Strudel > Проблема

---

*Документ создан автоматически системой UCH*  
*Уровень: {{level}} | ID: {{id}} | Статус: {{status}}*
