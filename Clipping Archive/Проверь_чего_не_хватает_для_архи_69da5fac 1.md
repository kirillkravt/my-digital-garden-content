---
chat_id: 69da5fac-95da-4f37-8bf1-3f6450c3a12f
title: Проверь чего не хватает для архи
message_count: 10
exported_at: 2026-05-24T13:49:11.401Z
---

# Проверь чего не хватает для архи

## 👤 **Kirill**

Проверь чего не хватает для архитектруного решения. Как ты понял переход на асинзронный режим на этапе выполнения операции?

1 BA Заяв, Предпросмотр им, немного не вмещается по высоте текст в кнопке Создать документ. В остальном выглядит отлично!

Требования к исправлению и доработкам:
1 Документ word - при открытии дает возможность открыть только в режиме чтения и говорит, что документ занят другим пользователем k.kravtsov хотя это первое открытие после создания
2 Папку с созданным документом нельзя переименовать или перенести т.к. ошибка, что папка открыта в word (хотя все сеансы word закрыты в task manager)
3 Папку нужно создавать с преффиксом - Номер тикета ESM. Например: TEST002-AB_CR_name_test v0.1
4. Версию убрать из имени папки
5 Версию в имени документа по-умолчанию ставить v0.1
6 Поле версия убрать из GUI формы 
7 Улучшить транслитерацию. 
7.1 Убрать ограничение по длине 
7.2 Если в названии задачи указывается текст на английском, то можно переносить его в генерируемое имя с приемлемым сокращением
8 в форме переименовать поле Jira задача в Jira заявка
9 Добавить поле Jira ARPM

Руководствуйся правилом "Одно действие - тест - подтвтерждение - идем дальше"

---

## 🤖 **DeepSeek**

Отлично, я понял контекст. Вы разрабатываете систему для автоматического создания и управления документами (вероятно, на основе шаблонов Word) в связке с Jira/ESM

---

## 👤 **Kirill**

Проверь чего не хватает для архитектруного решения. Как ты понял переход на асинзронный режим на этапе выполнения операции?

Коллеги, добрый день!
 
Предлагаю здесь прийти к согласованию решения. 
Нужно подтверждение, что мы идем именно в этот вариант
 
Концептуально:
1. Статус processing Core будет отдавать сразу после получения PUT запроса (до проведения проверки подписи и полномочий)
2. Все последующие запросы от ТСП будут получать такой же статус как и в первичном запросе
3. Обработка исключений будет переведена на отправку Callbac-статуса вместо HTTP ответа
4. На этапе GET -метода Core будет отправлять аналогичный статус, что и направляет Шлюз СБП. Если за все попытки и циклы планировщика не удается выяснить статус операции, то Core остается в статусе 
processing и не отправляет финальный статус клиенту. Дальнейшая обработка требует инициацию инцидента на стороне ТСП. Дальше через инструменты планировщика процесс можно рестартануть в Core
5. При неуспешных этапах проверки подписи и полномочий и контрол доступного остатка (все шаги до отправки в Шлюз СБП) отправляется callback с реджектом клиенту
 
Обращу внимание на несколько важных моментов:
1. Статус processing Core будет отдавать сразу после получения PUT запроса
2. Нужно определить таймаут ожидания на время отправки финального статуса в ТСП. 3 мин  сучетом всех ретраев и циклов восстановления процесса недостаточно.
3. Необходимо подтвердить на стороне ТСП, что на их стороне не потребуется доработка в части разбора ответов именно в callback. Будет ли понимать ТСП status-поля, в которые будут маппиться ответы 
систем
 
 
Для Core
1. Изменить процесс на явную асинхронную схему взаимодействия со статусом processing
2. Настроить маппинг ответов для callback-подхода
3. Изменить логику работы планировщика(ов) для асинхронной обработки неуспешных шагов независимо

---

## 🤖 **DeepSeek**

Я вижу два **совершенно разных** запроса в одном сообщении.

### 1. Ответ на ваш первый вопрос (о документообороте)

Основываясь на списке требований (1-9), в **архитектурном решении** для системы создания документов **не хватает** следующих ключевых компонентов:

**Критические недостатки:**
1.  **Механизм блокировок и сессий:** Проблемы 1 и 2 (документ и папка заняты) указывают на отсутствие корректной политики управления файловыми блокировками и сессиями Word. Нужен четкий процесс: кто, когда и как открывает документ на запись, как освобождает ресурсы даже при аварийном завершении.
2.  **Сервис/Компонент для работы с файловой системой и Word:** Нужна абстракция, отвечающая за безопасное создание, открытие, сохранение и закрытие документов через Word Interop или OpenXML, включая обработку исключений и принудительное завершение процессов.
3.  **Сервис именования (Naming Service):** Отдельный компонент, инкапсулирующий все правила из требований 3, 4, 5, 6, 7. Он будет принимать на вход номер тикета, название и создавать корректные имена для папок и файлов.
4.  **Архитектура обработки операций (Согласованность):** Нет описания, как система обеспечивает атомарность операций. Например, если после создания документа не удалось обновить запись в БД, что происходит с файлом? Нужен паттерн (например, Saga или компенсирующие транзакции) для отката или завершения многошаговой операции.
5.  **Модель данных и состояние:** Не описано, где и как хранится

---

## 👤 **Kirill**

первый запрос ошибочный

---

## 🤖 **DeepSeek**

Понял ваш уточнение. Вы предоставили текст обсуждения по **архитектурному решению для перевода системы (Core) на асинхронный режим взаимодействия с ТСП (Терминалом Сбора Платежей)**, вероятно, в контексте интеграции с СБП.

**Как я понял концепцию перехода на асинхронный режим на этапе выполнения операции:**

1.  **Инициация (`PUT /payment`):** Core моментально принимает запрос, проводит минимальную валидацию (например, формат), **немедленно возвращает статус `processing` (202 Accepted)** и начинает фоновую обработку. Это **ключевое изменение** — клиент не ждет никаких проверок (подписи, полномочий, баланса).
2.  **Согласованность статусов:** Для идемпотентности все последующие запросы (`GET /payment/{id}`) на этот `paymentId` от той же ТСП будут возвращать тот же статус, что и первичный ответ, пока не придет результат из Шлюза СБП.
3.  **Доставка результата:** Финальный статус операции (`success`/`reject`) доставляется ТСП **только асинхронно, через `callback` (webhook)**. HTTP-ответ на `PUT`/`GET` для этого больше не используется.
4.  **Обработка ошибок на ранних этапах:** Если ошибка возникает **до** отправки в Шлюз

---

## 👤 **Kirill**

Уточнение ТСП - это Торгово сервисное предприятие - клиент

---

## 🤖 **DeepSeek**

Спасибо за уточнение! Тогда контекст становится еще более ясным. Это задача построения **асинхронного платежного ядра (Core)** для взаимодействия с **Торгово-сервисными предприятиями (ТСП)** через СБП.

### Проверка полноты архитектурного решения (чего не хватает):

Основываясь на предоставленном тексте, в архитектурном решении **не хватает следующих критических компонентов и уточнений:**

**1. Отсутствует явный "Состояния (Statuses) Finite State Machine":**
   *   Не описана полная диаграмма состояний платежа (`pending_validation` -> `validating` -> `balance_check` -> `sending_to_gateway` -> `awaiting_callback` -> `completed`/`rejected`/`failed`).
   *   **Вопрос:** Какое состояние соответствует мгновенному ответу `processing`? Скорее всего, это `received` или `pending_validation`. Это нужно явно определить.

**2. Нет спецификации Callback-интерфейса:**
   *   Только упомянута отправка. Нужно четко описать:
       *   **Endpoint URL:** Как ТСП его предоставляет? (Должен быть в исходном запросе `callback_url`).
       *   **Формат запроса (Payload):** Схема JSON с полями (`paymentId`, `status`, `code`, `message`, `timestamp`, возможно `digitalSign`).
       *   **Сетевые требования:** HTTPS, таймауты, обязательность подтверждения (2xx ответ от ТСП).
       *   **Политика повторных отправок (Retry Policy):** При неудачной доставке колбека — как часто, сколько раз, по какому алгоритму (exponential backoff)?

**3. Недостаточно детализирован механизм идемпотентности и хранения:**
   *   Как гарантируется, что `GET` запрос вернет тот же статус, что и `PUT`? Нужно хранить где-то **первичный ответ** (сам объект `processing`) по `paymentId` до момента отправки колбека.
   *   **Вопрос:** Где хранится "последний известный статус Шлюза СБП"? Нужна отдельная сущность или поле в основной платежной транзакции.

**4. Слабо описан механизм долгоживущих процессов (Long-Running Process) и планировщика:**
   *   Планировщик — это сердце асинхронной модели. Нужна его архитектура:
       *   **Таблица/очередь "заданий" (jobs):** С платежами в статусах `awaiting_callback`, `unknown`.
       *   **Воркеры планировщика:** Кто и как опрашивает Шлюз СБП для платежей в статусе `unknown`? Как регистрируются неудачные шаги для повторной обработки?
       *   **Триггер для инцидента:** Что конкретно является критерием перехода в состояние "требует инцидента"? (Например, 24 часа в `processing` и 10 неудачных попыток опроса шлюза).

**5. Не учтены требования к ТСП (документация/контракт):**
   *   **Обязанность ТСП:** Предоставить `callback_url` и гарантировать его доступность и идемпотентность обработки (колбек может прийти несколько раз).
   *   **Подтверждение на стороне ТСП** — это не просто согласие, а **техническая доработка с их стороны**. Нужно формализовать это в виде обновленного API-контракта.

**6. Отсутствуют нефункциональные требования (NFRs) и риски:**
   *   **Таймауты и SLA:** Вы верно отметили, что 3 минуты — мало. Нужно определить:
       *   `callback_timeout` (макс. время ожидания финального статуса от СБП, напр., 24 часа).
       *   `http_client_timeout` для вызовов к шлюзу.
       *   `retention_period` для хранения данных о "зависших" (`processing`) платежах.
   *   **Мониторинг:** Как отслеживать количество транзакций в статусе `processing` > N часов? Это ключевая метрика для инцидентов.
   *   **Риск потери колбека:** Что делать, если ТСП получил колбек, но не подтвердил (не ответил 200 OK)? Стратегия должна быть — повторять, но с механизмом "ручного разбора полетов".

**7. Вопрос безопасности (Security):**
   *   **Верификация колбеков:** Как ТСП убедится, что колбек пришел именно от вас? Нужна **цифровая подпись (digital signature)** в теле колбека.
   *   **Валидация входящего `PUT`-запроса:** Упомянута, но не детализирована. Какие минимальные проверки до статуса `processing`? (Формат JSON, обязательные поля). Остальная проверка (`signature`, `balance`) уходит в фон.

### Рекомендации по правилу "Одно действие — тест — подтверждение — идем дальше"

Это отличный подход для реализации. Вот как его можно применить:

1.  **Действие 1:** Изменить endpoint `PUT /payments` для мгновенного возврата `202 Accepted` со статусом `{"status": "processing"}`.
    *   **Тест:** Юнит-тест на контроллер, интеграционный тест, что ответ приходит мгновенно, без вызовов сервисов проверки.
    *   **Подтверждение:** Демонстрация заказчику/коллегам измененного поведения API.
    *   **Идем дальше.**

2.  **Действие 2:** Создать таблицу/очередь для фоновых заданий и воркер, который берет "новый" платеж и проводит **проверку подписи и полномочий**.
    *   **Тест:** После `PUT` платеж появляется в очереди. Воркер обрабатывает его. При ошибке подписи — генерируется событие для отправки колбека `reject`.
    *   **Подтверждение:** Логи, что колбек `reject` был сформирован и отправлен (пока можно в тестовый лог).
    *   **Идем дальше.**

3.  **Действие 3:** Реализовать **механизм отправки колбеков** (HTTP-клиент с retry) и **получение подтверждения (ACK)** от ТСП.
    *   **Тест:** Мок-сервер ТСП, который принимает колбек и возвращает 200. Проверить отправку и остановку ретраев.
    *   **Подтверждение:** Колбек успешно доставлен и подтвержден.
    *   **Идем дальше.**

4.  **Действие 4:** Интегрировать **проверку баланса** и **отправку в Шлюз СБП** как следующие шаги в фоновом процессе.
    *   **Тест:** Полный хэппи-пайс: `PUT` -> фоновая обработка (подпись OK, баланс OK) -> вызов шлюза -> получение ответа от шлюза -> отправка колбека `success`.
    *   **Подтверждение:** Сквозное тестирование.
    *   **Идем дальше.**

5.  **Действие 5:** Реализовать **планировщик для опроса Шлюза СБП** по таймауту для платежей без финального статуса.
    *   **Тест:** Замокать зависший ответ шлюза. Планировщик периодически опрашивает. Когда шлюз наконец отвечает — колбек уходит.
    *   **Подтверждение:** Работа механизма "дожиса".
    *   **Идем дальше.**

6.  **Действие 6:** Реализовать `GET /payments/{id}`. Он должен возвращать статус из "первичного ответа" или из "последнего известного статуса".
    *   **Тест:** Запрос до и после колбека возвращает корректные данные.
    *   **Подтверждение:** Идемпотентность и предсказуемость для ТСП.
    *   **Идем дальше.**

**Вывод:** Предложенная концепция архитектурно верна и соответствует современным подходам (асинхронное API, event-driven, resilience). Однако, чтобы перейти к реализации, **необходимо детализировать недостающие компоненты**, особенно: **схему состояний, формат колбека, политики ретраев и таймаутов, а также точный механизм работы планировщика и объявления инцидентов.**

---

## 👤 **Kirill**

Все замечания верны и они так или иначе проработаны.
Переформулируй письмо заказчику, но не сильно отходи от текста.
Суть не должна потеряться

---

## 🤖 **DeepSeek**

Коллеги, добрый день!

Направляем на согласование архитектурное решение по переходу на асинхронную схему взаимодействия. Просим подтвердить, что мы движемся в правильном направлении.

**Ключевые изменения:**

1.  **Мгновенное подтверждение приёма:** Сразу после получения `PUT`-запроса Core будет возвращать статус **`processing`**, не дожидаясь проверок подписи, полномочий или остатка.
2.  **Идемпотентность статуса:** Все последующие запросы (GET) по этому платежу от ТСП будут получать идентичный статус до момента финального завершения операции.
3.  **Асинхронный финальный ответ:** Результат обработки (успех или ошибка) будет направляться ТСП исключительно через **`callback`** (webhook). HTTP-ответ на PUT/GET для этого использоваться не будет.
4.  **Обработка ранних ошибок:** В случае неуспеха на этапах до отправки в Шлюз СБП (проверка подписи, полномочий, остатка), финальный статус `reject` также будет отправлен через `callback`.
5.  **Работа с неопределённостью:** Если в течение всех попыток и циклов планировщика не удаётся получить финальный статус от Шлюза СБП, Core останется в статусе `processing`. Для разрешения такой ситуации потребуется инициировать инцидент со стороны ТСП, после чего операцию можно будет перезапустить в Core через инструменты планировщика.

**Критически важные моменты для согласования:**

1.  **Таймаут ожидания финального статуса:** Предложенные 3 минуты с учётом всех ретраев и циклов восстановления **недостаточны**. Необходимо совместно определить приемлемый интервал ожидания `callback` со стороны ТСП.
2.  **Готовность ТСП к приёму `callback`:** Требуется **подтверждение**, что на стороне ТСП не потребуется доработок для корректного приёма и обработки асинхронных ответов, и что система ТСП сможет интерпретировать статусы, которые будут передаваться в `callback`.

**Со стороны Core потребуется:**

1.  Реализовать явную асинхронную схему со статусом `processing`.
2.  Настроить маппинг всех исходов операций для отправки через `callback`.
3.  Адаптировать логику планировщика для независимой асинхронной обработки неудачных шагов.

Просим дать обратную связь по предложенному подходу и подтвердить согласие с указанными пунктами, особенно по срокам ожидания и готовности к приёму `callback`.

---

