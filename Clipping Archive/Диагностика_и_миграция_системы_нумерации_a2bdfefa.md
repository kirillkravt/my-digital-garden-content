---
chat_id: a2bdfefa-bdeb-468d-8490-5fd181792950
title: Диагностика и миграция системы нумерации
message_count: 42
exported_at: 2026-04-06T04:12:51.776Z
---

# Диагностика и миграция системы нумерации

## 👤 **Kirill**

Ранее мы разработали систему hex нумерации, но она не отвечала требованиям и потребовалась полная переработка системы. Как должно быть сейчас описано в документе 2-010400-1, а правильная типология в 3-010402-1. 
Сейчас от тебя нужно правильно составить скоуп задач (нумерацию соблюсти по системе uch-docs) на доработку системы.

Ключевые моменты: 
необходимо выяснить полное состояние системы сейчас 
определить пул задач на изменение (важно исправить текущую систему на новую перед созданием плагина и дальнейшего развития системы)

В ответах руководствуйся системным промптом 0-000000-1

---

## 🤖 **DeepSeek**

Отлично, я вижу ситуацию. У нас есть текущая система, разработанный гибридный подход, но при этом в документации **система нумерации (2-010400-1)** и **типология (3-010402-1)** описывают **новую, целевую систему**, которая не соответствует текущей реализации. Сейчас нужно привести систему в соответствие с документацией перед созданием плагина.

Давайте действовать пошагово, как указано в системном промпте.

## 📊 Шаг 1: Диагностика текущего состояния

Сначала нужно выяснить, какая система нумерации и типов используется **сейчас на практике** в файлах.

**Команда для анализа:**

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
find . -name "*.md" -type f | head -20 | xargs grep -l "id:" | head -10 | xargs -I {} sh -c 'echo "=== {} ===" && head -20 {}'
```

**Ожидаемый результат:**
Мы увидим реальные `id`, `type`, `level` в frontmatter существующих файлов и сравним их с целевой моделью из `3-010402-1`.

**Что ищем:**
1. Формат ID (`X-XXXXXX-X` или `X-XXXXXX-XXXX-X` или другой)
2. Типы документов (`PROD`, `LINE`, `COMP`, `TASK`, `ARCH`, `DOC`, etc.)
3. Уровни (`level: 1`, `level: 2`, etc.)

## 📋 Шаг 2: Определение разрывов и создание скоупа задач

На основе диагностики и целевого состояния из `3-010402-1` сформируем задачи. Предположу основные проблемы, но уточним после Шага 1.

**Скоуп задач (предварительный):**

### **Уровень 4: Задачи миграции системы нумерации (родитель: линия документации `2-010400-1`)**

1.  **`4-010400-0100-1_TASK_Аудит_текущего_состояния_нумерации.md`**
    *   **Тип:** `TASK`
    *   **Цель:** Получить полную картину используемых ID, типов, уровней и их соответствия целевой модели `3-010402-1`.
    *   **Выход:** Отчет со списком файлов, их текущими и целевыми параметрами.

2.  **`4-010400-0200-1_TASK_Разработка_скрипта_миграции_параметров.md`**
    *   **Тип:** `TASK`
    *   **Цель:** Создать инструмент для автоматического приведения frontmatter (`id`, `type`, `level`, `slug`) в соответствие с новой системой.
    *   **Выход:** Скрипт `migrate-naming-v2.sh`, который читает файл и обновляет его метаданные по правилам из `3-010402-1`.

3.  **`4-010400-0300-1_TASK_Массовая_миграция_существующих_документов.md`**
    *   **Тип:** `TASK`
    *   **Цель:** Применить скрипт миграции ко всем документам в `uch-docs`, сохранив логи и создав backup.
    *   **Выход:** Все файлы имеют корректные `id`, `type`, `level`, `slug`. Создан отчет о миграции.

4.  **`4-010400-0400-1_TASK_Обновление_скриптов_создания_документов.md`**
    *   **Тип:** `TASK`
    *   **Цель:** Модифицировать `uch-create.sh` и другие скрипты для генерации имен файлов и frontmatter **только** по новой системе (`X-XXXXXX-X_TYPE_slug.md`).
    *   **Выход:** Скрипты `uch-create-v2.sh`, `uch-create-modular.sh` создают файлы в полном соответствии с `3-010402-1`.

5.  **`4-010400-0500-1_TASK_Верификация_целостности_после_миграции.md`**
    *   **Тип:** `TASK`
    *   **Цель:** Проверить отсутствие конфликтов ID, корректность связей родитель-потомок, работоспособность всех инструментов (`uch-toolkit.sh`).
    *   **Выход:** Отчет о целостности. Система готова к переходу на этап разработки плагина.

**Следующий шаг:** Выполните **Шаг 1 (Диагностика)**, чтобы подтвердить или скорректировать этот список задач. Это даст точные данные для работы.

Выполните команду анализа, и я помогу интерпретировать результат и скорректировать скоуп.

---

## 👤 **Kirill**

kirillkravcov@MacBook-Air uch-docs % cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
find . -name "*.md" -type f | head -20 | xargs grep -l "id:" | head -10 | xargs -I {} sh -c 'echo "=== {} ===" && head -20 {}'
grep: ./frontmatter-fixed-backup-20260104-212703/01-01: No such file or directory
grep: ТЕКУЩИЙ: No such file or directory
grep: ПРОГРЕСС: No such file or directory
grep: UCH: No such file or directory
grep: (standard input): Bad file descriptor
grep: ЛИНИЯ: No such file or directory
grep: А: No such file or directory
grep: (БЛОГ).md: No such file or directory
grep: ./frontmatter-fixed-backup-20260104-212703/01-02: No such file or directory
grep: (standard input): Bad file descriptor
grep: ТЕКУЩИЙ: No such file or directory
grep: ПРОГРЕСС: No such file or directory
grep: UCH: No such file or directory
grep: (standard input): Bad file descriptor
grep: ЛИНИЯ: No such file or directory
grep: Б: No such file or directory
grep: (СТУДИЯ).md: No such file or directory
grep: ./Z-20260112135949-1_IDEA_Расширенная: No such file or directory
grep: типология: No such file or directory
grep: документации.md: No such file or directory
grep: ./archive/uch-old_docs/4-125: No such file or directory
grep: Прогресс: No such file or directory
grep: реализации: No such file or directory
grep: Фаза: No such file or directory
grep: 2.md: No such file or directory
=== ./2-010400-7_ARCH_Documentation-first_подход.md ===
---
id: "2-010400-7"
name: "Documentation-first подход"
type: "ARCH"
level: 2
status: "active"
tags: []
created: "2025-12-27"
updated: "2026-01-28"
author: "kirillkravcov"
slug: "2-010400-7_ARCH_Documentation-first_подход"
---
### Documentation-first подход

#### ОПИСАНИЕ

## 🎯 ПОЧЕМУ "DOCUMENTATION-FIRST" — ИДЕАЛЬНО ДЛЯ UCH (И ДЛЯ ИНДИ)

### 1\. Система создает сама себя

=== ./4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md ===
---
id: 4-010400-1400
name: Указание тегов при создании документов
type: TASK
level: 4
status: active
tags:
created: 2025-12-26
updated: 2026-01-28
author: kirillkravcov
slug: 4-010400-1400-1_TASK_Указание_тегов_при_создании_документов
---
### Задача. Исправить баг указания тегов при создании документов

#### ОПИСАНИЕ
Добавьте описание здесь.

#### ЗАДАЧИ
- [ ] Задача 1
- [ ] Задача 2
=== ./6-010200-001-1_SNAP_20251221.md ===
---
id: "6-010200-001"
name: "20251221"
type: "SNAP"
level: 6
status: "active"
tags: []
created: "2026-01-02"
updated: "2026-01-28"
author: "kirillkravcov"
slug: "6-010200-001-1_SNAP_20251221"
---

## 🗺️ UNIVERSAL CREATIVE HUB - СНАПШОТ СОСТОЯНИЯ: ЛИНИЯ Б (СТУДИЯ)

## 📊 СНИМОК СОСТОЯНИЯ

- **Версия**: Phase2-Day4 (рабочая версия)
- **Дата**: 2025-12-21
- **Коммит**: `e03943c` 🚀 НАЧАЛЬНАЯ ТОЧКА: Рабочая версия Phase2-Day4 с Strudel run()
=== ./6-010400-007-1_SNAP_20251228.md ===
---
id: "6-010400-007"
name: "20251228"
type: "SNAP"
level: 6
status: "planning"
tags: []
created: "2025-12-29"
updated: "2026-01-28"
author: "kirillkravcov"
slug: "6-010400-007-1_SNAP_20251228"
---

### Снапшот системы после исправления багов 20251228

#### ОПИСАНИЕ
### Снапшот системы после исправления тегов 20251228

#### 📊 СТАТУС СИСТЕМЫ (2025-12-28)

xargs: command line cannot be assembled, too long
kirillkravcov@MacBook-Air uch-docs % 

сейчас все документы уже переведены в новый формат, частично временным скриптом, частично вручную.
Нужно сделать изменения и в самой "автоматической" системе

---

## 🤖 **DeepSeek**

Отлично, диагностика показывает, что **основные документы уже в новом формате**, но есть ключевые проблемы с системой генерации. 

Видно, что:
1. **ID формат правильный**: `2-010400-7`, `4-010400-1400`, `6-010200-001`
2. **Типы правильные**: `ARCH`, `TASK`, `SNAP`
3. **Уровни правильные**: `level: 2`, `level: 4`, `level: 6`

**Но есть проблемы в именах файлов:**
- `2-010400-7_ARCH_Documentation-first_подход.md` - ✅ Правильно
- `4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md` - ❌ **Неправильно!** Лишний `-1` после `1400`
- `6-010200-001-1_SNAP_20251221.md` - ❌ **Неправильно!** Лишний `-1` после `001`

Согласно системе `3-010402-1`, формат должен быть:
- Уровни 1-3: `X-XXXXXX-X_TYPE_slug.md`
- Уровни 4-5: `X-XXXXXX-XXXX-X_TYPE_slug.md`
- Уровень 6: `X-XXXXXX-XXXX-X_TYPE_YYYYMMDD.md`

## 📋 Актуализированный скоуп задач на исправление системы

### **Уровень 4: Задачи исправления генерации (родитель: `2-010400-1`)**

1. **`4-010400-0600-1_TASK_Аудит_несоответствий_имен_файлов.md`**
   - **Цель:** Найти все файлы с неправильным форматом имени (лишние `-1`, неправильные паттерны).
   - **Выход:** Список файлов для исправления.

2. **`4-010400-0601-1_SOL_Алгоритм_корректного_формирования_имени.md`**
   - **Цель:** Разработать логику формирования имени файла на основе `id`, `type`, `slug`/`date` согласно `3-010402-1`.
   - **Выход:** Четкий алгоритм в виде псевдокода/правил.

3. **`4-010400-0602-1_TASK_Исправление_скрипта_uch-create.sh.md`**
   - **Цель:** Переписать `uch-create.sh` для генерации **только** правильных имен файлов и frontmatter.
   - **Ключевые изменения:**
     - Убрать автоматическое добавление `-1` к `id` в имени файла для уровней 4-6
     - Правильно определять формат имени по `level`
     - Генерировать `slug` из `name` автоматически

4. **`4-010400-0603-1_TASK_Создание_скрипта_исправления_имен_файлов.md`**
   - **Цель:** Создать безопасный скрипт для переименования существующих файлов с неправильными именами.
   - **Выход:** Скрипт `fix-filenames-v2.sh` с backup и логом.

5. **`4-010400-0604-1_TASK_Обновление_всех_вспомогательных_скриптов.md`**
   - **Цель:** Проверить и обновить все скрипты в `uch-scripts/` (анализаторы, репортеры) на работу с новым форматом.
   - **Выход:** Все инструменты в `uch-toolkit.sh` работают корректно.

## 🚀 Шаг 1: Запуск аудита несоответствий

Выполните эту команду для поиска всех файлов с потенциально неправильными именами:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
find . -name "*.md" -type f | grep -E '.*-[0-9]{3,4}-[0-9]+_.*\.md$' | head -20
```

**Что ищем:** Файлы, где после основной части ID стоит еще `-число` перед `_TYPE_`.

**Ожидаемый результат:** Список файлов вроде `4-010400-1400-1_TASK_...md`, которые нужно исправить в `4-010400-1400_TASK_...md`.

После этого можно будет:
1. Создать задачу `4-010400-0600-1_TASK_Аудит_несоответствий_имен_файлов.md` с результатами
2. Начать исправление скрипта генерации

**Выполните команду аудита, и я помогу проанализировать масштаб проблемы.**

---

## 👤 **Kirill**

Подожди, а вчем здесь не правильность?

Но есть проблемы в именах файлов:

2-010400-7_ARCH_Documentation-first_подход.md - ✅ Правильно

4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md - ❌ Неправильно! Лишний -1 после 1400

6-010200-001-1_SNAP_20251221.md - ❌ Неправильно! Лишний -1 после 001

Согласно системе 3-010402-1, формат должен быть:

Уровни 1-3: X-XXXXXX-X_TYPE_slug.md

Уровни 4-5: X-XXXXXX-XXXX-X_TYPE_slug.md

Уровень 6: X-XXXXXX-XXXX-X_TYPE_YYYYMMDD.md


4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md = Уровни 4-5: X-XXXXXX-XXXX-X_TYPE_slug.md

6-010200-001-1_SNAP_20251221.md = Уровень 6: X-XXXXXX-XXXX-X_TYPE_YYYYMMDD.md

---

## 🤖 **DeepSeek**

Вы абсолютно правы! Я совершил ошибку в анализе. Давайте внимательно перечитаем спецификацию из `3-010402-1`:

## 📖 **Правильный анализ форматов:**

Из документа `3-010402-1`:
- **Уровни 4-5:** `X-XXXXXX-XXXX-X_TYPE_slug.md`
- **Уровень 6:** `X-XXXXXX-XXXX-X_TYPE_YYYYMMDD.md`

Где `XXXX` - это **номер задачи/решения**, и последний `X` - это **номер типа** (1-6 для уровневых типов, 7-99 для общих).

**Значит:**
1. `4-010400-1400-1_TASK_...md` - ✅ **Правильно!**
   - `4` = уровень
   - `010400` = иерархия (линия документации)
   - `1400` = номер задачи
   - `1` = номер типа (TASK - тип 1 на уровне 4)
   - `TASK` = тип документа
   - Имя файла: ✅

2. `6-010200-001-1_SNAP_...md` - ✅ **Правильно!**
   - `6` = уровень
   - `010200` = иерархия (предположительно линия Б)
   - `001` = номер отчета
   - `1` = номер типа (REPORT - тип 1 на уровне 6, хотя здесь SNAP - нужно проверить соответствие)
   - `SNAP` = тип документа
   - Имя файла: ✅

3. `2-010400-7_ARCH_...md` - ✅ **Правильно!**
   - `2` = уровень  
   - `010400` = иерархия
   - `7` = номер типа (ARCH - общий тип 7)
   - `ARCH` = тип документа
   - Имя файла: ✅

## 🎯 **Переопределим проблему:**

Проблема **не в формате имен файлов**, а в **соответствии между `id` в frontmatter и именем файла**:

1. В `4-010400-1400-1_TASK_...md`:
   - **frontmatter:** `id: 4-010400-1400` ❌ **Неполный!**
   - **должно быть:** `id: 4-010400-1400-1` ✅

2. В `6-010200-001-1_SNAP_...md`:
   - **frontmatter:** `id: "6-010200-001"` ❌ **Неполный!**
   - **должно быть:** `id: "6-010200-001-1"` ✅

**Правило:** `id` в frontmatter должен **полностью совпадать** с частью имени файла до первого `_`.

## 🔧 **Исправленный скоуп задач:**

### **Уровень 4: Задачи синхронизации id (родитель: `2-010400-1`)**

1. **`4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md`**
   - **Цель:** Найти все файлы, где `id` в frontmatter не совпадает с частью имени файла до `_`.
   - **Выход:** Список файлов для исправления.

2. **`4-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md`**
   - **Цель:** Разработать скрипт, который выравнивает `id` в frontmatter с именем файла.
   - **Выход:** Скрипт `fix-id-consistency.sh`.

3. **`4-010400-0702-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md`**
   - **Цель:** Убедиться, что `uch-create.sh` генерирует полные `id` (включая номер типа) в frontmatter.
   - **Выход:** Обновленный `uch-create-v2.sh`.

## 🚀 **Шаг 1: Запуск аудита несоответствий id**

Выполните эту команду для проверки несоответствий:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
find . -name "*.md" -type f -exec grep -l "id:" {} \; | head -10 | while read file; do
    filename=$(basename "$file")
    id_part=$(echo "$filename" | cut -d'_' -f1)
    frontmatter_id=$(grep -m1 "^id:" "$file" | sed 's/^id: *//' | tr -d '" ' | tr -d "'")
    
    if [ "$id_part" != "$frontmatter_id" ]; then
        echo "❌ НЕСООТВЕТСТВИЕ: $filename"
        echo "   ID в имени: $id_part"
        echo "   ID в frontmatter: $frontmatter_id"
        echo "---"
    fi
done
```

**Ожидаемый результат:** Мы увидим файлы типа `4-010400-1400-1_TASK_...md`, где frontmatter содержит `id: 4-010400-1400` вместо `id: 4-010400-1400-1`.

**Выполните команду аудита - это даст точную картину проблемы.**

---

## 👤 **Kirill**

смотри, чтобы мы дальше правильно пошли давай взглянем на ошибку в твоем описании:

📖 Правильный анализ форматов:
Из документа 3-010402-1:

Уровни 4-5: X-XXXXXX-XXXX-X_TYPE_slug.md

Уровень 6: X-XXXXXX-XXXX-X_TYPE_YYYYMMDD.md

Где XXXX - это номер задачи/решения, и последний X - это номер типа (1-6 для уровневых типов, 7-99 для общих).

Значит:

4-010400-1400-1_TASK_...md - ✅ Правильно!

4 = уровень

010400 = иерархия (линия документации)

1400 = номер задачи

1 = номер типа (TASK - тип 1 на уровне 4)

TASK = тип документа

Имя файла: ✅

6-010200-001-1_SNAP_...md - ✅ Правильно!

6 = уровень

010200 = иерархия (предположительно линия Б)

001 = номер отчета

1 = номер типа (REPORT - тип 1 на уровне 6, хотя здесь SNAP - нужно проверить соответствие)

SNAP = тип документа

Имя файла: ✅

2-010400-7_ARCH_...md - ✅ Правильно!

2 = уровень

010400 = иерархия

7 = номер типа (ARCH - общий тип 7)

ARCH = тип документа

Имя файла: ✅

здесь все отлично определил!

Но в постановке задач допущена ошибка:

Уровень 4: Задачи синхронизации id (родитель: 2-010400-1)
4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md

4-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md - -неправильно.
Должно быть:
5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md
т.к. это уровень решений и это решение к задаче 0700

4-010400-0702-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md -неправильно. 
Должно быть:
4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md 

т.к. 0800 это следующий номер задачи

---

## 🤖 **DeepSeek**

Вы абсолютно правы! Отлично, что вы внимательно следите за нумерацией. Это критически важно для системы. Давайте исправим мой ошибки в соответствии с системой:

## 🎯 **Правильная иерархия задач и решений:**

Согласно системе `3-010402-1`:
- **Уровень 4:** Задачи (`TASK`, `FEAT`, `BUG`, etc.) - номера `0100`, `0200`, `0300`, etc.
- **Уровень 5:** Решения (`SOL`, `CODE`, etc.) - номера `0101`, `0102`, `0201`, etc. (первые 2 цифры = задача, последние 2 = решение)

## 🔧 **Исправленный скоуп задач с правильной нумерацией:**

### **Уровень 4: Задачи исправления системы (родитель: `2-010400-1`)**

1. **`4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md`** ✅
   - **Цель:** Найти все файлы, где `id` в frontmatter не совпадает с частью имени файла до `_`.
   - **Выход:** Список файлов для исправления.

2. **`4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md`** ✅
   - **Цель:** Убедиться, что `uch-create.sh` генерирует полные `id` (включая номер типа) в frontmatter.
   - **Выход:** Обновленный `uch-create-v2.sh`.

### **Уровень 5: Решения для задачи 0700**

1. **`5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md`** ✅
   - **Цель:** Разработать скрипт, который выравнивает `id` в frontmatter с именем файла.
   - **Выход:** Скрипт `fix-id-consistency.sh`.

2. **`5-010400-0702-1_CODE_Реализация_скрипта_корректировки_id.md`** (опционально)
   - **Цель:** Реализовать и протестировать скрипт из решения 0701.
   - **Выход:** Рабочий скрипт с тестами.

## 🚀 **Шаг 1: Создание первой задачи аудита**

Давайте начнем с создания задачи `4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md`:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md << "EOF"
---
id: "4-010400-0700-1"
name: "Аудит несоответствий id и имени файла"
type: "TASK"
level: 4
status: "active"
tags: ["@docs", "@system", "@audit"]
parent: "2-010400"
parent_name: "docs system"
created: "2026-02-04"
updated: "2026-02-04"
author: "kirillkravcov"
slug: "4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла"
---

### Задача: Аудит несоответствий между id в frontmatter и именем файла

#### ОПИСАНИЕ
В процессе миграции на новую систему нумерации обнаружены расхождения между:
1. `id` указанным в frontmatter документа
2. Частью имени файла до первого `_`

**Пример несоответствия:**
- Имя файла: `4-010400-1400-1_TASK_...md`
- Frontmatter: `id: 4-010400-1400` ❌ (не хватает `-1`)

**Цель задачи:** Выявить все подобные несоответствия в системе uch-docs.

#### КРИТЕРИИ ВЫПОЛНЕНИЯ
- [ ] Создан скрипт для поиска несоответствий
- [ ] Проведен аудит всех .md файлов в uch-docs
- [ ] Сформирован отчет с списком файлов для исправления
- [ ] Определен масштаб проблемы (количество файлов)

#### ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ
1. Скрипт должен:
   - Находить все .md файлы с frontmatter
   - Извлекать `id` из frontmatter
   - Извлекать первую часть имени файла (до `_`)
   - Сравнивать их
   - Выводить список несоответствий

2. Отчет должен содержать:
   - Общее количество проверенных файлов
   - Количество файлов с несоответствиями
   - Список файлов с указанием:
     - Текущий id в frontmatter
     - Ожидаемый id (из имени файла)
     - Путь к файлу

#### СЛЕДУЮЩИЕ ШАГИ
После выполнения этой задачи:
1. Создать решение `5-010400-0701-1_SOL_Алгоритм_корректировки_id.md`
2. Создать задачу `4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh.md`

#### КОМАНДА ДЛЯ АУДИТА
```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
find . -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
    filename=$(basename "$file")
    id_part=$(echo "$filename" | cut -d"_" -f1)
    frontmatter_id=$(grep -m1 "^id:" "$file" | sed "s/^id: *//" | tr -d "\"'" | tr -d " " | tr -d ":")
    
    if [ "$id_part" != "$frontmatter_id" ]; then
        echo "❌ НЕСООТВЕТСТВИЕ: $filename"
        echo "   ID в имени:    $id_part"
        echo "   ID в frontmatter: $frontmatter_id"
        echo "   Путь: $file"
        echo "---"
    fi
done
```

#### ОЖИДАЕМЫЙ РЕЗУЛЬТАТ
Пример вывода:
```
❌ НЕСООТВЕТСТВИЕ: 4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md
   ID в имени:    4-010400-1400-1
   ID в frontmatter: 4-010400-1400
   Путь: ./4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md
---
```

EOF'
```

**Выполните создание задачи этой командой, затем запустите аудит (команда в документе), чтобы получить реальные данные о проблеме.**

---

## 👤 **Kirill**

подожди. Давай дальше не идти пока не разберемся в ошибках нумерации.
Ты пишешь 
Уровень 5: Решения для задачи 0700
5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md ✅

Цель: Разработать скрипт, который выравнивает id в frontmatter с именем файла.

Выход: Скрипт fix-id-consistency.sh.

5-010400-0702-1_CODE_Реализация_скрипта_корректировки_id.md (опционально)

Цель: Реализовать и протестировать скрипт из решения 0701.

Выход: Рабочий скрипт с тестами.

Все бы ничего, но порядковый номер типа документа CODE - "2" к задаче 0700 (у пятого уровня родитель 4-й уровень, а не SOL 5-го уровня), а значит должно быть
5-010400-0701-2_CODE_Реализация_скрипта_корректировки_id.md (опционально)

---

## 🤖 **DeepSeek**

Вы абсолютно правы! Еще одна ошибка с моей стороны. Спасибо за внимательность!

Давайте разберемся правильно с нумерацией согласно системе `3-010402-1`:

## 📚 **Правила нумерации из документа `3-010402-1`:**

1. **Уровень 5: РЕШЕНИЯ** (пятый уровень в системе)
   - Типы: `SOL` (тип 1), `CODE` (тип 2), `BUG` (тип 3), `ALG` (тип 4), `CONF` (тип 5), `SCRIPT` (тип 6)

2. **Порядковый номер типа:** Уникален в рамках:
   - Уровня + Иерархии + Типа документа

## ✅ **Правильная нумерация для задачи `0700`:**

Для задачи `4-010400-0700-1_TASK_...` (родитель) создаются решения на **уровне 5**:

1. **`5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md`**
   - `5` = уровень 5 (решения)
   - `010400` = иерархия (линия документации)
   - `0701` = номер решения (07 = задача, 01 = первое решение)
   - `1` = номер типа (`SOL` - тип 1 на уровне 5)

2. **`5-010400-0701-2_CODE_Реализация_скрипта_корректировки_id.md`** ✅
   - `5` = уровень 5 (решения)
   - `010400` = иерархия
   - `0701` = номер решения (то же решение, что и выше)
   - `2` = номер типа (`CODE` - тип 2 на уровне 5)

## 🎯 **Уточненный скоуп с правильной нумерацией:**

### **Уровень 4: Задачи исправления системы**

1. **`4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md`**
2. **`4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md`**

### **Уровень 5: Решения для задачи 0700**

1. **`5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md`**
2. **`5-010400-0701-2_CODE_Реализация_скрипта_корректировки_id.md`**

**Вопрос:** Нужно ли нам отдельное `CODE` решение, или достаточно `SOL` с приложенным кодом? 

Согласно системе:
- `SOL` = техническое решение (описание алгоритма, логики)
- `CODE` = код, реализация (файлы скриптов, исходный код)

Если мы планируем создать отдельный файл со скриптом, то `CODE` уместен. Если скрипт будет частью `SOL`, то только `SOL`.

**Что предпочтительнее для этой задачи?**

---

## 👤 **Kirill**

теперь правильно! Думаю, что достаточно будет SOL

---

## 🤖 **DeepSeek**

Отлично! Тогда упрощаем скоуп:

## 🎯 **Финальный скоуп задач с правильной нумерацией:**

### **Уровень 4: Задачи исправления системы (родитель: `2-010400-1`)**

1. **`4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md`**
   - **Цель:** Найти все файлы, где `id` в frontmatter не совпадает с частью имени файла до `_`.
   - **Выход:** Список файлов для исправления.

2. **`4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md`**
   - **Цель:** Убедиться, что `uch-create.sh` генерирует полные `id` (включая номер типа) в frontmatter.
   - **Выход:** Обновленный `uch-create-v2.sh`.

### **Уровень 5: Решение для задачи 0700**

1. **`5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md`**
   - **Цель:** Разработать скрипт, который выравнивает `id` в frontmatter с именем файла.
   - **Выход:** Алгоритм и скрипт `fix-id-consistency.sh`.

## 🚀 **Шаг 1: Создаем задачу аудита**

Выполните команду для создания первой задачи:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md << "EOF"
---
id: "4-010400-0700-1"
name: "Аудит несоответствий id и имени файла"
type: "TASK"
level: 4
status: "active"
tags: ["@docs", "@system", "@audit"]
parent: "2-010400"
parent_name: "docs system"
created: "2026-02-04"
updated: "2026-02-04"
author: "kirillkravcov"
slug: "4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла"
---

### Задача: Аудит несоответствий между id в frontmatter и именем файла

#### ОПИСАНИЕ
В процессе миграции на новую систему нумерации обнаружены расхождения между:
1. `id` указанным в frontmatter документа
2. Частью имени файла до первого `_`

**Пример несоответствия:**
- Имя файла: `4-010400-1400-1_TASK_...md`
- Frontmatter: `id: 4-010400-1400` ❌ (не хватает `-1`)

**Цель задачи:** Выявить все подобные несоответствия в системе uch-docs.

#### КРИТЕРИИ ВЫПОЛНЕНИЯ
- [ ] Создан скрипт для поиска несоответствий
- [ ] Проведен аудит всех .md файлов в uch-docs
- [ ] Сформирован отчет с списком файлов для исправления
- [ ] Определен масштаб проблемы (количество файлов)

#### ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ
1. Скрипт должен:
   - Находить все .md файлы с frontmatter
   - Извлекать `id` из frontmatter
   - Извлекать первую часть имени файла (до `_`)
   - Сравнивать их
   - Выводить список несоответствий

2. Отчет должен содержать:
   - Общее количество проверенных файлов
   - Количество файлов с несоответствиями
   - Список файлов с указанием:
     - Текущий id в frontmatter
     - Ожидаемый id (из имени файла)
     - Путь к файлу

#### СЛЕДУЮЩИЕ ШАГИ
После выполнения этой задачи:
1. Создать решение `5-010400-0701-1_SOL_Алгоритм_корректировки_id.md`
2. Создать задачу `4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh.md`

EOF'
```

## 🔍 **Шаг 2: Запускаем аудит**

После создания задачи выполните команду аудита, чтобы получить реальные данные:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 Начинаем аудит несоответствий ID..."
echo "======================================"

total_files=0
mismatch_files=0

find . -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
    total_files=$((total_files + 1))
    filename=$(basename "$file")
    
    # Извлекаем ID из имени файла (часть до первого _)
    id_part=$(echo "$filename" | cut -d"_" -f1)
    
    # Извлекаем ID из frontmatter (убираем кавычки, пробелы, двоеточия)
    frontmatter_id=$(grep -m1 "^id:" "$file" | sed "s/^id: *//" | tr -d "\"'" | tr -d " " | tr -d ":")
    
    if [ "$id_part" != "$frontmatter_id" ]; then
        mismatch_files=$((mismatch_files + 1))
        echo "❌ НЕСООТВЕТСТВИЕ #$mismatch_files:"
        echo "   Файл: $filename"
        echo "   ID в имени:    '$id_part'"
        echo "   ID в frontmatter: '$frontmatter_id'"
        echo "   Путь: $file"
        echo "---"
    fi
done

echo "======================================"
echo "📊 ИТОГИ АУДИТА:"
echo "   Всего проверено файлов: $total_files"
echo "   Файлов с несоответствиями: $mismatch_files"
echo "   Процент несоответствий: $(echo "scale=2; $mismatch_files * 100 / $total_files" | bc)%"
```

**Выполните обе команды:** сначала создание задачи, затем аудит. Результаты аудита покажут реальный масштаб проблемы и помогут правильно спланировать дальнейшие действия.

---

## 👤 **Kirill**

несоответствий много, небольшую часть сюда направлю

❌ НЕСООТВЕТСТВИЕ #119:
   Файл: 5-010400-0001-4_TEST_Тестирование_функционала_uch-docs.md
   ID в имени:    '5-010400-0001-4'
   ID в frontmatter: '5-010400-0001'
   Путь: ./5-010400-0001-4_TEST_Тестирование_функционала_uch-docs.md
---
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
❌ НЕСООТВЕТСТВИЕ #120:
   Файл: 6-010400-005-1_SNAP_20251228.md
   ID в имени:    '6-010400-005-1'
   ID в frontmatter: '6-010400-005'
   Путь: ./6-010400-005-1_SNAP_20251228.md
---
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
❌ НЕСООТВЕТСТВИЕ #121:
   Файл: 6-010400-006-1_SNAP_20251228.md
   ID в имени:    '6-010400-006-1'
   ID в frontmatter: '6-010400-006'
   Путь: ./6-010400-006-1_SNAP_20251228.md
---
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
❌ НЕСООТВЕТСТВИЕ #122:
   Файл: 4-010400-1000-1_TASK_Добавить_справочник_типов_документов_в_исполняемый_скрипт.md
   ID в имени:    '4-010400-1000-1'
   ID в frontmatter: '4-010400-1000'
   Путь: ./4-010400-1000-1_TASK_Добавить_справочник_типов_документов_в_исполняемый_скрипт.md
---
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
❌ НЕСООТВЕТСТВИЕ #123:
   Файл: 4-010400-1500-1_TASK_Создание_документа_с_указанием_полного_имени.md
   ID в имени:    '4-010400-1500-1'
   ID в frontmatter: '4-010400-1500'
   Путь: ./4-010400-1500-1_TASK_Создание_документа_с_указанием_полного_имени.md
---
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
❌ НЕСООТВЕТСТВИЕ #124:
   Файл: Z-20251227114234-1_IDEA_UCH_Music_concept.md
   ID в имени:    'Z-20251227114234-1'
   ID в frontmatter: 'Z-20251227114234'
   Путь: ./Z-20251227114234-1_IDEA_UCH_Music_concept.md
---
zsh: unknown file attribute: ^
zsh: unknown file attribute: ^
❌ НЕСООТВЕТСТВИЕ #125:
   Файл: 6-010000-002-1_SNAP_20251225.md
   ID в имени:    '6-010000-002-1'
   ID в frontmatter: '6-010000-002'
   Путь: ./6-010000-002-1_SNAP_20251225.md
---
======================================
📊 ИТОГИ АУДИТА:
   Всего проверено файлов: 154
   Файлов с несоответствиями: 125
   Процент несоответствий: 81.16%
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Превосходно! Аудит выявил **серьезную системную проблему**: **81% файлов имеют несоответствие ID**! Это подтверждает, что наша система генерации работает неправильно.

## 📊 **Анализ результатов:**

1. **Проблема системная**: ID в frontmatter не включает номер типа (`-1`, `-4`, `-7` и т.д.)
2. **Шаблон очевиден**: Все несоответствия одного типа: `ID-в-имени: X-XXXXXX-XXXX-X` vs `ID-в-frontmatter: X-XXXXXX-XXXX`
3. **Уровень Z тоже затронут**: Даже Zettel-документы имеют ту же проблему

## 🎯 **Создаем решение для задачи 0700**

Теперь создадим решение `5-010400-0701-1_SOL_Алгоритм_корректировки_id.md`:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md << "EOF"
---
id: "5-010400-0701-1"
name: "Алгоритм автоматической корректировки id"
type: "SOL"
level: 5
status: "active"
tags: ["@docs", "@system", "@fix", "@automation"]
parent: "4-010400-0700"
parent_name: "Аудит несоответствий id и имени файла"
created: "2026-02-04"
updated: "2026-02-04"
author: "kirillkravcov"
slug: "5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id"
---

### Решение: Алгоритм автоматической корректировки ID в frontmatter

#### ПРОБЛЕМА
Аудит выявил 125 файлов (81%) с несоответствием между ID в имени файла и ID в frontmatter.

**Шаблон несоответствия:**
- Имя файла: `X-XXXXXX-XXXX-X_TYPE_slug.md` (полный ID)
- Frontmatter: `id: X-XXXXXX-XXXX` (не хватает номера типа)

#### АНАЛИЗ
1. **Причина**: Скрипты создания документов не включают номер типа в `id` frontmatter
2. **Масштаб**: 125 из 154 файлов требуют исправления
3. **Риски**: 
   - Нарушение целостности системы
   - Проблемы с поиском и ссылками
   - Некорректная работа плагинов

#### АЛГОРИТМ РЕШЕНИЯ

**Шаг 1: Извлечение данных**
```bash
# Для каждого .md файла:
# 1. Получить имя файла
# 2. Извлечь полный ID (часть до первого _)
# 3. Прочитать текущий ID из frontmatter
```

**Шаг 2: Сравнение и определение действия**
```bash
if [ "$full_id" != "$current_id" ]; then
    # Требуется исправление
    # Проверка: full_id должен начинаться с current_id + "-N"
    if [[ "$full_id" =~ ^${current_id}-[0-9]+$ ]]; then
        # Корректное исправление - заменить ID в frontmatter
    else
        # Ошибка - логировать для ручной проверки
    fi
fi
```

**Шаг 3: Безопасное обновление**
```bash
# 1. Создать backup файла
# 2. Заменить строку с id в frontmatter
# 3. Сохранить с тем же именем
# 4. Записать в лог изменения
```

#### ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

**Скрипт: `fix-id-consistency.sh`**

```bash
#!/bin/bash
# fix-id-consistency.sh - Исправление несоответствий ID в frontmatter

set -e

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
BACKUP_DIR="./backup-id-fix-$(date +%Y%m%d_%H%M%S)"
LOG_FILE="./id-fix-log-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    grep -m1 "^id:" "$1" | sed "s/^id: *//" | tr -d "\"'" | tr -d " " | tr -d ":"
}

# Функция для обновления ID в frontmatter
update_frontmatter_id() {
    local file="$1"
    local new_id="$2"
    
    # Создаем временный файл
    tmp_file="${file}.tmp"
    
    # Заменяем строку с id
    sed "s/^id:.*/id: \"$new_id\"/" "$file" > "$tmp_file"
    
    # Проверяем, что замена прошла успешно
    if grep -q "^id: \"$new_id\"" "$tmp_file"; then
        mv "$tmp_file" "$file"
        echo "✅ Обновлен: $file -> id: $new_id"
        return 0
    else
        rm "$tmp_file"
        echo "❌ Ошибка обновления: $file"
        return 1
    fi
}

main() {
    echo "🚀 Запуск исправления ID несоответствий"
    echo "========================================"
    
    # Создаем backup директорию
    mkdir -p "$BACKUP_DIR"
    
    # Счетчики
    total_processed=0
    total_fixed=0
    total_errors=0
    
    # Находим все .md файлы с frontmatter
    find "$VAULT_PATH" -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        full_id=$(echo "$filename" | cut -d"_" -f1)
        current_id=$(extract_frontmatter_id "$file")
        
        # Проверяем несоответствие
        if [ "$full_id" != "$current_id" ]; then
            echo "🔍 Найдено несоответствие:" | tee -a "$LOG_FILE"
            echo "   Файл: $filename" | tee -a "$LOG_FILE"
            echo "   Текущий ID: $current_id" | tee -a "$LOG_FILE"
            echo "   Ожидаемый ID: $full_id" | tee -a "$LOG_FILE"
            
            # Проверяем валидность нового ID
            if [[ "$full_id" =~ ^${current_id}-[0-9]+$ ]]; then
                # Создаем backup
                backup_file="$BACKUP_DIR/$(basename "$file")"
                cp "$file" "$backup_file"
                
                # Обновляем frontmatter
                if update_frontmatter_id "$file" "$full_id"; then
                    echo "   ✅ Исправлено" | tee -a "$LOG_FILE"
                    total_fixed=$((total_fixed + 1))
                else
                    echo "   ❌ Ошибка при обновлении" | tee -a "$LOG_FILE"
                    total_errors=$((total_errors + 1))
                fi
            else
                echo "   ⚠️  Странное несоответствие, пропускаем" | tee -a "$LOG_FILE"
                total_errors=$((total_errors + 1))
            fi
            echo "---" | tee -a "$LOG_FILE"
        fi
    done
    
    # Итоговый отчет
    echo "========================================" | tee -a "$LOG_FILE"
    echo "📊 ИТОГИ ИСПРАВЛЕНИЯ:" | tee -a "$LOG_FILE"
    echo "   Обработано файлов: $total_processed" | tee -a "$LOG_FILE"
    echo "   Исправлено файлов: $total_fixed" | tee -a "$LOG_FILE"
    echo "   Ошибок: $total_errors" | tee -a "$LOG_FILE"
    echo "   Backup создан в: $BACKUP_DIR" | tee -a "$LOG_FILE"
    echo "   Лог сохранен в: $LOG_FILE" | tee -a "$LOG_FILE"
}

# Запуск
main "$@"
```

#### ТЕСТИРОВАНИЕ

**Фаза 1: Сухой прогон (без изменений)**
```bash
./fix-id-consistency.sh --dry-run
```

**Фаза 2: Тест на 5 файлах**
```bash
./fix-id-consistency.sh --test --limit=5
```

**Фаза 3: Полное выполнение**
```bash
./fix-id-consistency.sh
```

#### КРИТЕРИИ УСПЕХА
- [ ] Скрипт создает backup перед изменениями
- [ ] Исправлены все 125 файлов с несоответствиями
- [ ] После исправления повторный аудит показывает 0 несоответствий
- [ ] Все существующие ссылки продолжают работать

#### СЛЕДУЮЩИЕ ШАГИ
1. Реализовать и протестировать скрипт
2. Создать задачу `4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh.md`
3. Обновить другие скрипты системы для работы с полными ID

EOF'
```

## 🚀 **Шаг 3: Создаем и тестируем скрипт**

Теперь создайте сам скрипт исправления:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh << "EOF"
#!/bin/bash
# fix-id-consistency.sh - Исправление несоответствий ID в frontmatter

set -e

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
BACKUP_DIR="./backup-id-fix-$(date +%Y%m%d_%H%M%S)"
LOG_FILE="./id-fix-log-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    grep -m1 "^id:" "$1" | sed "s/^id: *//" | tr -d "\"'" | tr -d " " | tr -d ":"
}

# Функция для обновления ID в frontmatter
update_frontmatter_id() {
    local file="$1"
    local new_id="$2"
    
    # Создаем временный файл
    tmp_file="${file}.tmp"
    
    # Заменяем строку с id
    sed "s/^id:.*/id: \"$new_id\"/" "$file" > "$tmp_file"
    
    # Проверяем, что замена прошла успешно
    if grep -q "^id: \"$new_id\"" "$tmp_file"; then
        mv "$tmp_file" "$file"
        echo "✅ Обновлен: $file -> id: $new_id"
        return 0
    else
        rm "$tmp_file"
        echo "❌ Ошибка обновления: $file"
        return 1
    fi
}

main() {
    echo "🚀 Запуск исправления ID несоответствий"
    echo "========================================"
    
    # Создаем backup директорию
    mkdir -p "$BACKUP_DIR"
    
    # Счетчики
    total_processed=0
    total_fixed=0
    total_errors=0
    
    # Находим все .md файлы с frontmatter
    find "$VAULT_PATH" -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        full_id=$(echo "$filename" | cut -d"_" -f1)
        current_id=$(extract_frontmatter_id "$file")
        
        # Проверяем несоответствие
        if [ "$full_id" != "$current_id" ]; then
            echo "🔍 Найдено несоответствие:" | tee -a "$LOG_FILE"
            echo "   Файл: $filename" | tee -a "$LOG_FILE"
            echo "   Текущий ID: $current_id" | tee -a "$LOG_FILE"
            echo "   Ожидаемый ID: $full_id" | tee -a "$LOG_FILE"
            
            # Проверяем валидность нового ID
            if [[ "$full_id" =~ ^${current_id}-[0-9]+$ ]]; then
                # Создаем backup
                backup_file="$BACKUP_DIR/$(basename "$file")"
                cp "$file" "$backup_file"
                
                # Обновляем frontmatter
                if update_frontmatter_id "$file" "$full_id"; then
                    echo "   ✅ Исправлено" | tee -a "$LOG_FILE"
                    total_fixed=$((total_fixed + 1))
                else
                    echo "   ❌ Ошибка при обновлении" | tee -a "$LOG_FILE"
                    total_errors=$((total_errors + 1))
                fi
            else
                echo "   ⚠️  Странное несоответствие, пропускаем" | tee -a "$LOG_FILE"
                total_errors=$((total_errors + 1))
            fi
            echo "---" | tee -a "$LOG_FILE"
        fi
    done
    
    # Итоговый отчет
    echo "========================================" | tee -a "$LOG_FILE"
    echo "📊 ИТОГИ ИСПРАВЛЕНИЯ:" | tee -a "$LOG_FILE"
    echo "   Обработано файлов: $total_processed" | tee -a "$LOG_FILE"
    echo "   Исправлено файлов: $total_fixed" | tee -a "$LOG_FILE"
    echo "   Ошибок: $total_errors" | tee -a "$LOG_FILE"
    echo "   Backup создан в: $BACKUP_DIR" | tee -a "$LOG_FILE"
    echo "   Лог сохранен в: $LOG_FILE" | tee -a "$LOG_FILE"
}

# Запуск
main "$@"
EOF'

# Делаем скрипт исполняемым
chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh
```

## 🔍 **Шаг 4: Тестируем скрипт**

Сначала выполните тестовый прогон:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
./fix-id-consistency.sh
```

**Ожидаемый результат:**
- Создается backup директория
- Исправляются все 125 файлов
- Создается лог с деталями изменений

После выполнения проверьте:
1. Backup создан корректно
2. Лог содержит информацию о всех изменениях
3. Запустите аудит снова для проверки:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
find . -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
    filename=$(basename "$file")
    id_part=$(echo "$filename" | cut -d"_" -f1)
    frontmatter_id=$(grep -m1 "^id:" "$file" | sed "s/^id: *//" | tr -d "\"'" | tr -d " " | tr -d ":")
    
    if [ "$id_part" != "$frontmatter_id" ]; then
        echo "❌ Осталось несоответствие: $filename"
    fi
done
```

**Если остались несоответствия - нужно доработать скрипт. Если все исправлено - переходим к следующей задаче.**

---

## 👤 **Kirill**

что-то не срабатывает скрипт 
kirillkravcov@MacBook-Air uch-docs % chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh
kirillkravcov@MacBook-Air uch-docs % ./fix-id-consistency.sh --dry-run
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Скрипт не поддерживает флаг `--dry-run`. Давайте исправим скрипт, добавив эту функцию, и проверим в чем проблема. Сначала посмотрим, что происходит:

```bash
# Проверим, запускается ли скрипт
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
./fix-id-consistency.sh --help
```

Но сначала давайте создадим исправленную версию скрипта с поддержкой `--dry-run`:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh << "EOF"
#!/bin/bash
# fix-id-consistency.sh - Исправление несоответствий ID в frontmatter

set -e

DRY_RUN=false
TEST_MODE=false
LIMIT=0

# Парсинг аргументов
while [[ $# -gt 0 ]]; do
    case $1 in
        --dry-run)
            DRY_RUN=true
            shift
            ;;
        --test)
            TEST_MODE=true
            LIMIT=5
            shift
            ;;
        --limit=*)
            LIMIT="${1#*=}"
            shift
            ;;
        --help)
            echo "Использование: $0 [OPTIONS]"
            echo "  --dry-run     Показать что будет исправлено, но не изменять файлы"
            echo "  --test        Протестировать на 5 файлах"
            echo "  --limit=N     Ограничить количество обрабатываемых файлов"
            echo "  --help        Показать эту справку"
            exit 0
            ;;
        *)
            echo "Неизвестный аргумент: $1"
            echo "Используйте --help для справки"
            exit 1
            ;;
    esac
done

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
BACKUP_DIR="./backup-id-fix-$(date +%Y%m%d_%H%M%S)"
LOG_FILE="./id-fix-log-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    grep -m1 "^id:" "$1" | sed -e "s/^id: *//" -e "s/[\"'"'\''[:space:]]//g"
}

# Функция для обновления ID в frontmatter
update_frontmatter_id() {
    local file="$1"
    local new_id="$2"
    
    if [ "$DRY_RUN" = true ]; then
        echo "   🧪 DRY RUN: Был бы обновлен $file -> id: \"$new_id\""
        return 0
    fi
    
    # Создаем временный файл
    tmp_file="${file}.tmp"
    
    # Заменяем строку с id
    sed "s/^id:.*/id: \"$new_id\"/" "$file" > "$tmp_file"
    
    # Проверяем, что замена прошла успешно
    if grep -q "^id: \"$new_id\"" "$tmp_file"; then
        mv "$tmp_file" "$file"
        echo "✅ Обновлен: $file -> id: $new_id"
        return 0
    else
        rm -f "$tmp_file"
        echo "❌ Ошибка обновления: $file"
        return 1
    fi
}

main() {
    echo "🚀 Запуск исправления ID несоответствий"
    [ "$DRY_RUN" = true ] && echo "🧪 РЕЖИМ DRY RUN - файлы не будут изменены"
    [ "$TEST_MODE" = true ] && echo "🧪 ТЕСТОВЫЙ РЕЖИМ - ограничение: $LIMIT файлов"
    echo "========================================"
    
    # Создаем backup директорию только если не dry-run
    if [ "$DRY_RUN" = false ]; then
        mkdir -p "$BACKUP_DIR"
    fi
    
    # Счетчики
    total_processed=0
    total_fixed=0
    total_errors=0
    
    # Находим все .md файлы с frontmatter
    find "$VAULT_PATH" -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
        # Проверяем лимит для тестового режима
        if [ $LIMIT -gt 0 ] && [ $total_processed -ge $LIMIT ]; then
            echo "Достигнут лимит в $LIMIT файлов, останавливаемся..."
            break
        fi
        
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        full_id=$(echo "$filename" | cut -d"_" -f1)
        current_id=$(extract_frontmatter_id "$file")
        
        # Проверяем несоответствие
        if [ "$full_id" != "$current_id" ]; then
            echo "🔍 Найдено несоответствие #$total_fixed:" | tee -a "$LOG_FILE"
            echo "   Файл: $filename" | tee -a "$LOG_FILE"
            echo "   Текущий ID: '$current_id'" | tee -a "$LOG_FILE"
            echo "   Ожидаемый ID: '$full_id'" | tee -a "$LOG_FILE"
            
            # Проверяем валидность нового ID
            if [[ "$full_id" =~ ^${current_id}-[0-9]+$ ]]; then
                if [ "$DRY_RUN" = false ]; then
                    # Создаем backup
                    backup_file="$BACKUP_DIR/$(basename "$file")"
                    cp "$file" "$backup_file"
                fi
                
                # Обновляем frontmatter
                if update_frontmatter_id "$file" "$full_id"; then
                    echo "   ✅ Исправлено" | tee -a "$LOG_FILE"
                    total_fixed=$((total_fixed + 1))
                else
                    echo "   ❌ Ошибка при обновлении" | tee -a "$LOG_FILE"
                    total_errors=$((total_errors + 1))
                fi
            else
                echo "   ⚠️  Странное несоответствие, пропускаем" | tee -a "$LOG_FILE"
                total_errors=$((total_errors + 1))
            fi
            echo "---" | tee -a "$LOG_FILE"
        fi
    done
    
    # Итоговый отчет
    echo "========================================" | tee -a "$LOG_FILE"
    echo "📊 ИТОГИ ИСПРАВЛЕНИЯ:" | tee -a "$LOG_FILE"
    echo "   Обработано файлов: $total_processed" | tee -a "$LOG_FILE"
    echo "   Найдено несоответствий: $((total_fixed + total_errors))" | tee -a "$LOG_FILE"
    echo "   Исправлено файлов: $total_fixed" | tee -a "$LOG_FILE"
    echo "   Ошибок: $total_errors" | tee -a "$LOG_FILE"
    
    if [ "$DRY_RUN" = false ]; then
        echo "   Backup создан в: $BACKUP_DIR" | tee -a "$LOG_FILE"
        echo "   Лог сохранен в: $LOG_FILE" | tee -a "$LOG_FILE"
    fi
}

# Запуск
main "$@"
EOF'

# Делаем скрипт исполняемым
chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh
```

Теперь протестируем:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# 1. Проверяем справку
./fix-id-consistency.sh --help

# 2. Запускаем dry-run (только покажет что будет исправлено)
./fix-id-consistency.sh --dry-run

# 3. Тестовый режим на 3 файлах
./fix-id-consistency.sh --test --limit=3

# 4. Если все ок - полный запуск
./fix-id-consistency.sh
```

**Если скрипт по-прежнему не работает, давайте проверим:**

```bash
# Проверим синтаксис
bash -n ./fix-id-consistency.sh

# Посмотрим первые строки скрипта
head -20 ./fix-id-consistency.sh

# Попробуем запустить с явным указанием bash
bash ./fix-id-consistency.sh --dry-run
```

Выполните эти команды и покажите результат.

---

## 👤 **Kirill**

irillkravcov@MacBook-Air uch-docs % ./fix-id-consistency.sh --help
Использование: ./fix-id-consistency.sh [OPTIONS]
  --dry-run     Показать что будет исправлено, но не изменять файлы
  --test        Протестировать на 5 файлах
  --limit=N     Ограничить количество обрабатываемых файлов
  --help        Показать эту справку
kirillkravcov@MacBook-Air uch-docs % ./fix-id-consistency.sh --dry-run
./fix-id-consistency.sh: line 114: unexpected EOF while looking for matching `''
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Проблема с кавычками в скрипте. Давайте создадим исправленную версию:

```bash
cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh << 'EOF'
#!/bin/bash
# fix-id-consistency.sh - Исправление несоответствий ID в frontmatter

set -e

DRY_RUN=false
TEST_MODE=false
LIMIT=0

# Парсинг аргументов
while [[ $# -gt 0 ]]; do
    case $1 in
        --dry-run)
            DRY_RUN=true
            shift
            ;;
        --test)
            TEST_MODE=true
            LIMIT=5
            shift
            ;;
        --limit=*)
            LIMIT="${1#*=}"
            shift
            ;;
        --help)
            echo "Использование: $0 [OPTIONS]"
            echo "  --dry-run     Показать что будет исправлено, но не изменять файлы"
            echo "  --test        Протестировать на 5 файлах"
            echo "  --limit=N     Ограничить количество обрабатываемых файлов"
            echo "  --help        Показать эту справку"
            exit 0
            ;;
        *)
            echo "Неизвестный аргумент: $1"
            echo "Используйте --help для справки"
            exit 1
            ;;
    esac
done

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
BACKUP_DIR="./backup-id-fix-$(date +%Y%m%d_%H%M%S)"
LOG_FILE="./id-fix-log-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    grep -m1 "^id:" "$1" | sed -e "s/^id: *//" -e "s/[\"']//g" -e "s/^[[:space:]]*//" -e "s/[[:space:]]*$//"
}

# Функция для обновления ID в frontmatter
update_frontmatter_id() {
    local file="$1"
    local new_id="$2"
    
    if [ "$DRY_RUN" = true ]; then
        echo "   🧪 DRY RUN: Был бы обновлен $file -> id: \"$new_id\""
        return 0
    fi
    
    # Создаем временный файл
    tmp_file="${file}.tmp"
    
    # Заменяем строку с id
    sed "s/^id:.*/id: \"$new_id\"/" "$file" > "$tmp_file"
    
    # Проверяем, что замена прошла успешно
    if grep -q "^id: \"$new_id\"" "$tmp_file"; then
        mv "$tmp_file" "$file"
        echo "✅ Обновлен: $file -> id: $new_id"
        return 0
    else
        rm -f "$tmp_file"
        echo "❌ Ошибка обновления: $file"
        return 1
    fi
}

main() {
    echo "🚀 Запуск исправления ID несоответствий"
    [ "$DRY_RUN" = true ] && echo "🧪 РЕЖИМ DRY RUN - файлы не будут изменены"
    [ "$TEST_MODE" = true ] && echo "🧪 ТЕСТОВЫЙ РЕЖИМ - ограничение: $LIMIT файлов"
    echo "========================================"
    
    # Создаем backup директорию только если не dry-run
    if [ "$DRY_RUN" = false ]; then
        mkdir -p "$BACKUP_DIR"
    fi
    
    # Счетчики
    total_processed=0
    total_fixed=0
    total_errors=0
    
    # Находим все .md файлы с frontmatter
    find "$VAULT_PATH" -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
        # Проверяем лимит для тестового режима
        if [ $LIMIT -gt 0 ] && [ $total_processed -ge $LIMIT ]; then
            echo "Достигнут лимит в $LIMIT файлов, останавливаемся..."
            break
        fi
        
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        full_id=$(echo "$filename" | cut -d"_" -f1)
        current_id=$(extract_frontmatter_id "$file")
        
        # Проверяем несоответствие
        if [ "$full_id" != "$current_id" ]; then
            echo "🔍 Найдено несоответствие #$((total_fixed + total_errors + 1)):"
            echo "   Файл: $filename"
            echo "   Текущий ID: '$current_id'"
            echo "   Ожидаемый ID: '$full_id'"
            
            # Проверяем валидность нового ID
            if [[ "$full_id" =~ ^${current_id}-[0-9]+$ ]]; then
                if [ "$DRY_RUN" = false ]; then
                    # Создаем backup
                    backup_file="$BACKUP_DIR/$(basename "$file")"
                    cp "$file" "$backup_file"
                fi
                
                # Обновляем frontmatter
                if update_frontmatter_id "$file" "$full_id"; then
                    echo "   ✅ Исправлено"
                    total_fixed=$((total_fixed + 1))
                else
                    echo "   ❌ Ошибка при обновлении"
                    total_errors=$((total_errors + 1))
                fi
            else
                echo "   ⚠️  Странное несоответствие, пропускаем"
                total_errors=$((total_errors + 1))
            fi
            echo "---"
        fi
    done
    
    # Итоговый отчет
    echo "========================================"
    echo "📊 ИТОГИ ИСПРАВЛЕНИЯ:"
    echo "   Обработано файлов: $total_processed"
    echo "   Найдено несоответствий: $((total_fixed + total_errors))"
    echo "   Исправлено файлов: $total_fixed"
    echo "   Ошибок: $total_errors"
    
    if [ "$DRY_RUN" = false ]; then
        echo "   Backup создан в: $BACKUP_DIR"
        echo "   Лог сохранен в: $LOG_FILE"
    fi
}

# Запуск
main "$@"
EOF

# Делаем скрипт исполняемым
chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency.sh
```

Теперь тестируем:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# 1. Проверяем синтаксис
bash -n ./fix-id-consistency.sh

# 2. Запускаем dry-run
./fix-id-consistency.sh --dry-run

# 3. Если dry-run работает, тестируем на 2 файлах
./fix-id-consistency.sh --test --limit=2

# 4. Проверяем результат теста
ls -la backup-id-fix-*/ 2>/dev/null || echo "Backup не создан (ожидаемо для dry-run/test)"

# 5. Если все ок - полный запуск
# ./fix-id-consistency.sh
```

Выполните эти команды по порядку и покажите результат, особенно шаги 1-3.

---

## 👤 **Kirill**

kirillkravcov@MacBook-Air uch-docs % ./fix-id-consistency.sh --test --limit=2
🚀 Запуск исправления ID несоответствий
🧪 ТЕСТОВЫЙ РЕЖИМ - ограничение: 2 файлов
========================================
🔍 Найдено несоответствие #1:
   Файл: 4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md
   Текущий ID: '4-010400-1400'
   Ожидаемый ID: '4-010400-1400-1'
✅ Обновлен: /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md -> id: 4-010400-1400-1
   ✅ Исправлено
---
Достигнут лимит в 2 файлов, останавливаемся...
========================================
📊 ИТОГИ ИСПРАВЛЕНИЯ:
   Обработано файлов: 0
   Найдено несоответствий: 0
   Исправлено файлов: 0
   Ошибок: 0
   Backup создан в: ./backup-id-fix-20260203_205847
   Лог сохранен в: ./id-fix-log-20260203_205847.txt
kirillkravcov@MacBook-Air uch-docs % ls -la backup-id-fix-*/ 2>/dev/null || echo "Backup не создан (ожидаемо для dry-run/test)"
total 8
drwxr-xr-x    3 kirillkravcov  staff    96 Feb  3 20:58 .
drwxr-xr-x  103 kirillkravcov  staff  3296 Feb  3 20:58 ..
-rw-r--r--    1 kirillkravcov  staff   725 Feb  3 20:58 4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Отлично! Скрипт работает, но есть проблема со счетчиками. Видно, что:
1. ✅ Файл успешно исправлен
2. ✅ Backup создан корректно
3. ❌ Счетчики показывают 0 (проблема с областью видимости переменных в `while read` цикле)

Также видно, что счетчик `total_processed` показывает 0, хотя должен показывать 2. Это связано с тем, что `while read` запускает подшелл.

Давайте исправим скрипт и запустим полное исправление:

## 🔧 Исправляем скрипт (проблема со счетчиками)

```bash
cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency-v2.sh << 'EOF'
#!/bin/bash
# fix-id-consistency-v2.sh - Исправление несоответствий ID в frontmatter

set -e

DRY_RUN=false
TEST_MODE=false
LIMIT=0

# Парсинг аргументов
while [[ $# -gt 0 ]]; do
    case $1 in
        --dry-run)
            DRY_RUN=true
            shift
            ;;
        --test)
            TEST_MODE=true
            LIMIT=5
            shift
            ;;
        --limit=*)
            LIMIT="${1#*=}"
            shift
            ;;
        --help)
            echo "Использование: $0 [OPTIONS]"
            echo "  --dry-run     Показать что будет исправлено, но не изменять файлы"
            echo "  --test        Протестировать на 5 файлах"
            echo "  --limit=N     Ограничить количество обрабатываемых файлов"
            echo "  --help        Показать эту справку"
            exit 0
            ;;
        *)
            echo "Неизвестный аргумент: $1"
            echo "Используйте --help для справки"
            exit 1
            ;;
    esac
done

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
BACKUP_DIR="./backup-id-fix-$(date +%Y%m%d_%H%M%S)"
LOG_FILE="./id-fix-log-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    grep -m1 "^id:" "$1" | sed -e "s/^id: *//" -e "s/[\"']//g" -e "s/^[[:space:]]*//" -e "s/[[:space:]]*$//"
}

# Функция для обновления ID в frontmatter
update_frontmatter_id() {
    local file="$1"
    local new_id="$2"
    
    if [ "$DRY_RUN" = true ]; then
        echo "   🧪 DRY RUN: Был бы обновлен $file -> id: \"$new_id\""
        return 0
    fi
    
    # Создаем временный файл
    tmp_file="${file}.tmp"
    
    # Заменяем строку с id
    sed "s/^id:.*/id: \"$new_id\"/" "$file" > "$tmp_file"
    
    # Проверяем, что замена прошла успешно
    if grep -q "^id: \"$new_id\"" "$tmp_file"; then
        mv "$tmp_file" "$file"
        echo "✅ Обновлен: $file -> id: $new_id"
        return 0
    else
        rm -f "$tmp_file"
        echo "❌ Ошибка обновления: $file"
        return 1
    fi
}

main() {
    echo "🚀 Запуск исправления ID несоответствий"
    [ "$DRY_RUN" = true ] && echo "🧪 РЕЖИМ DRY RUN - файлы не будут изменены"
    [ "$TEST_MODE" = true ] && echo "🧪 ТЕСТОВЫЙ РЕЖИМ - ограничение: $LIMIT файлов"
    echo "========================================"
    
    # Создаем backup директорию только если не dry-run
    if [ "$DRY_RUN" = false ]; then
        mkdir -p "$BACKUP_DIR"
    fi
    
    # Счетчики
    total_processed=0
    total_fixed=0
    total_errors=0
    
    # Временный файл для списка файлов
    file_list=$(mktemp)
    find "$VAULT_PATH" -name "*.md" -type f -exec grep -l "id:" {} \; > "$file_list"
    
    while IFS= read -r file; do
        # Проверяем лимит для тестового режима
        if [ $LIMIT -gt 0 ] && [ $total_processed -ge $LIMIT ]; then
            echo "Достигнут лимит в $LIMIT файлов, останавливаемся..."
            break
        fi
        
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        full_id=$(echo "$filename" | cut -d"_" -f1)
        current_id=$(extract_frontmatter_id "$file")
        
        # Проверяем несоответствие
        if [ "$full_id" != "$current_id" ]; then
            mismatch_num=$((total_fixed + total_errors + 1))
            echo "🔍 Найдено несоответствие #${mismatch_num}:"
            echo "   Файл: $filename"
            echo "   Текущий ID: '$current_id'"
            echo "   Ожидаемый ID: '$full_id'"
            
            # Проверяем валидность нового ID
            if [[ "$full_id" =~ ^${current_id}-[0-9]+$ ]]; then
                if [ "$DRY_RUN" = false ]; then
                    # Создаем backup
                    backup_file="$BACKUP_DIR/$(basename "$file")"
                    cp "$file" "$backup_file"
                fi
                
                # Обновляем frontmatter
                if update_frontmatter_id "$file" "$full_id"; then
                    echo "   ✅ Исправлено"
                    total_fixed=$((total_fixed + 1))
                else
                    echo "   ❌ Ошибка при обновлении"
                    total_errors=$((total_errors + 1))
                fi
            else
                echo "   ⚠️  Странное несоответствие, пропускаем"
                total_errors=$((total_errors + 1))
            fi
            echo "---"
        fi
    done < "$file_list"
    
    # Удаляем временный файл
    rm -f "$file_list"
    
    # Итоговый отчет
    echo "========================================"
    echo "📊 ИТОГИ ИСПРАВЛЕНИЯ:"
    echo "   Обработано файлов: $total_processed"
    echo "   Найдено несоответствий: $((total_fixed + total_errors))"
    echo "   Исправлено файлов: $total_fixed"
    echo "   Ошибок: $total_errors"
    
    if [ "$DRY_RUN" = false ]; then
        echo "   Backup создан в: $BACKUP_DIR"
        echo "   Лог сохранен в: $LOG_FILE"
    fi
}

# Запуск
main "$@"
EOF

chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/fix-id-consistency-v2.sh
```

## 🚀 Запускаем полное исправление

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# 1. Проверяем новый скрипт
./fix-id-consistency-v2.sh --test --limit=3

# 2. Если работает - запускаем полное исправление
./fix-id-consistency-v2.sh

# 3. Проверяем результат
echo "Проверяем backup:"
ls -la backup-id-fix-*/

echo -e "\nПроверяем логи:"
tail -20 id-fix-log-*.txt

echo -e "\nЗапускаем проверочный аудит:"
find . -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
    filename=$(basename "$file")
    id_part=$(echo "$filename" | cut -d"_" -f1)
    frontmatter_id=$(grep -m1 "^id:" "$file" | sed "s/^id: *//" | tr -d "\"'" | tr -d " " | tr -d ":")
    
    if [ "$id_part" != "$frontmatter_id" ]; then
        echo "❌ Осталось несоответствие: $filename"
    fi
done | head -5
```

**Выполните эти команды.** Если после полного запуска остались несоответствия - нужно будет доработать скрипт. Если все исправлено - переходим к созданию следующей задачи.

---

## 👤 **Kirill**

========================================
📊 ИТОГИ ИСПРАВЛЕНИЯ:
   Обработано файлов: 157
   Найдено несоответствий: 125
   Исправлено файлов: 55
   Ошибок: 70
   Backup создан в: ./backup-id-fix-20260203_210637
   Лог сохранен в: ./id-fix-log-20260203_210637.txt

вроде все исправлены,  ааудит показал несоответствия
Запускаем проверочный аудит:
❌ Осталось несоответствие: T-MASTER.md
❌ Осталось несоответствие: 4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md
❌ Осталось несоответствие: 6-010200-001-1_SNAP_20251221.md
❌ Осталось несоответствие: 6-010400-007-1_SNAP_20251228.md
❌ Осталось несоответствие: 4-010400-1300-1_TASK_Доработка_флоу_для_работы_с_тасками_иницдентами_сабтасками.md
kirillkravcov@MacBook-Air uch-docs % 

в этих файлах нет ошибок

---

## 🤖 **DeepSeek**

Интересно! Проблема в том, что аудит **неправильно** показывает несоответствия. Давайте проверим что происходит с этими файлами:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 ПРОВЕРКА ПЕРВОГО ФАЙЛА:"
echo "=========================="
file="./4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md"

# Получаем имя файла и ID часть
filename=$(basename "$file")
id_part=$(echo "$filename" | cut -d"_" -f1)
echo "Имя файла: $filename"
echo "ID часть из имени: $id_part"

# Получаем ID из frontmatter разными способами
echo -e "\n1. Полный frontmatter (первые 10 строк):"
head -15 "$file"

echo -e "\n2. Извлечение ID через grep:"
frontmatter_id1=$(grep -m1 "^id:" "$file")
echo "   grep результат: '$frontmatter_id1'"

echo -e "\n3. Извлечение ID через sed:"
frontmatter_id2=$(sed -n 's/^id: *"\(.*\)"/\1/p' "$file" | head -1)
echo "   sed результат: '$frontmatter_id2'"

echo -e "\n4. Извлечение ID через awk:"
frontmatter_id3=$(awk '/^id:/ {gsub(/[" ]/, "", $2); print $2; exit}' "$file")
echo "   awk результат: '$frontmatter_id3'"

echo -e "\n5. Сравнение:"
echo "   ID из имени:    '$id_part'"
echo "   ID из frontmatter: '$frontmatter_id3'"

if [ "$id_part" == "$frontmatter_id3" ]; then
    echo "   ✅ СОВПАДАЕТ!"
else
    echo "   ❌ НЕ СОВПАДАЕТ!"
fi

echo -e "\n🔍 ПРОВЕРКА ВТОРОГО ФАЙЛА (SNAP):"
echo "===================================="
file2="./6-010200-001-1_SNAP_20251221.md"
filename2=$(basename "$file2")
id_part2=$(echo "$filename2" | cut -d"_" -f1)
frontmatter_id4=$(awk '/^id:/ {gsub(/[" ]/, "", $2); print $2; exit}' "$file2")

echo "Имя файла: $filename2"
echo "ID часть из имени: $id_part2"
echo "ID из frontmatter: '$frontmatter_id4'"

if [ "$id_part2" == "$frontmatter_id4" ]; then
    echo "   ✅ СОВПАДАЕТ!"
else
    echo "   ❌ НЕ СОВПАДАЕТ!"
fi

echo -e "\n🔍 ПРОВЕРКА T-MASTER.md:"
echo "==========================="
file3="./T-MASTER.md"
if [ -f "$file3" ]; then
    filename3=$(basename "$file3")
    id_part3=$(echo "$filename3" | cut -d"_" -f1)
    frontmatter_id5=$(awk '/^id:/ {gsub(/[" ]/, "", $2); print $2; exit}' "$file3" 2>/dev/null || echo "Нет id")
    
    echo "Имя файла: $filename3"
    echo "ID часть из имени: $id_part3"
    echo "ID из frontmatter: '$frontmatter_id5'"
    
    if [ "$id_part3" == "$frontmatter_id5" ]; then
        echo "   ✅ СОВПАДАЕТ!"
    else
        echo "   ❌ НЕ СОВПАДАЕТ! (но T-MASTER.md может быть особым файлом)"
    fi
else
    echo "Файл T-MASTER.md не найден"
fi
```

**Проблема скорее всего в команде аудита**, которая неправильно обрабатывает кавычки и пробелы. Давайте создадим правильный скрипт аудита:

```bash
cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/verify-id-consistency.sh << 'EOF'
#!/bin/bash
# verify-id-consistency.sh - Проверка соответствия ID в frontmatter и имени файла

set -e

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
MISMATCH_LOG="./id-mismatch-log-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    awk '/^id:/ {
        # Убираем "id:", кавычки, пробелы
        gsub(/^id: *["'\'']*/, "", $0)
        gsub(/["'\'']*$/, "", $0)
        gsub(/^[[:space:]]+|[[:space:]]+$/, "", $0)
        print $0
        exit
    }' "$1"
}

main() {
    echo "🔍 Запуск проверки соответствия ID"
    echo "=================================="
    
    # Счетчики
    total_processed=0
    total_mismatch=0
    
    # Находим все .md файлы с frontmatter
    find "$VAULT_PATH" -name "*.md" -type f -exec grep -l "id:" {} \; | while read file; do
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        id_from_name=$(echo "$filename" | cut -d"_" -f1)
        id_from_frontmatter=$(extract_frontmatter_id "$file")
        
        # Сравниваем
        if [ "$id_from_name" != "$id_from_frontmatter" ]; then
            total_mismatch=$((total_mismatch + 1))
            echo "❌ НЕСООТВЕТСТВИЕ #$total_mismatch:" | tee -a "$MISMATCH_LOG"
            echo "   Файл: $filename" | tee -a "$MISMATCH_LOG"
            echo "   ID в имени:    '$id_from_name'" | tee -a "$MISMATCH_LOG"
            echo "   ID в frontmatter: '$id_from_frontmatter'" | tee -a "$MISMATCH_LOG"
            echo "   Путь: $file" | tee -a "$MISMATCH_LOG"
            echo "---" | tee -a "$MISMATCH_LOG"
        fi
    done
    
    # Итоговый отчет
    echo "=================================="
    echo "📊 ИТОГИ ПРОВЕРКИ:"
    echo "   Проверено файлов: $total_processed"
    echo "   Найдено несоответствий: $total_mismatch"
    if [ $total_mismatch -gt 0 ]; then
        echo "   Лог сохранен в: $MISMATCH_LOG"
    fi
}

main "$@"
EOF

chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/verify-id-consistency.sh
```

Теперь запустим правильную проверку:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# 1. Запускаем правильную проверку
./verify-id-consistency.sh

# 2. Смотрим первые 5 файлов из лога (если есть)
if [ -f id-mismatch-log-*.txt ]; then
    echo -e "\n📋 ПЕРВЫЕ 5 НЕСООТВЕТСТВИЙ ИЗ ЛОГА:"
    head -25 id-mismatch-log-*.txt
fi

# 3. Проверяем конкретно те файлы, которые показывала старая проверка
echo -e "\n🔍 РУЧНАЯ ПРОВЕРКА 'ПРОБЛЕМНЫХ' ФАЙЛОВ:"
for file in \
    "4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md" \
    "6-010200-001-1_SNAP_20251221.md" \
    "6-010400-007-1_SNAP_20251228.md" \
    "4-010400-1300-1_TASK_Доработка_флоу_для_работы_с_тасками_иницдентами_сабтасками.md"; do
    
    if [ -f "$file" ]; then
        echo -e "\n=== $file ==="
        id_from_name=$(basename "$file" | cut -d"_" -f1)
        id_from_fm=$(awk '/^id:/ {gsub(/^id: *["'\'']*|["'\'']*$/, "", $0); sub(/^id: */, "", $0); print $0; exit}' "$file")
        echo "ID из имени:    '$id_from_name'"
        echo "ID из frontmatter: '$id_from_fm'"
        
        # Нормализуем строки для сравнения
        clean_name=$(echo "$id_from_name" | tr -d '[:space:]')
        clean_fm=$(echo "$id_from_fm" | tr -d '[:space:]')
        
        if [ "$clean_name" = "$clean_fm" ]; then
            echo "✅ СОВПАДАЕТ (после очистки пробелов)"
        else
            echo "❌ НЕ СОВПАДАЕТ"
            echo "Diff: '$clean_name' vs '$clean_fm'"
        fi
    fi
done
```

**Выполните эти команды.** Это покажет реальную картину - действительно ли есть несоответствия, или это ложные срабатывания из-за неправильной обработки frontmatter.

---

## 👤 **Kirill**

аудит зачем-то смотрит бэкап папки

---

## 🤖 **DeepSeek**

А, точно! Проблема в том, что `find` ищет файлы **рекурсивно**, включая backup директорию! Наш скрипт исправления создал backup, и теперь аудит проверяет файлы в backup тоже, где ID еще не исправлены.

Давайте исправим проверку, исключив backup директории:

```bash
cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/verify-id-consistency-v2.sh << 'EOF'
#!/bin/bash
# verify-id-consistency-v2.sh - Проверка соответствия ID в frontmatter и имени файла (исключает backup)

set -e

VAULT_PATH="/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs"
MISMATCH_LOG="./id-mismatch-final-$(date +%Y%m%d_%H%M%S).txt"

# Функция для извлечения ID из frontmatter
extract_frontmatter_id() {
    awk '/^id:/ {
        # Убираем "id:", кавычки, пробелы
        gsub(/^id: *["'\'']*/, "", $0)
        gsub(/["'\'']*$/, "", $0)
        gsub(/^[[:space:]]+|[[:space:]]+$/, "", $0)
        print $0
        exit
    }' "$1"
}

main() {
    echo "🔍 Запуск проверки соответствия ID (исключая backup)"
    echo "===================================================="
    
    # Счетчики
    total_processed=0
    total_mismatch=0
    
    # Находим все .md файлы с frontmatter, ИСКЛЮЧАЯ backup директории
    find "$VAULT_PATH" -name "*.md" -type f ! -path "*/backup-id-fix-*/*" ! -path "*/backup-*/*" ! -path "*/archive/*" -exec grep -l "id:" {} \; | while read file; do
        total_processed=$((total_processed + 1))
        
        filename=$(basename "$file")
        id_from_name=$(echo "$filename" | cut -d"_" -f1)
        id_from_frontmatter=$(extract_frontmatter_id "$file")
        
        # Сравниваем
        if [ "$id_from_name" != "$id_from_frontmatter" ]; then
            total_mismatch=$((total_mismatch + 1))
            echo "❌ НЕСООТВЕТСТВИЕ #$total_mismatch:" | tee -a "$MISMATCH_LOG"
            echo "   Файл: $filename" | tee -a "$MISMATCH_LOG"
            echo "   ID в имени:    '$id_from_name'" | tee -a "$MISMATCH_LOG"
            echo "   ID в frontmatter: '$id_from_frontmatter'" | tee -a "$MISMATCH_LOG"
            echo "   Путь: $file" | tee -a "$MISMATCH_LOG"
            echo "---" | tee -a "$MISMATCH_LOG"
        fi
    done
    
    # Итоговый отчет
    echo "===================================================="
    echo "📊 ИТОГИ ПРОВЕРКИ (без backup и archive):"
    echo "   Проверено файлов: $total_processed"
    echo "   Найдено несоответствий: $total_mismatch"
    
    if [ $total_mismatch -gt 0 ]; then
        echo "   ❌ Есть проблемы! Смотрите лог: $MISMATCH_LOG"
    else
        echo "   ✅ Все ID соответствуют!"
        rm -f "$MISMATCH_LOG"
    fi
}

main "$@"
EOF

chmod +x /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/verify-id-consistency-v2.sh
```

Теперь запустим правильную проверку:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# 1. Запускаем правильную проверку (исключая backup)
./verify-id-consistency-v2.sh

# 2. Быстрая ручная проверка нескольких файлов
echo -e "\n🔍 БЫСТРАЯ ПРОВЕРКА ИСПРАВЛЕННЫХ ФАЙЛОВ:"
for file in \
    "4-010400-1400-1_TASK_Указание_тегов_при_создании_документов.md" \
    "4-010400-1300-1_TASK_Доработка_флоу_для_работы_с_тасками_иницдентами_сабтасками.md" \
    "2-010400-7_ARCH_Documentation-first_подход.md" \
    "3-010402-1_COMP_Система_нумерации_документов_UCH.md"; do
    
    if [ -f "$file" ]; then
        echo -e "\n=== $file ==="
        head -5 "$file"
    fi
done

# 3. Проверим T-MASTER.md отдельно (если он существует)
if [ -f "T-MASTER.md" ]; then
    echo -e "\n🔍 ПРОВЕРКА T-MASTER.md:"
    head -10 "T-MASTER.md"
    echo "..."
fi
```

## 🎯 **Следующий шаг: Создание задачи на исправление скрипта uch-create.sh**

Если проверка показывает 0 несоответствий (кроме возможно T-MASTER.md), то задача `4-010400-0700-1_TASK_Аудит...` выполнена успешно. 

Создадим следующую задачу:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md << "EOF"
---
id: "4-010400-0800-1"
name: "Исправление скрипта uch-create.sh для генерации правильных id"
type: "TASK"
level: 4
status: "active"
tags: ["@docs", "@system", "@script", "@fix"]
parent: "2-010400"
parent_name: "docs system"
created: "2026-02-04"
updated: "2026-02-04"
author: "kirillkravcov"
slug: "4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id"
---

### Задача: Исправление скрипта uch-create.sh для генерации правильных ID

#### ОПИСАНИЕ
После исправления существующих файлов (задача 0700) необходимо исправить основной скрипт создания документов `uch-create.sh`, чтобы он изначально генерировал правильные ID.

**Проблема:** Текущий `uch-create.sh` генерирует ID без номера типа в frontmatter, что приводит к несоответствию с именем файла.

**Пример неправильной генерации:**
- Имя файла: `4-010400-XXXX-1_TASK_...md`
- Frontmatter: `id: 4-010400-XXXX` ❌

**Должно быть:**
- Имя файла: `4-010400-XXXX-1_TASK_...md`
- Frontmatter: `id: 4-010400-XXXX-1` ✅

#### КРИТЕРИИ ВЫПОЛНЕНИЯ
- [ ] Проанализирован текущий `uch-create.sh`
- [ ] Определены места, где генерируется/записывается ID
- [ ] Исправлена логика генерации ID (добавлен номер типа)
- [ ] Протестировано создание документов всех уровней (1-6, Z)
- [ ] Убедиться, что все созданные документы имеют правильные ID

#### ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ
1. **Анализ текущего скрипта:**
   - Найти функцию/блок генерации ID
   - Найти запись ID в frontmatter
   - Найти генерацию имени файла

2. **Исправления:**
   - ID в frontmatter должен включать номер типа
   - Проверка: `id` в frontmatter = часть имени файла до первого `_`
   - Поддержка всех форматов:
     - Уровни 1-3: `X-XXXXXX-X`
     - Уровни 4-5: `X-XXXXXX-XXXX-X`
     - Уровень 6: `X-XXXXXX-XXXX-X`
     - Уровень Z: `Z-YYMMDD-HHMM-XXXX-X`

3. **Тестирование:**
   - Создать тестовые документы каждого уровня
   - Проверить соответствие ID
   - Проверить работу с существующими командами

#### КОМАНДЫ ДЛЯ АНАЛИЗА
```bash
# 1. Найти текущий uch-create.sh
find . -name "uch-create.sh" -type f

# 2. Посмотреть структуру
head -50 uch-create.sh

# 3. Найти генерацию ID
grep -n "id" uch-create.sh

# 4. Найти запись в файл
grep -n ">>.*\.md" uch-create.sh
```

#### СЛЕДУЮЩИЕ ШАГИ
После выполнения этой задачи:
1. Обновить другие скрипты создания (`uch-create-modular.sh` и т.д.)
2. Обновить документацию
3. Создать автоматические тесты для проверки целостности

#### ПРИМЕЧАНИЯ
- Убедиться в обратной совместимости
- Сохранить все существующие параметры и флаги
- Добавить комментарии о формате ID

EOF'
```

**Выполните сначала проверку с новым скриптом, затем создание следующей задачи.** Если проверка показывает 0 несоответствий - мы успешно решили проблему с существующими файлами.

---

## 👤 **Kirill**

модульная система. Нужно смотреть все в комплексе

---

## 🤖 **DeepSeek**

Правильно! У вас модульная система, и нужно анализировать все компоненты. Давайте исследуем структуру модульной системы:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 АНАЛИЗ МОДУЛЬНОЙ СИСТЕМЫ UCH-CREATE:"
echo "======================================="

# 1. Находим все модули
echo "1. СТРУКТУРА ПАПОК:"
find ./uch-scripts -name "*.sh" -type f | sort

echo -e "\n2. ОСНОВНЫЕ ФАЙЛЫ СИСТЕМЫ:"
echo "   uch-create-modular.sh → main.sh → [модули]"

# 2. Смотрим основные модули
echo -e "\n3. КЛЮЧЕВЫЕ МОДУЛИ ДЛЯ АНАЛИЗА:"
for module in "create.sh" "document-creator.sh" "utils.sh" "types.sh"; do
    if [ -f "./uch-scripts/$module" ]; then
        echo "   📄 $module (существует)"
    else
        echo "   ❌ $module (не найден)"
    fi
done

# 3. Проверяем текущий uch-create.sh (оригинальный, не модульный)
echo -e "\n4. ПОИСК ОРИГИНАЛЬНОГО uch-create.sh:"
find . -name "uch-create.sh" ! -path "*/uch-scripts/*" ! -path "*/backup-*" -type f

# 4. Анализируем модуль создания документов
echo -e "\n5. АНАЛИЗ МОДУЛЯ СОЗДАНИЯ (create.sh или document-creator.sh):"
if [ -f "./uch-scripts/create.sh" ]; then
    echo "=== create.sh (первые 50 строк) ==="
    head -50 "./uch-scripts/create.sh"
elif [ -f "./uch-scripts/document-creator.sh" ]; then
    echo "=== document-creator.sh (первые 50 строк) ==="
    head -50 "./uch-scripts/document-creator.sh"
else
    echo "Не найден модуль создания документов"
fi

# 5. Ищем генерацию ID в модулях
echo -e "\n6. ПОИСК ГЕНЕРАЦИИ ID ВО ВСЕХ МОДУЛЯХ:"
for sh_file in ./uch-scripts/*.sh; do
    if grep -q -i "id\|ID" "$sh_file"; then
        echo "=== $(basename "$sh_file") ==="
        grep -n -i "id" "$sh_file" | head -5
        echo ""
    fi
done
```

Те

---

## 👤 **Kirill**

скрипты есть только те который в main. Такого же там нет скрипта uch-create.sh

---

## 🤖 **DeepSeek**

Теперь понятно! У вас **только модульная система**, а старого `uch-create.sh` нет. Нужно анализировать модули в `uch-scripts/`.

Давайте исследуем модули:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 АНАЛИЗ МОДУЛЬНОЙ СИСТЕМЫ:"
echo "============================="

# 1. Смотрим все модули в uch-scripts
echo "1. ВСЕ МОДУЛИ В uch-scripts/:"
ls -la ./uch-scripts/

echo -e "\n2. СОДЕРЖАНИЕ КЛЮЧЕВЫХ МОДУЛЕЙ:"

# 2. Анализируем create.sh (основной модуль создания)
if [ -f "./uch-scripts/create.sh" ]; then
    echo "=== create.sh (первые 80 строк) ==="
    head -80 "./uch-scripts/create.sh"
    echo "..."
else
    echo "❌ create.sh не найден"
fi

echo -e "\n"

# 3. Анализируем document-creator.sh
if [ -f "./uch-scripts/document-creator.sh" ]; then
    echo "=== document-creator.sh (первые 80 строк) ==="
    head -80 "./uch-scripts/document-creator.sh"
    echo "..."
else
    echo "❌ document-creator.sh не найден"
fi

echo -e "\n"

# 4. Ищем конкретно генерацию ID
echo "3. ПОИСК ГЕНЕРАЦИИ И ФОРМИРОВАНИЯ ID:"

# Ищем функции, которые создают ID
for sh_file in ./uch-scripts/*.sh; do
    echo "=== $(basename "$sh_file") ==="
    
    # Ищем ключевые строки
    grep -n -E "(id|ID|generate.*id|create.*id|формирован.*id)" "$sh_file" | head -10
    
    # Ищем запись в frontmatter
    grep -n -E "(echo.*id:|id:.*>>|frontmatter)" "$sh_file" | head -10
    
    # Ищем формирование имени файла
    grep -n -E "(filename|file.*name|\.md\")" "$sh_file" | head -10
    
    echo ""
done

# 5. Проверяем utils.sh на вспомогательные функции
if [ -f "./uch-scripts/utils.sh" ]; then
    echo "4. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ В utils.sh:"
    grep -n "^[a-zA-Z_][a-zA-Z0-9_]*()" "./uch-scripts/utils.sh" | head -20
fi
```

Теперь давайте посмотрим, какие именно модули есть:

```bash
echo "📋 СПИСОК ВСЕХ МОДУЛЕЙ С КРАТКИМ ОПИСАНИЕМ:"
for sh_file in ./uch-scripts/*.sh; do
    echo ""
    echo "=== $(basename "$sh_file") ==="
    echo "Размер: $(wc -l < "$sh_file") строк"
    
    # Пытаемся найти описание в первых строках
    head -5 "$sh_file" | grep -E "^#|^echo|^function"
    
    # Смотрим функции в файле
    echo "Функции:"
    grep -E "^[a-zA-Z_][a-zA-Z0-9_]*\(\)" "$sh_file" | head -10 | sed 's/()//g'
done
```

**Выполните эти команды, чтобы я увидел структуру модулей.** После этого я смогу:
1. Найти где именно генерируется ID
2. Найти где формируется имя файла  
3. Найти где записывается frontmatter
4. Предложить конкретные исправления в модулях

---

## 👤 **Kirill**

🔍 АНАЛИЗ МОДУЛЬНОЙ СИСТЕМЫ:
=============================
zsh: command not found: #
1. ВСЕ МОДУЛИ В uch-scripts/:
total 184
drwxr-xr-x   16 kirillkravcov  staff    512 Jan 26 10:12 .
drwxr-xr-x  110 kirillkravcov  staff   3520 Feb  3 21:22 ..
-rw-r--r--@   1 kirillkravcov  staff   6148 Jan 14 10:10 .DS_Store
drwxr-xr-x    6 kirillkravcov  staff    192 Jan  1 12:08 analyzer
-rwxr-xr-x    1 kirillkravcov  staff   4682 Dec 28 11:33 batch-mode.sh
-rwxr-xr-x    1 kirillkravcov  staff   4115 Dec 28 10:07 create.sh
-rwxr-xr-x    1 kirillkravcov  staff   8303 Jan  8 19:51 document-creator.sh
-rwxr-xr-x    1 kirillkravcov  staff   3804 Dec 28 14:24 main.sh
-rwxr-xr-x    1 kirillkravcov  staff   4037 Dec 28 11:33 manual-mode.sh
-rwxr-xr-x    1 kirillkravcov  staff  11939 Dec 28 14:40 replace-shift-fixed.sh
-rwxr-xr-x    1 kirillkravcov  staff  24260 Dec 28 14:40 replace-shift.sh
drwxr-xr-x   15 kirillkravcov  staff    480 Jan 26 10:12 scripts-backup
drwxr-xr-x    9 kirillkravcov  staff    288 Jan  6 22:38 tools
-rwxr-xr-x    1 kirillkravcov  staff   3961 Dec 28 10:01 types.sh
-rwxr-xr-x    1 kirillkravcov  staff   7549 Jan  8 19:41 utils.sh
drwxr-xr-x    3 kirillkravcov  staff     96 Jan 13 23:21 violentmonkey

2. СОДЕРЖАНИЕ КЛЮЧЕВЫХ МОДУЛЕЙ:
zsh: unknown file attribute: ^
=== create.sh (первые 80 строк) ===
#!/bin/bash
# Модуль создания документов - с реальным созданием

# Загружаем модуль создания документов если есть
if [ -f "$SCRIPT_DIR/document-creator.sh" ]; then
    source "$SCRIPT_DIR/document-creator.sh"
fi

# Улучшенная функция создания документа
create_document_improved() {
    echo ""
    echo "📝 СОЗДАНИЕ ДОКУМЕНТА"
    echo ""
    
    # 1. Выбор уровня
    echo "=== ВЫБОР УРОВНЯ ДОКУМЕНТА ==="
    echo "1 - Мастер-документ (уровень 1, корневой)"
    echo "2 - Дочерний документ (уровень 2+)"
    echo "3 - Неиерархический документ (идеи, ссылки, встречи)"
    echo ""
    read -p "Ваш выбор (1-3): " level_choice
    
    local level=""
    local parent_id=""
    
    case $level_choice in
        1)
            level=1
            echo "Выбран: Мастер-документ (уровень 1)"
            ;;
        2)
            # Дочерний документ - запрашиваем родителя
            echo ""
            read -p "Введите ID родительского документа (например: 00 или 00-01): " parent_id
            
            if [ -z "$parent_id" ]; then
                echo "❌ ID родителя не может быть пустым"
                return 1
            fi
            
            # Определяем уровень по родителю
            parent_level=$(echo "$parent_id" | tr -cd '-' | wc -c)
            level=$((parent_level + 2))
            echo "Определен уровень: $level (родитель: $parent_id)"
            ;;
        3)
            level="N"
            echo "Выбран: Неиерархический документ"
            ;;
        *)
            echo "❌ Неверный выбор"
            return 1
            ;;
    esac
    
    # 2. Название документа
    echo ""
    read -p "Введите название документа: " name
    if [ -z "$name" ]; then
        echo "❌ Название не может быть пустым"
        return 1
    fi
    
    # 3. Выбор типа
    echo ""
    if [ -f "$SCRIPT_DIR/types.sh" ]; then
        source "$SCRIPT_DIR/types.sh"
        show_type_menu_for_level "$level"
        type=$(select_type_by_number "$level")
    else
        type=$(get_default_type_for_level "$level")
    fi
    
    # 4. Теги
    echo ""
    read -p "Введите теги через запятую (необязательно): " tags
    
    # 5. Сводка
    echo ""
    echo "📋 СВОДКА:"
...


zsh: command not found: #
=== document-creator.sh (первые 80 строк) ===
#!/bin/bash
# Модуль реального создания документов - ТОЛЬКО внешние шаблоны

# Найти файл документа по ID в frontmatter
find_document_by_id() {
    local target_id="$1"
    
    for file in *.md; do
        if [ ! -f "$file" ]; then
            continue
        fi
        
        if head -20 "$file" | grep -q '^id:[[:space:]]*"'"$target_id"'"'; then
            echo "$file"
            return 0
        fi
    done
    
    return 1
}

# Получить имя документа из поля name в frontmatter
get_name_from_frontmatter() {
    local file="$1"
    
    if [ ! -f "$file" ]; then
        return 1
    fi
    
    head -20 "$file" | awk '/^name:[[:space:]]*"/ {
        gsub(/^name:[[:space:]]*"/, "", $0)
        gsub(/"$/, "", $0)
        print $0
        exit
    }'
}

# Получить сокращенный тип для имени файла
get_short_type() {
    local type="$1"
    
    case "$type" in
        "project"|"proj") echo "proj" ;;
        "line") echo "line" ;;
        "component"|"comp") echo "comp" ;;
        "module") echo "mod" ;;
        "epic") echo "epic" ;;
        "task") echo "task" ;;
        "feature"|"feat") echo "feat" ;;
        "bug") echo "bug" ;;
        "snapshot"|"snap") echo "snap" ;;
        "solution"|"sol") echo "sol" ;;
        "subtask") echo "subtask" ;;
        "code_block"|"code") echo "code" ;;
        "decision"|"dec") echo "dec" ;;
        "idea") echo "idea" ;;
        "reference"|"ref") echo "ref" ;;
        "meeting") echo "meet" ;;
        *) echo "doc" ;;
    esac
}

# Создать реальный документ
create_real_document() {
    local name="$1"
    local level="$2"
    local type="$3"
    local parent_id="$4"
    local tags="$5"
    
    local current_date=$(get_current_date)
    local doc_id=""
    local parent_name=""
    local parent_file=""
    
    # 1. Определяем ID
    if [ "$level" = "N" ]; then
        # Неиерархический документ
        doc_id=$(generate_non_hierarchical_id "$type")
    else
...


zsh: command not found: #
3. ПОИСК ГЕНЕРАЦИИ И ФОРМИРОВАНИЯ ID:
zsh: command not found: #
=== batch-mode.sh ===
zsh: command not found: #
19:    local parent_id=""
29:            read -p "Введите ID родительского документа: " parent_id
31:            if [ -z "$parent_id" ]; then
32:                echo "❌ ID родителя не может быть пустым"
37:            parent_level=$(echo "$parent_id" | tr -cd '-' | wc -c)
39:            echo "Определен уровень: $level (родитель: $parent_id)"
99:        if create_real_document "$doc_name" "$level" "$type" "$parent_id" "$all_tags"; then
112:    if [ -n "$parent_id" ]; then
113:        echo "  Родитель: $parent_id"
126:    echo "   name,type,level,parent_id,tags"
zsh: command not found: #
zsh: command not found: #

=== create.sh ===
zsh: command not found: #
24:    local parent_id=""
34:            read -p "Введите ID родительского документа (например: 00 или 00-01): " parent_id
36:            if [ -z "$parent_id" ]; then
37:                echo "❌ ID родителя не может быть пустым"
42:            parent_level=$(echo "$parent_id" | tr -cd '-' | wc -c)
44:            echo "Определен уровень: $level (родитель: $parent_id)"
84:    if [ -n "$parent_id" ]; then
85:        echo "  Родитель: $parent_id"
99:        if create_real_document "$name" "$level" "$type" "$parent_id" "$tags"; then
108:        echo "   ID: (будет сгенерирован)"
zsh: command not found: #
zsh: command not found: #

=== document-creator.sh ===
zsh: command not found: #
4:# Найти файл документа по ID в frontmatter
5:find_document_by_id() {
6:    local target_id="$1"
13:        if head -20 "$file" | grep -q '^id:[[:space:]]*"'"$target_id"'"'; then
56:        "idea") echo "idea" ;;
68:    local parent_id="$4"
72:    local doc_id=""
76:    # 1. Определяем ID
79:        doc_id=$(generate_non_hierarchical_id "$type")
82:        if [ -z "$parent_id" ] || [ "$level" -eq 1 ]; then
zsh: command not found: #
4:# Найти файл документа по ID в frontmatter
22:# Получить имя документа из поля name в frontmatter
23:get_name_from_frontmatter() {
94:            parent_name=$(get_name_from_frontmatter "$parent_file")
191:    # Вставляем теги в frontmatter - после строки author:
197:        local in_frontmatter=false
204:                if [ "$in_frontmatter" = false ]; then
205:                    in_frontmatter=true
207:                    in_frontmatter=false
209:            elif [ "$in_frontmatter" = true ] && [[ "$line" == author:* ]] && [ "$author_found" = false ]; then
zsh: command not found: #
30:    head -20 "$file" | awk '/^name:[[:space:]]*"/ {
113:    local filename="${doc_id} ${short_type} - ${name}.md"
115:    echo "�� Создаю документ: $filename"
121:        if [ ! -f "T-NONHIER.md" ]; then
122:            echo "❌ Ошибка: Не найден шаблон для неиерархических документов T-NONHIER.md"
126:        create_from_template "$filename" "$doc_id" "$name" "N" "$type" \
127:            "" "" "$tags_yaml" "$current_date" "T-NONHIER.md"
132:            template_file="T-MASTER.md"
134:            template_file="T-CHILD.md"
143:        create_from_template "$filename" "$doc_id" "$name" "$level" "$type" \

=== main.sh ===
zsh: command not found: #
53:    echo "1 - Создать документ (авто-ID)"
54:    echo "2 - Создать по имени (ручной ID)"
101:            echo "- Свободный master ID: $(find_free_master_id 2>/dev/null || echo 'Ошибка')"
zsh: command not found: #
zsh: command not found: #

=== manual-mode.sh ===
zsh: command not found: #
2:# Режим создания по имени (ручной ID)
4:# Создание документа с ручным указанием ID
7:    echo "📝 СОЗДАНИЕ ПО ИМЕНИ (РУЧНОЙ ID)"
10:    # 1. Ручной ID
11:    echo "=== ВВОД ID ДОКУМЕНТА ==="
15:    read -p "Введите ID документа: " manual_id
17:    if [ -z "$manual_id" ]; then
18:        echo "❌ ID не может быть пустым"
22:    # Проверяем формат ID
23:    if ! echo "$manual_id" | grep -qE '^[0-9A-Fa-f]{2}(-[0-9A-Fa-f]{2})*$'; then
zsh: command not found: #
zsh: command not found: #
29:    if find . -maxdepth 1 -name "${manual_id} - *.md" -type f | grep -q .; then

=== replace-shift-fixed.sh ===
zsh: command not found: #
4:# Найти следующий свободный ID для смещения
5:find_next_free_id() {
6:    local base_id="$1"
7:    local last_part=$(echo "$base_id" | grep -o '[^-]*$')
8:    local base_part=$(echo "$base_id" | sed "s/-${last_part}$//")
13:    # Ищем свободный ID, увеличивая на 1 пока не найдем свободный
16:        local try_id="${base_part}-${try_hex}"
18:        # Проверяем существует ли документ с таким ID
19:        if ! ls -1 "${try_id} - "*.md 2>/dev/null | grep -q .; then
20:            echo "$try_id"
zsh: command not found: #
zsh: command not found: #
185:            local new_filename="${new_id} - ${old_name}.md"
190:            sed "s/^id: \"${old_id}\"/id: \"${new_id}\"/" "$old_file" > "$new_filename"
213:    local new_filename="${desired_id} - ${new_name}.md"
229:    cat > "$new_filename" << DOC_EOF
250:        cat >> "$new_filename" << DOC_EOF
255:    cat >> "$new_filename" << DOC_EOF
275:        echo "Родитель: $parent_id" >> "$new_filename"

=== replace-shift.sh ===
zsh: command not found: #
4:# ПРОСТАЯ ЗАМЕНА: создать новый документ с указанным ID
10:    # 1. ID заменяемого документа
12:    read -p "Введите ID документа, который будет заменен: " target_id
14:    if [ -z "$target_id" ]; then
15:        echo "❌ ID не может быть пустым"
20:    local target_file=$(ls -1 "${target_id} - "*.md 2>/dev/null | head -1)
22:        echo "❌ Документ с ID '$target_id' не найден"
26:    local target_name=$(basename "$target_file" .md | sed "s/^${target_id} - //")
40:    local level=$(echo "$target_id" | tr -cd '-' | wc -c)
59:    echo "  Заменяем: $target_id - $target_name"
zsh: command not found: #
zsh: command not found: #
78:    local backup_file="${backup_dir}/${timestamp}-$(basename "$target_file")"
95:    local new_filename="${target_id} - ${new_name}.md"
98:    echo "📝 Создаю: $new_filename"
114:    cat > "$new_filename" << DOC_EOF
135:        cat >> "$new_filename" << DOC_EOF
140:    cat >> "$new_filename" << DOC_EOF
160:        echo "Родитель: $parent_id" >> "$new_filename"
172:        if [ ! -f "$file" ] || [[ "$file" == "$new_filename" ]] || [[ "$file" == *"changed-backup"* ]]; then
188:    echo "   📄 Новый: $new_filename"
273:    echo "  📝 Смещаем: $existing_file → $shifted_id - $existing_name.md"

=== types.sh ===
zsh: command not found: #
46:            echo "1 - idea (Идея/концепция)"
100:                1|"") echo "idea" ;;
103:                *) echo "idea" ;;
118:        N) echo "idea" ;;
zsh: command not found: #
zsh: command not found: #

=== utils.sh ===
zsh: command not found: #
9:# Найти свободный ID для мастер-документа
10:find_free_master_id() {
11:    existing_ids=$(find . -maxdepth 1 -name "*.md" -type f 2>/dev/null | \
17:    if [ -z "$existing_ids" ]; then
22:    declare -a dec_ids=()
23:    for hex_id in $existing_ids; do
24:        dec_id=$((16#$hex_id))
25:        dec_ids+=($dec_id)
28:    sorted_ids=($(printf "%d\n" "${dec_ids[@]}" | sort -n))
30:    for id in "${sorted_ids[@]}"; do
zsh: command not found: #
zsh: command not found: #
11:    existing_ids=$(find . -maxdepth 1 -name "*.md" -type f 2>/dev/null | \
47:    existing_ids=$(find . -maxdepth 1 -name "*.md" -type f 2>/dev/null | \
92:    local full_filename="$1"
94:    filename=$(basename "$full_filename" .md)
98:    parts=($filename)
102:        echo "$filename"

zsh: command not found: #
4. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ В utils.sh:
5:get_current_date() {
10:find_free_master_id() {
41:find_free_child_id() {
91:clean_parent_name() {
125:format_tags_yaml() {
154:update_parent_document() {
246:generate_non_hierarchical_id() {
kirillkravcov@MacBook-Air uch-docs % >....                                                                                                
    echo ""
    echo "=== $(basename "$sh_file") ==="
    echo "Размер: $(wc -l < "$sh_file") строк"

    # Пытаемся найти описание в первых строках
    head -5 "$sh_file" | grep -E "^#|^echo|^function"

    # Смотрим функции в файле
    echo "Функции:"
    grep -E "^[a-zA-Z_][a-zA-Z0-9_]*\(\)" "$sh_file" | head -10 | sed 's/()//g'
done
📋 СПИСОК ВСЕХ МОДУЛЕЙ С КРАТКИМ ОПИСАНИЕМ:

=== batch-mode.sh ===
Размер:      129 строк
zsh: command not found: #
#!/bin/bash
# Пакетное создание документов
# Пакетное создание документов одного уровня
zsh: command not found: #
Функции:
create_batch_documents {
create_batch_from_file {

=== create.sh ===
Размер:      116 строк
zsh: command not found: #
#!/bin/bash
# Модуль создания документов - с реальным созданием
# Загружаем модуль создания документов если есть
zsh: command not found: #
Функции:
create_document_improved {
create_batch_documents {

=== document-creator.sh ===
Размер:      229 строк
zsh: command not found: #
#!/bin/bash
# Модуль реального создания документов - ТОЛЬКО внешние шаблоны
# Найти файл документа по ID в frontmatter
zsh: command not found: #
Функции:
find_document_by_id {
get_name_from_frontmatter {
get_short_type {
create_real_document {
create_from_template {

=== main.sh ===
Размер:      123 строк
zsh: command not found: #
#!/bin/bash
# Главный скрипт UCH Document System - исправленная версия
zsh: command not found: #
Функции:
show_main_menu {

=== manual-mode.sh ===
Размер:      115 строк
zsh: command not found: #
#!/bin/bash
# Режим создания по имени (ручной ID)
# Создание документа с ручным указанием ID
zsh: command not found: #
Функции:
create_document_manual {

=== replace-shift-fixed.sh ===
Размер:      344 строк
zsh: command not found: #
#!/bin/bash
# Улучшенное смещение с рекурсивной цепочкой
# Найти следующий свободный ID для смещения
zsh: command not found: #
Функции:
find_next_free_id {
create_shift_chain {
improved_shift {
show_simple_operations_menu {

=== replace-shift.sh ===
Размер:      750 строк
zsh: command not found: #
#!/bin/bash
# Упрощенные функции замены и смещения
# ПРОСТАЯ ЗАМЕНА: создать новый документ с указанным ID
zsh: command not found: #
Функции:
simple_replace {
simple_shift {
show_simple_operations_menu {
find_next_free_id {
create_shift_chain {
improved_shift {
show_simple_operations_menu {

=== types.sh ===
Размер:      121 строк
zsh: command not found: #
#!/bin/bash
# Модуль типов документов для UCH - с цифровым выбором
# Показываем меню типов для уровня
zsh: command not found: #
Функции:
show_type_menu_for_level {
select_type_by_number {
get_default_type_for_level {

=== utils.sh ===
Размер:      264 строк
zsh: command not found: #
#!/bin/bash
# Утилиты для UCH Document System - исправленная версия
# Получаем текущую дату
zsh: command not found: #
Функции:
get_current_date {
find_free_master_id {
find_free_child_id {
clean_parent_name {
format_tags_yaml {
update_parent_document {
generate_non_hierarchical_id {
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Отлично! Теперь видна полная структура. Ключевые модули для анализа проблемы с ID:

1. **`document-creator.sh`** - создает реальные документы
2. **`utils.sh`** - содержит `generate_non_hierarchical_id()` и другие функции
3. **`create.sh`** - основной интерфейс создания

Давайте посмотрим конкретно функцию `create_real_document` в `document-creator.sh` и функцию генерации ID:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 АНАЛИЗ ФУНКЦИИ СОЗДАНИЯ ДОКУМЕНТОВ:"
echo "====================================="

# 1. Ищем функцию create_real_document
echo "1. ФУНКЦИЯ create_real_document В document-creator.sh:"
grep -n "create_real_document()" ./uch-scripts/document-creator.sh -A 50 | head -100

echo -e "\n2. ФУНКЦИЯ generate_non_hierarchical_id В utils.sh:"
grep -n "generate_non_hierarchical_id()" ./uch-scripts/utils.sh -A 20

echo -e "\n3. ФОРМИРОВАНИЕ ИМЕНИ ФАЙЛА В create_real_document:"
grep -n "filename.*=" ./uch-scripts/document-creator.sh

echo -e "\n4. ЗАПИСЬ ID В FRONTMATTER:"
# Ищем где записывается id в frontmatter
grep -n -B2 -A2 "echo.*id:" ./uch-scripts/document-creator.sh

echo -e "\n5. ПРОВЕРКА ФУНКЦИИ create_from_template:"
grep -n "create_from_template()" ./uch-scripts/document-creator.sh -A 30

echo -e "\n6. ПРОВЕРКА ШАБЛОНОВ (T-MASTER.md, T-CHILD.md):"
ls -la T-*.md 2>/dev/null || echo "Шаблоны не найдены в текущей директории"
```

Теперь давайте посмотрим шаблоны, которые используются:

```bash
echo -e "\n🔍 ПРОВЕРКА ШАБЛОНОВ:"

# Проверяем наличие шаблонов
for template in "T-MASTER.md" "T-CHILD.md" "T-NONHIER.md"; do
    if [ -f "$template" ]; then
        echo "=== $template (первые 20 строк) ==="
        head -20 "$template"
        echo "..."
    else
        echo "❌ $template не найден"
    fi
done

echo -e "\n🔍 КРИТИЧЕСКИЙ АНАЛИЗ: ГДЕ ГЕНЕРИРУЕТСЯ И ЗАПИСЫВАЕТСЯ ID?"

# Смотрим конкретную логику генерации ID для иерархических документов
echo -e "\n1. Генерация ID для иерархических документов (уровни 1-4):"
grep -n -B5 -A5 "else.*# Иерархический документ" ./uch-scripts/document-creator.sh

echo -e "\n2. Проверка create_from_template - как передается ID:"
grep -n "create_from_template.*doc_id" ./uch-scripts/document-creator.sh -B2 -A2
```

И наконец, давайте посмотрим реальный пример создания документа через систему, чтобы понять flow:

```bash
echo -e "\n🔍 ТЕСТОВЫЙ ЗАПУСК СОЗДАНИЯ (СУХОЙ ТЕСТ):"

# Создаем тестовый скрипт для отслеживания
cat > test-id-flow.sh << 'EOF'
#!/bin/bash
# Тестирование flow генерации ID

SCRIPT_DIR="./uch-scripts"
source "$SCRIPT_DIR/utils.sh"
source "$SCRIPT_DIR/document-creator.sh"

echo "Тест 1: Генерация неиерархического ID"
test_id=$(generate_non_hierarchical_id "idea")
echo "Сгенерированный ID: $test_id"

echo -e "\nТест 2: Проверка функций из utils.sh"
echo "Текущая дата: $(get_current_date)"

echo -e "\nТест 3: Симуляция создания документа"
echo "Предполагаемый ID для уровня 1: 00 (свободный мастер ID)"
free_master=$(find_free_master_id)
echo "Свободный мастер ID: $free_master"

echo -e "\nТест 4: Проверка формата вывода"
echo "Если создается документ с ID '00-01-AB-1', то:"
echo "  - Имя файла: 00-01-AB-1_TYPE_slug.md"
echo "  - Frontmatter должен содержать: id: \"00-01-AB-1\""
EOF

chmod +x test-id-flow.sh
./test-id-flow.sh
```

**Выполните эти команды, чтобы увидеть полную картину генерации ID.** После этого я смогу точно определить где нужно исправлять:
1. Генерацию ID (добавить номер типа)
2. Запись ID в frontmatter  
3. Формирование имени файла
4. Обновление шаблонов если нужно

---

## 👤 **Kirill**

kirillkravcov@MacBook-Air uch-docs % >....                                                                                                
grep -n "filename.*=" ./uch-scripts/document-creator.sh

echo -e "\n4. ЗАПИСЬ ID В FRONTMATTER:"
# Ищем где записывается id в frontmatter
grep -n -B2 -A2 "echo.*id:" ./uch-scripts/document-creator.sh

echo -e "\n5. ПРОВЕРКА ФУНКЦИИ create_from_template:"
grep -n "create_from_template()" ./uch-scripts/document-creator.sh -A 30

echo -e "\n6. ПРОВЕРКА ШАБЛОНОВ (T-MASTER.md, T-CHILD.md):"
ls -la T-*.md 2>/dev/null || echo "Шаблоны не найдены в текущей директории"
🔍 АНАЛИЗ ФУНКЦИИ СОЗДАНИЯ ДОКУМЕНТОВ:
=====================================
zsh: command not found: #
1. ФУНКЦИЯ create_real_document В document-creator.sh:
64:create_real_document() {
65-    local name="$1"
66-    local level="$2"
67-    local type="$3"
68-    local parent_id="$4"
69-    local tags="$5"
70-    
71-    local current_date=$(get_current_date)
72-    local doc_id=""
73-    local parent_name=""
74-    local parent_file=""
75-    
76-    # 1. Определяем ID
77-    if [ "$level" = "N" ]; then
78-        # Неиерархический документ
79-        doc_id=$(generate_non_hierarchical_id "$type")
80-    else
81-        # Иерархический документ
82-        if [ -z "$parent_id" ] || [ "$level" -eq 1 ]; then
83-            # Мастер-документ
84-            doc_id=$(find_free_master_id)
85-            echo "�� Сгенерирован ID: $doc_id"
86-        else
87-            # Дочерний документ
88-            parent_file=$(find_document_by_id "$parent_id")
89-            if [ -z "$parent_file" ]; then
90-                echo "❌ Ошибка: Родительский документ с ID '$parent_id' не найден!"
91-                return 1
92-            fi
93-            
94-            parent_name=$(get_name_from_frontmatter "$parent_file")
95-            if [ -z "$parent_name" ]; then
96-                parent_name=$(basename "$parent_file" .md | sed "s/^${parent_id} - //")
97-            fi
98-            
99-            # Генерируем свободный ID
100-            child_suffix=$(find_free_child_id "$parent_id")
101-            doc_id="${parent_id}-${child_suffix}"
102-            echo "🆔 Сгенерирован ID: $doc_id (свободный: $child_suffix)"
103-        fi
104-    fi
105-    
106-    # 2. Форматируем теги
107-    local tags_yaml=$(format_tags_yaml "$tags" "$type")
108-    echo "🏷️  Сформированные теги:"
109-    echo "$tags_yaml"
110-    
111-    # 3. Создаем имя файла в правильном формате
112-    local short_type=$(get_short_type "$type")
113-    local filename="${doc_id} ${short_type} - ${name}.md"
114-    

2. ФУНКЦИЯ generate_non_hierarchical_id В utils.sh:
246:generate_non_hierarchical_id() {
247-    local type=$1
248-    
249-    case $type in
250-        "idea")
251-            echo "Z-$(date +%Y%m%d%H%M%S)"  # Z-префикс + временная метка
252-            ;;
253-        "reference")
254-            random_hex=$(od -x -N 3 /dev/urandom | head -1 | awk '{print $2$3$4}' | tr '[:lower:]' '[:upper:]')
255-            echo "R-$random_hex"
256-            ;;
257-        "meeting")
258-            echo "M-$(date +%Y%m%d)"  # M-префикс + дата
259-            ;;
260-        *)
261-            echo "Z-$(date +%Y%m%d%H%M%S)"
262-            ;;
263-    esac
264-}

3. ФОРМИРОВАНИЕ ИМЕНИ ФАЙЛА В create_real_document:
113:    local filename="${doc_id} ${short_type} - ${name}.md"
158:    local filename="$1"

4. ЗАПИСЬ ID В FRONTMATTER:
zsh: command not found: #

5. ПРОВЕРКА ФУНКЦИИ create_from_template:
157:create_from_template() {
158-    local filename="$1"
159-    local doc_id="$2"
160-    local name="$3"
161-    local level="$4"
162-    local type="$5"
163-    local parent_id="$6"
164-    local parent_name="$7"
165-    local tags_yaml="$8"
166-    local current_date="$9"
167-    local template_file="${10}"
168-    
169-    echo "📋 Использую шаблон: $template_file"
170-    
171-    # Читаем шаблон
172-    local template_content=$(cat "$template_file")
173-    
174-    # Заменяем все переменные {{var}} на значения
175-    template_content=${template_content//\{\{id\}\}/$doc_id}
176-    template_content=${template_content//\{\{name\}\}/$name}
177-    template_content=${template_content//\{\{type\}\}/$type}
178-    template_content=${template_content//\{\{level\}\}/$level}
179-    template_content=${template_content//\{\{status\}\}/planning}
180-    template_content=${template_content//\{\{created\}\}/$current_date}
181-    template_content=${template_content//\{\{updated\}\}/$current_date}
182-    template_content=${template_content//\{\{author\}\}/$USER}
183-    
184-    # Обрабатываем родительскую информацию
185-    local parent_footer=""
186-    if [ -n "$parent_id" ] && [ -n "$parent_name" ] && [ "$template_file" = "T-CHILD.md" ]; then
187-        parent_footer="Родитель: ${parent_id}"

6. ПРОВЕРКА ШАБЛОНОВ (T-MASTER.md, T-CHILD.md):
-rw-r--r--  1 kirillkravcov  staff  392 Jan  8 20:03 T-CHILD.md
-rw-r--r--  1 kirillkravcov  staff  373 Jan  8 19:41 T-MASTER.md
-rw-r--r--  1 kirillkravcov  staff  228 Jan  8 19:55 T-NONHIER.md
kirillkravcov@MacBook-Air uch-docs % >....                                                                                                
    fi
done

echo -e "\n🔍 КРИТИЧЕСКИЙ АНАЛИЗ: ГДЕ ГЕНЕРИРУЕТСЯ И ЗАПИСЫВАЕТСЯ ID?"

# Смотрим конкретную логику генерации ID для иерархических документов
echo -e "\n1. Генерация ID для иерархических документов (уровни 1-4):"
grep -n -B5 -A5 "else.*# Иерархический документ" ./uch-scripts/document-creator.sh

echo -e "\n2. Проверка create_from_template - как передается ID:"
grep -n "create_from_template.*doc_id" ./uch-scripts/document-creator.sh -B2 -A2

🔍 ПРОВЕРКА ШАБЛОНОВ:
zsh: command not found: #
=== T-MASTER.md (первые 20 строк) ===
---
id: "{{id}}"
name: "{{name}}"
type: "{{type}}"
level: {{level}}
status: "{{status}}"
created: "{{created}}"
updated: "{{updated}}"
author: "{{author}}"
---

### {{name}}

#### ОПИСАНИЕ
Добавьте описание здесь.

#### ДОЧЕРНИЕ ДОКУМЕНТЫ
Пока нет дочерних документов.

---
...
=== T-CHILD.md (первые 20 строк) ===
---
id: "{{id}}"
name: "{{name}}"
type: "{{type}}"
level: {{level}}
status: "{{status}}"
created: "{{created}}"
updated: "{{updated}}"
author: "{{author}}"
---

### {{name}}

#### ОПИСАНИЕ
Добавьте описание здесь.


#### ДОЧЕРНИЕ ДОКУМЕНТЫ
Пока нет дочерних документов.

...
=== T-NONHIER.md (первые 20 строк) ===
---
id: "{{id}}"
name: "{{name}}"
type: "{{type}}"
level: "N"
status: "{{status}}"
created: "{{created}}"
updated: "{{updated}}"
author: "{{author}}"
---

### {{name}}

#### СОДЕРЖАНИЕ

---
Создано: {{created}}
...

🔍 КРИТИЧЕСКИЙ АНАЛИЗ: ГДЕ ГЕНЕРИРУЕТСЯ И ЗАПИСЫВАЕТСЯ ID?
zsh: command not found: #

1. Генерация ID для иерархических документов (уровни 1-4):

2. Проверка create_from_template - как передается ID:
124-            return 1
125-        fi
126:        create_from_template "$filename" "$doc_id" "$name" "N" "$type" \
127-            "" "" "$tags_yaml" "$current_date" "T-NONHIER.md"
128-    else
--
141-        fi
142-        
143:        create_from_template "$filename" "$doc_id" "$name" "$level" "$type" \
144-            "$parent_id" "$parent_name" "$tags_yaml" "$current_date" "$template_file"
145-    fi
kirillkravcov@MacBook-Air uch-docs % chmod +x test-id-flow.sh
./test-id-flow.sh
Тест 1: Генерация неиерархического ID
Сгенерированный ID: Z-20260203220200

Тест 2: Проверка функций из utils.sh
Текущая дата: 2026-02-03

Тест 3: Симуляция создания документа
Предполагаемый ID для уровня 1: 00 (свободный мастер ID)
Свободный мастер ID: 00

Тест 4: Проверка формата вывода
Если создается документ с ID '00-01-AB-1', то:
  - Имя файла: 00-01-AB-1_TYPE_slug.md
  - Frontmatter должен содержать: id: "00-01-AB-1"
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Отлично! Теперь я вижу **корень проблемы**. Давайте проанализируем:

## 🎯 **ПРОБЛЕМА ОБНАРУЖЕНА:**

1. **Генерация ID в системе:**
   - `find_free_master_id()` → возвращает `00` (без номера типа)
   - `find_free_child_id()` → возвращает `AB` (без номера типа) 
   - `doc_id` формируется как `"${parent_id}-${child_suffix}"` → `00-AB` (без номера типа)

2. **Формирование имени файла:**
   ```bash
   local filename="${doc_id} ${short_type} - ${name}.md"
   ```
   → `00-AB TYPE - name.md` (НЕПРАВИЛЬНО! Должно быть `00-AB-1_TYPE_name.md`)

3. **Шаблоны:** Правильно используют `{{id}}`, но передается неправильный ID (без номера типа)

## 🔧 **ИСПРАВЛЕНИЯ, КОТОРЫЕ НУЖНО СДЕЛАТЬ:**

### **1. Исправить генерацию ID (добавить номер типа):**
Функции должны генерировать ID **с номером типа**:
- Уровень 1: `00-1` (вместо `00`)
- Уровень 2-4: `00-AB-1` (вместо `00-AB`)
- Номер типа должен определяться по типу документа

### **2. Исправить формирование имени файла:**
Формат должен быть: `X-XXXXXX-X_TYPE_slug.md` или `X-XXXXXX-XXXX-X_TYPE_slug.md`

### **3. Обновить шаблоны** (они вроде правильные)

## 📝 **Создаем решение для задачи 0800:**

Давайте создадим решение, которое исправит модульную систему:

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/5-010400-0801-1_SOL_Исправление_модульной_системы_генерации_ID.md << "EOF"
---
id: "5-010400-0801-1"
name: "Исправление модульной системы генерации ID"
type: "SOL"
level: 5
status: "active"
tags: ["@docs", "@system", "@fix", "@modules"]
parent: "4-010400-0800"
parent_name: "Исправление скрипта uch-create.sh для генерации правильных id"
created: "2026-02-04"
updated: "2026-02-04"
author: "kirillkravcov"
slug: "5-010400-0801-1_SOL_Исправление_модульной_системы_генерации_ID"
---

### Решение: Исправление модульной системы генерации ID

#### ПРОБЛЕМА
Модульная система uch-create (через uch-scripts/) генерирует неправильные ID:
1. **ID без номера типа**: `00`, `00-AB` вместо `00-1`, `00-AB-1`
2. **Неправильный формат имени файла**: `00-AB TYPE - name.md` вместо `00-AB-1_TYPE_name.md`
3. **Несоответствие с системой нумерации** из документа `3-010402-1`

#### АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

**Модуль document-creator.sh:**
```bash
# Строка 113 - формирование имени файла:
local filename="${doc_id} ${short_type} - ${name}.md"
# Проблема: пробелы вместо подчеркиваний, нет номера типа в doc_id

# Строки 84, 101 - генерация ID:
doc_id=$(find_free_master_id)           # → "00" (без номера типа)
doc_id="${parent_id}-${child_suffix}"   # → "00-AB" (без номера типа)
```

**Модуль utils.sh:**
```bash
# find_free_master_id() → "00"
# find_free_child_id() → "AB"
# Нет функций для добавления номера типа
```

#### РЕШЕНИЕ

**Шаг 1: Добавить функцию определения номера типа по типу документа**

```bash
# В utils.sh добавить:
get_type_number() {
    local type="$1"
    local level="$2"
    
    # Согласно 3-010402-1:
    case "$level" in
        1)
            case "$type" in
                "project"|"proj") echo "1" ;;
                "vision") echo "2" ;;
                "strategy") echo "3" ;;
                "roadmap") echo "4" ;;
                "business") echo "5" ;;
                "brand") echo "6" ;;
                *) echo "1" ;; # По умолчанию
            esac
            ;;
        2)
            case "$type" in
                "line") echo "1" ;;
                "platform") echo "2" ;;
                "service") echo "3" ;;
                "tool") echo "4" ;;
                "library") echo "5" ;;
                "application") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        # ... и так для всех уровней
        4)
            case "$type" in
                "task") echo "1" ;;
                "feature") echo "2" ;;
                "research") echo "3" ;;
                "test") echo "4" ;;
                "improvement") echo "5" ;;
                "refactoring") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        *)
            # Для общих типов (ARCH, DOC, SPEC и т.д.)
            case "$type" in
                "architecture"|"arch") echo "7" ;;
                "documentation"|"doc") echo "8" ;;
                "specification"|"spec") echo "9" ;;
                "design") echo "10" ;;
                "plan") echo "11" ;;
                # ... другие общие типы
                *) echo "99" ;; # OTHER
            esac
            ;;
    esac
}
```

**Шаг 2: Обновить функции генерации ID**

```bash
# В utils.sh обновить:
generate_full_id() {
    local base_id="$1"
    local type="$2"
    local level="$3"
    
    type_number=$(get_type_number "$type" "$level")
    echo "${base_id}-${type_number}"
}
```

**Шаг 3: Обновить document-creator.sh**

```bash
# В create_real_document() заменить:
# Строка 84:
doc_id=$(find_free_master_id)
# На:
base_id=$(find_free_master_id)
doc_id=$(generate_full_id "$base_id" "$type" "$level")

# Строка 101:
doc_id="${parent_id}-${child_suffix}"
# На:
base_id="${parent_id}-${child_suffix}"
doc_id=$(generate_full_id "$base_id" "$type" "$level")

# Строка 113:
local filename="${doc_id} ${short_type} - ${name}.md"
# На:
local filename="${doc_id}_${short_type}_${name// /_}.md"
```

**Шаг 4: Обновить другие модули**
- `manual-mode.sh` - ручное создание с ID
- `replace-shift.sh` - операции замены/смещения
- `batch-mode.sh` - пакетное создание

#### ТЕСТИРОВАНИЕ

**Тест 1: Создание мастер-документа**
```bash
# Должно создать: 00-1_PROJ_project_name.md
# Frontmatter: id: "00-1"
```

**Тест 2: Создание задачи**
```bash
# Должно создать: 00-01-AB-1_TASK_task_name.md  
# Frontmatter: id: "00-01-AB-1"
```

**Тест 3: Создание архитектурного документа**
```bash
# Должно создать: 00-01-AB-7_ARCH_architecture_name.md
# Frontmatter: id: "00-01-AB-7"
```

#### КРИТЕРИИ УСПЕХА
- [ ] Все новые документы создаются с правильным форматом ID
- [ ] Имена файлов соответствуют `X-XXXXXX-X_TYPE_slug.md`
- [ ] Frontmatter содержит полный ID (с номером типа)
- [ ] Обратная совместимость с существующими документами

#### РИСКИ И МИТИГАЦИЯ
1. **Риск**: Существующие ссылки сломаются
   **Митигация**: Использовать `fix-id-consistency.sh` для обновления ссылок

2. **Риск**: Сложность реализации для всех типов
   **Митигация**: Начать с основных типов (PROJ, LINE, COMP, TASK, ARCH, DOC)

3. **Риск**: Производительность
   **Митигация**: Кэшировать mapping типов-номеров

#### СЛЕДУЮЩИЕ ШАГИ
1. Реализовать `get_type_number()` в `utils.sh`
2. Обновить `document-creator.sh`
3. Протестировать создание документов
4. Обновить остальные модули
5. Создать миграционный скрипт для обновления формата существующих файлов

EOF'
```

## 🚀 **Шаг 1: Создаем исправленный utils.sh с функцией get_type_number**

Давайте создаем исправленную версию:

```bash
# Сначала backup текущего utils.sh
cp ./uch-scripts/utils.sh ./uch-scripts/utils.sh.backup

# Создаем патч для utils.sh
cat > ./uch-scripts/utils.sh.patch << 'EOF'
# Добавить после существующих функций:

# Функция для получения номера типа по типу и уровню
get_type_number() {
    local type="$1"
    local level="$2"
    
    # Приводим type к lowercase для сравнения
    type_lower=$(echo "$type" | tr '[:upper:]' '[:lower:]')
    
    # Согласно системе нумерации из 3-010402-1
    case "$level" in
        1)
            # Уровень 1: Проекты/Стратегия
            case "$type_lower" in
                "project"|"prod"|"proj") echo "1" ;;
                "vision") echo "2" ;;
                "strategy"|"strat") echo "3" ;;
                "roadmap"|"road") echo "4" ;;
                "business"|"bus") echo "5" ;;
                "brand") echo "6" ;;
                *) echo "1" ;; # По умолчанию PROJ
            esac
            ;;
        2)
            # Уровень 2: Линии развития
            case "$type_lower" in
                "line") echo "1" ;;
                "platform"|"plat") echo "2" ;;
                "service"|"serv") echo "3" ;;
                "tool") echo "4" ;;
                "library"|"lib") echo "5" ;;
                "application"|"app") echo "6" ;;
                *) echo "1" ;; # По умолчанию LINE
            esac
            ;;
        3)
            # Уровень 3: Компоненты
            case "$type_lower" in
                "component"|"comp") echo "1" ;;
                "module"|"mod") echo "2" ;;
                "system"|"sys") echo "3" ;;
                "api") echo "4" ;;
                "database"|"db") echo "5" ;;
                "infrastructure"|"infra") echo "6" ;;
                *) echo "1" ;; # По умолчанию COMP
            esac
            ;;
        4)
            # Уровень 4: Задачи
            case "$type_lower" in
                "task") echo "1" ;;
                "feature"|"feat") echo "2" ;;
                "research"|"res") echo "3" ;;
                "test") echo "4" ;;
                "improvement"|"improv") echo "5" ;;
                "refactoring"|"ref") echo "6" ;;
                *) echo "1" ;; # По умолчанию TASK
            esac
            ;;
        5)
            # Уровень 5: Решения
            case "$type_lower" in
                "solution"|"sol") echo "1" ;;
                "code") echo "2" ;;
                "bug") echo "3" ;;
                "algorithm"|"alg") echo "4" ;;
                "configuration"|"conf") echo "5" ;;
                "script") echo "6" ;;
                *) echo "1" ;; # По умолчанию SOL
            esac
            ;;
        6)
            # Уровень 6: Отчеты
            case "$type_lower" in
                "report") echo "1" ;;
                "metric") echo "2" ;;
                "analytics"|"analyt") echo "3" ;;
                "log") echo "4" ;;
                "backup") echo "5" ;;
                "audit") echo "6" ;;
                *) echo "1" ;; # По умолчанию REPORT
            esac
            ;;
        *)
            # Общие типы (номера 7-99) - для любого уровня
            case "$type_lower" in
                "architecture"|"arch") echo "7" ;;
                "documentation"|"doc") echo "8" ;;
                "specification"|"spec") echo "9" ;;
                "design") echo "10" ;;
                "plan") echo "11" ;;
                "process"|"proc") echo "12" ;;
                "review"|"rev") echo "13" ;;
                "tutorial"|"tut") echo "14" ;;
                "guide") echo "15" ;;
                "kanban") echo "16" ;;
                "tech-debt"|"debt") echo "17" ;;
                "migration"|"mig") echo "18" ;;
                # ... можно добавить другие
                *) echo "99" ;; # OTHER
            esac
            ;;
    esac
}

# Функция для генерации полного ID с номером типа
generate_full_id() {
    local base_id="$1"
    local type="$2"
    local level="$3"
    
    type_number=$(get_type_number "$type" "$level")
    echo "${base_id}-${type_number}"
}
EOF

# Применяем патч к utils.sh
cat ./uch-scripts/utils.sh.patch >> ./uch-scripts/utils.sh

echo "✅ utils.sh обновлен с функциями get_type_number и generate_full_id"
```

**Выполните эти команды, чтобы обновить utils.sh.** Затем мы будем обновлять document-creator.sh.

---

## 👤 **Kirill**

погоди. вся старая структура id не правильная. Ее не нужно пытаться переиспользовать. 

Не правильно. 
# Должно создать: 00-1_PROJ_project_name.md
# Frontmatter: id: "00-1"

Руководствуйся системой нумерации 
Вот же ты очень хорошо разобрался

Давайте разберемся правильно с нумерацией согласно системе 3-010402-1:

📚 Правила нумерации из документа 3-010402-1:
Уровень 5: РЕШЕНИЯ (пятый уровень в системе)

Типы: SOL (тип 1), CODE (тип 2), BUG (тип 3), ALG (тип 4), CONF (тип 5), SCRIPT (тип 6)

Порядковый номер типа: Уникален в рамках:

Уровня + Иерархии + Типа документа

✅ Правильная нумерация для задачи 0700:
Для задачи 4-010400-0700-1_TASK_... (родитель) создаются решения на уровне 5:

5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md

5 = уровень 5 (решения)

010400 = иерархия (линия документации)

0701 = номер решения (07 = задача, 01 = первое решение)

1 = номер типа (SOL - тип 1 на уровне 5)

5-010400-0701-2_CODE_Реализация_скрипта_корректировки_id.md ✅

5 = уровень 5 (решения)

010400 = иерархия

0701 = номер решения (то же решение, что и выше)

2 = номер типа (CODE - тип 2 на уровне 5)

🎯 Уточненный скоуп с правильной нумерацией:
Уровень 4: Задачи исправления системы
4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md

4-010400-0800-1_TASK_Исправление_скрипта_uch-create.sh_для_генерации_правильных_id.md

Уровень 5: Решения для задачи 0700
5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md

5-010400-0701-2_CODE_Реализация_скрипта_корректировки_id.md

---

## 🤖 **DeepSeek**

Вы абсолютно правы! Я запутался и попытался исправить старую неправильную систему, вместо того чтобы реализовать новую правильную систему из документа `3-010402-1`.

Давайте начнем с чистого листа и реализуем **правильную систему нумерации**:

## 🎯 **ПРАВИЛЬНАЯ СИСТЕМА НУМЕРАЦИИ (из 3-010402-1):**

### **ФОРМАТЫ:**
- **Уровни 1-3:** `X-XXXXXX-X_TYPE_slug.md`
  - Пример: `2-010400-7_ARCH_Documentation-first_подход.md`
  
- **Уровни 4-5:** `X-XXXXXX-XXXX-X_TYPE_slug.md`
  - Пример: `4-010400-0700-1_TASK_Аудит_несоответствий_id_и_имени_файла.md`
  - Пример: `5-010400-0701-1_SOL_Алгоритм_автоматической_корректировки_id.md`

- **Уровень 6:** `X-XXXXXX-XXXX-X_TYPE_YYYYMMDD.md`
  - Пример: `6-010400-0001-1_REPORT_20250129.md`

### **КОМПОНЕНТЫ ID:**
1. **Уровень** (1-6, Z)
2. **Иерархия** (010000, 010400, etc.)
3. **Номер в иерархии** (0001, 0100, 0700, etc.)
4. **Номер типа** (1-6 для уровневых типов, 7-99 для общих типов)

## 🔧 **ПЕРЕПИСЫВАЕМ СИСТЕМУ ГЕНЕРАЦИИ:**

Нужно **полностью переписать** генерацию ID в модульной системе. Старая система (00, 00-AB) **неправильная**.

### **Шаг 1: Новая архитектура генерации ID**

```bash
# НОВЫЙ ПОДХОД:

# 1. Мастер-документ (уровень 1):
#    ID: 1-010000-1 (не 00!)
#    Имя: 1-010000-1_PROD_universal_creative_hub.md

# 2. Линия документации (уровень 2):
#    ID: 2-010400-1 (не 00-01!)
#    Имя: 2-010400-1_LINE_documentation_system.md

# 3. Задача (уровень 4):
#    ID: 4-010400-0700-1 (не 00-01-AB!)
#    Имя: 4-010400-0700-1_TASK_аудит_несоответствий.md

# 4. Решение (уровень 5):
#    ID: 5-010400-0701-1
#    Имя: 5-010400-0701-1_SOL_алгоритм_корректировки.md
```

## 📝 **Создаем ПРАВИЛЬНОЕ решение:**

```bash
bash -c 'cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/5-010400-0801-1_SOL_Полная_переработка_генерации_ID_по_системе_нумерации.md << "EOF"
---
id: "5-010400-0801-1"
name: "Полная переработка генерации ID по системе нумерации"
type: "SOL"
level: 5
status: "active"
tags: ["@docs", "@system", "@core", "@refactor"]
parent: "4-010400-0800"
parent_name: "Исправление скрипта uch-create.sh для генерации правильных id"
created: "2026-02-04"
updated: "2026-02-04"
author: "kirillkravcov"
slug: "5-010400-0801-1_SOL_Полная_переработка_генерации_ID_по_системе_нумерации"
---

### Решение: Полная переработка генерации ID согласно системе 3-010402-1

#### ПРОБЛЕМА
Текущая модульная система использует **старую, неправильную систему нумерации**:
- `00`, `00-AB`, `00-01-AB` (шестнадцатеричная, непонятная)
- Не соответствует документу `3-010402-1`
- Нет разделения на уровни, иерархию, номера, типы

#### ЦЕЛЬ
Реализовать **новую систему генерации ID** полностью соответствующую `3-010402-1`:
- Форматы: `X-XXXXXX-X_TYPE_slug.md` и `X-XXXXXX-XXXX-X_TYPE_slug.md`
- Четкая структура: Уровень-Иерархия-Номер-Тип
- Понятная десятичная нумерация вместо шестнадцатеричной

#### НОВАЯ АРХИТЕКТУРА ГЕНЕРАЦИИ

**1. Уровни и их назначение:**
```
Уровень 1: Проекты          (1-010000-X)
Уровень 2: Линии            (2-XXXXXX-X)  
Уровень 3: Компоненты       (3-XXXXXX-X)
Уровень 4: Задачи           (4-XXXXXX-XXXX-X)
Уровень 5: Решения          (5-XXXXXX-XXXX-X)
Уровень 6: Отчеты           (6-XXXXXX-XXXX-X)
Уровень Z: Zettel-идеи      (Z-YYMMDD-HHMM-XXXX-X)
```

**2. Примеры правильных ID:**
```
1-010000-1      # Проект UCH (PROD, тип 1)
2-010400-1      # Линия документации (LINE, тип 1)
2-010400-7      # Архитектура линии (ARCH, тип 7)
3-010401-1      # Компонент системы нумерации (COMP, тип 1)
4-010400-0700-1 # Задача аудита (TASK, тип 1)
5-010400-0701-1 # Решение переработки (SOL, тип 1)
5-010400-0701-2 # Код реализации (CODE, тип 2)
```

#### ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

**Шаг 1: Создать новый модуль `id-generator.sh`**

```bash
#!/bin/bash
# id-generator.sh - Генерация ID по системе 3-010402-1

# Константы иерархий
UCH_PROJECT_HIERARCHY="010000"
DOCS_LINE_HIERARCHY="010400"
NUMBERING_COMPONENT_HIERARCHY="010401"

# Генерация ID для уровня 1 (проекты)
generate_level1_id() {
    local type_number="$1"  # 1=PROD, 2=VISION, 3=STRAT, etc.
    local project_number="$2"  # 01, 02, 03...
    
    # Формат: 1-010000-X
    echo "1-${UCH_PROJECT_HIERARCHY}-${project_number}${type_number}"
}

# Генерация ID для уровня 2 (линии)
generate_level2_id() {
    local hierarchy="$1"    # 010400, 010200, etc.
    local line_number="$2"  # 01, 02, 03...
    local type_number="$3"  # 1=LINE, 2=PLAT, 3=SERV, 7=ARCH, 8=DOC, etc.
    
    # Формат: 2-XXXXXX-X
    echo "2-${hierarchy}-${line_number}${type_number}"
}

# Генерация ID для уровня 3 (компоненты)
generate_level3_id() {
    local parent_id="$1"    # ID родителя (2-010400-1)
    local component_number="$2"  # 01, 02, 03...
    local type_number="$3"  # 1=COMP, 2=MOD, 3=SYS, 7=ARCH, etc.
    
    # Извлекаем иерархию из родителя
    local hierarchy=$(echo "$parent_id" | cut -d'-' -f2)
    
    # Формат: 3-XXXXXX-X
    echo "3-${hierarchy}-${component_number}${type_number}"
}

# Генерация ID для уровня 4 (задачи)
generate_level4_id() {
    local parent_id="$1"    # ID родителя (3-010401-1)
    local task_number="$2"  # 0100, 0200, 0300...
    local type_number="$3"  # 1=TASK, 2=FEAT, 3=RES, etc.
    
    # Извлекаем иерархию из родителя
    local hierarchy=$(echo "$parent_id" | cut -d'-' -f2)
    
    # Формат: 4-XXXXXX-XXXX-X
    echo "4-${hierarchy}-${task_number}-${type_number}"
}

# Генерация ID для уровня 5 (решения)
generate_level5_id() {
    local parent_id="$1"    # ID родителя (4-010400-0700-1)
    local solution_number="$2"  # 01, 02, 03...
    local type_number="$3"  # 1=SOL, 2=CODE, 3=BUG, etc.
    
    # Извлекаем иерархию и номер задачи
    local hierarchy=$(echo "$parent_id" | cut -d'-' -f2)
    local task_number=$(echo "$parent_id" | cut -d'-' -f3)
    
    # Формат: 5-XXXXXX-XXXX-X
    echo "5-${hierarchy}-${task_number}${solution_number}-${type_number}"
}

# Генерация имени файла
generate_filename() {
    local id="$1"
    local type="$2"
    local name="$3"
    local level="$4"
    
    # Преобразуем имя в slug
    local slug=$(echo "$name" | tr ' ' '_' | tr '[:upper:]' '[:lower:]' | tr -cd '[:alnum:]_')
    
    # Определяем short type
    local short_type=$(get_short_type "$type")
    
    # Формируем имя файла
    if [ "$level" -eq 6 ]; then
        # Уровень 6: с датой вместо slug
        local date_part=$(date +%Y%m%d)
        echo "${id}_${short_type}_${date_part}.md"
    else
        # Уровни 1-5: со slug
        echo "${id}_${short_type}_${slug}.md"
    fi
}
```

**Шаг 2: Обновить `document-creator.sh` для использования новой системы**

```bash
# В create_real_document() заменить всю логику генерации ID:

# Старая (неправильная):
if [ "$level" = "N" ]; then
    doc_id=$(generate_non_hierarchical_id "$type")
else
    if [ -z "$parent_id" ] || [ "$level" -eq 1 ]; then
        doc_id=$(find_free_master_id)  # → "00" (НЕПРАВИЛЬНО!)
    else
        child_suffix=$(find_free_child_id "$parent_id")
        doc_id="${parent_id}-${child_suffix}"  # → "00-AB" (НЕПРАВИЛЬНО!)
    fi
fi

# Новая (правильная):
if [ "$level" = "N" ]; then
    doc_id=$(generate_non_hierarchical_id "$type")
else
    case "$level" in
        1) doc_id=$(generate_level1_id "$type_number" "01") ;;
        2) doc_id=$(generate_level2_id "$hierarchy" "01" "$type_number") ;;
        3) doc_id=$(generate_level3_id "$parent_id" "01" "$type_number") ;;
        4) doc_id=$(generate_level4_id "$parent_id" "0100" "$type_number") ;;
        5) doc_id=$(generate_level5_id "$parent_id" "01" "$type_number") ;;
        6) doc_id=$(generate_level6_id "$parent_id" "0001" "$type_number") ;;
    esac
fi

# И обновить формирование имени файла:
# Было: local filename="${doc_id} ${short_type} - ${name}.md"
# Стало: local filename=$(generate_filename "$doc_id" "$type" "$name" "$level")
```

**Шаг 3: Обновить `utils.sh` - удалить старые функции**
- Удалить `find_free_master_id()` (шестнадцатеричная система)
- Удалить `find_free_child_id()` (шестнадцатеричная система)
- Добавить новые вспомогательные функции

**Шаг 4: Создать систему управления номерами**
```bash
# number-manager.sh - Управление последовательными номерами
get_next_number() {
    local level="$1"
    local hierarchy="$2"
    local type="$3"
    
    # Хранить в SQLite или файле последние использованные номера
    # Для уровня 4: 0100, 0200, 0300...
    # Для уровня 5: 01, 02, 03...
}
```

#### МИГРАЦИЯ СУЩЕСТВУЮЩИХ ДОКУМЕНТОВ

**План миграции:**
1. Создать mapping старых ID → новых ID
   - `00` → `1-010000-1`
   - `00-01` → `2-010400-1`
   - `00-01-AB` → `3-010401-1`
   - `00-01-AB-1` → `4-010401-0100-1`

2. Создать миграционный скрипт
3. Обновить все ссылки между документами

#### ТЕСТИРОВАНИЕ

**Тест 1: Создание проекта UCH**
```bash
# Должно создать: 1-010000-1_PROD_universal_creative_hub.md
# Frontmatter: id: "1-010000-1"
```

**Тест 2: Создание линии документации**
```bash
# Должно создать: 2-010400-1_LINE_documentation_system.md
# Frontmatter: id: "2-010400-1"
```

**Тест 3: Создание задачи**
```bash
# Должно создать: 4-010400-0700-1_TASK_аудит_несоответствий.md
# Frontmatter: id: "4-010400-0700-1"
```

#### РИСКИ
1. **Высокая сложность**: Полная переработка системы
2. **Миграция**: Нужно преобразовать все существующие документы
3. **Совместимость**: Старые скрипты перестанут работать

#### АЛЬТЕРНАТИВА
Если полная переработка слишком сложна, можно:
1. Оставить старую систему для существующих документов
2. Для новых документов использовать новую систему
3. Постепенно мигрировать

#### РЕКОМЕНДАЦИЯ
Начать с MVP:
1. Реализовать `id-generator.sh` с основными функциями
2. Обновить `document-creator.sh` для поддержки новой системы
3. Протестировать создание новых документов
4. Постепенно мигрировать существующие

EOF'
```

## 🤔 **Вопрос: Какой подход выбрать?**

**Вариант А: Полная переработка (рекомендую)**
- ✅ Чистая, понятная система
- ✅ Соответствие документации
- ❌ Сложная миграция
- ❌ Высокие риски

**Вариант Б: Гибридный подход**
- ✅ Меньше изменений
- ✅ Постепенная миграция  
- ❌ Две системы параллельно
- ❌ Путаница

**Вариант В: Исправить только генерацию имен файлов**
- ✅ Минимальные изменения
- ✅ Быстрое решение
- ❌ Не решает корневую проблему
- ❌ ID в frontmatter останутся неправильными

**Что выберем?** Полная переработка даст чистую систему на будущее, но требует больше работы.

---

## 👤 **Kirill**

Так миграция и не нужна. Все текущие документы уже исправлены. Система нужна для новых документов

---

## 🤖 **DeepSeek**

Отлично! Если все существующие документы уже исправлены и используют **правильную систему нумерации** (как в `3-010402-1`), то задача упрощается!

Нужно **только обновить модульную систему** для генерации новых документов с правильными ID.

## 🎯 **УПРОЩЕННАЯ ЗАДАЧА:**

Исправить модульную систему (`uch-scripts/`) чтобы она создавала новые документы с **правильными ID** по системе `3-010402-1`.

### **Текущее состояние:**
- Существующие документы: ✅ Правильные ID (например, `2-010400-7_ARCH_...`)
- Модульная система: ❌ Генерирует неправильные ID (`00`, `00-AB`)

### **Цель:**
Модульная система должна генерировать ID как:
- `1-010000-1`, `2-010400-1`, `4-010400-0700-1`, etc.

## 🔧 **ПРАВИЛЬНЫЙ ПОДХОД:**

**Шаг 1: Определить какую иерархию использовать для новых документов**

Посмотрим на существующие документы:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 АНАЛИЗ СУЩЕСТВУЮЩИХ ID ДЛЯ ОПРЕДЕЛЕНИЯ ПАТТЕРНОВ:"
echo "===================================================="

# Находим несколько примеров каждого уровня
find . -name "*.md" -type f ! -path "*/backup-*" ! -path "*/archive/*" | \
  xargs grep -l "^id:" | head -20 | while read file; do
    filename=$(basename "$file")
    id=$(grep -m1 "^id:" "$file" | sed 's/^id: *"//' | sed 's/"//')
    level=$(echo "$id" | cut -d'-' -f1)
    
    echo "📄 $filename"
    echo "   ID: $id"
    echo "   Уровень: $level"
    
    # Анализируем структуру ID
    if [[ "$id" =~ ^[0-9]-[0-9]{6}-[0-9]+(-[0-9]+)?$ ]]; then
        echo "   ✅ Правильный формат"
        hierarchy=$(echo "$id" | cut -d'-' -f2)
        echo "   Иерархия: $hierarchy"
    else
        echo "   ❌ Неправильный формат"
    fi
    echo "---"
done | head -30
```

**Шаг 2: Создать упрощенный `id-generator.sh` для новой системы**

```bash
cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/uch-scripts/id-generator-v2.sh << 'EOF'
#!/bin/bash
# id-generator-v2.sh - Упрощенная генерация ID по системе 3-010402-1
# Используется для создания новых документов

# Константы иерархий (брать из существующих документов)
UCH_PROJECT_HIERARCHY="010000"      # Проект UCH
DOCS_LINE_HIERARCHY="010400"        # Линия документации
BLOG_LINE_HIERARCHY="010100"        # Линия блога (предположительно)
STUDIO_LINE_HIERARCHY="010200"      # Линия студии (предположительно)

# Получить следующий номер для уровня
get_next_number_for_level() {
    local level="$1"
    local hierarchy="$2"
    local type_number="$3"
    
    # Для простоты - используем простую нумерацию
    # В реальной системе нужно искать существующие документы
    
    case "$level" in
        1) echo "1" ;;      # Проекты: 1-010000-1, 1-010000-2
        2) echo "1" ;;      # Линии: 2-010400-1, 2-010500-1
        3) echo "1" ;;      # Компоненты: 3-010401-1, 3-010401-2
        4) 
            # Для задач: 0100, 0200, 0300...
            # Найти максимальный номер задачи в этой иерархии
            find . -name "4-${hierarchy}-*-*.md" -type f 2>/dev/null | \
                sed "s/.*4-${hierarchy}-//" | cut -d'-' -f1 | \
                sort -n | tail -1 | while read last; do
                    if [ -n "$last" ]; then
                        next=$((last + 100))
                        printf "%04d" $next
                    else
                        echo "0100"  # Первая задача
                    fi
                done
            ;;
        5)
            # Для решений: 01, 02, 03...
            # Нужен parent_id (задача) чтобы определить номер
            echo "01"  # По умолчанию
            ;;
        6) echo "001" ;;    # Отчеты: 001, 002, 003
        *) echo "01" ;;
    esac
}

# Генерация ID для нового документа
generate_new_id() {
    local level="$1"
    local hierarchy="$2"
    local type_number="$3"
    local parent_id="$4"  # Опционально, для уровней 3-6
    
    # Определяем номер в иерархии
    local number=$(get_next_number_for_level "$level" "$hierarchy" "$type_number")
    
    # Формируем ID в зависимости от уровня
    case "$level" in
        1|2|3)
            # Уровни 1-3: X-XXXXXX-X
            echo "${level}-${hierarchy}-${number}${type_number}"
            ;;
        4|5|6)
            # Уровни 4-6: X-XXXXXX-XXXX-X
            # Для уровня 5 нужно извлечь номер задачи из parent_id
            if [ "$level" -eq 5 ] && [ -n "$parent_id" ]; then
                task_number=$(echo "$parent_id" | cut -d'-' -f3)
                echo "${level}-${hierarchy}-${task_number}${number}-${type_number}"
            else
                echo "${level}-${hierarchy}-${number}-${type_number}"
            fi
            ;;
        *)
            echo "Ошибка: Неизвестный уровень $level"
            return 1
            ;;
    esac
}

# Определить иерархию по родительскому ID или контексту
determine_hierarchy() {
    local parent_id="$1"
    local level="$2"
    
    if [ -n "$parent_id" ]; then
        # Берем иерархию из родителя
        echo "$parent_id" | cut -d'-' -f2
    else
        # Нет родителя - выбираем по уровню
        case "$level" in
            1) echo "$UCH_PROJECT_HIERARCHY" ;;
            2) echo "$DOCS_LINE_HIERARCHY" ;;  # По умолчанию линия документации
            3) echo "$DOCS_LINE_HIERARCHY" ;;
            *) echo "$DOCS_LINE_HIERARCHY" ;;
        esac
    fi
}

# Определить номер типа по типу и уровню
determine_type_number() {
    local type="$1"
    local level="$2"
    
    # Приводим к lowercase
    type_lower=$(echo "$type" | tr '[:upper:]' '[:lower:]')
    
    # Уровневые типы (1-6)
    case "$level" in
        1)
            case "$type_lower" in
                "project"|"prod"|"proj") echo "1" ;;
                "vision") echo "2" ;;
                "strategy"|"strat") echo "3" ;;
                "roadmap"|"road") echo "4" ;;
                "business"|"bus") echo "5" ;;
                "brand") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        2)
            case "$type_lower" in
                "line") echo "1" ;;
                "platform"|"plat") echo "2" ;;
                "service"|"serv") echo "3" ;;
                "tool") echo "4" ;;
                "library"|"lib") echo "5" ;;
                "application"|"app") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        3)
            case "$type_lower" in
                "component"|"comp") echo "1" ;;
                "module"|"mod") echo "2" ;;
                "system"|"sys") echo "3" ;;
                "api") echo "4" ;;
                "database"|"db") echo "5" ;;
                "infrastructure"|"infra") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        4)
            case "$type_lower" in
                "task") echo "1" ;;
                "feature"|"feat") echo "2" ;;
                "research"|"res") echo "3" ;;
                "test") echo "4" ;;
                "improvement"|"improv") echo "5" ;;
                "refactoring"|"ref") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        5)
            case "$type_lower" in
                "solution"|"sol") echo "1" ;;
                "code") echo "2" ;;
                "bug") echo "3" ;;
                "algorithm"|"alg") echo "4" ;;
                "configuration"|"conf") echo "5" ;;
                "script") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        6)
            case "$type_lower" in
                "report") echo "1" ;;
                "metric") echo "2" ;;
                "analytics"|"analyt") echo "3" ;;
                "log") echo "4" ;;
                "backup") echo "5" ;;
                "audit") echo "6" ;;
                *) echo "1" ;;
            esac
            ;;
        *)
            # Общие типы (7-99)
            case "$type_lower" in
                "architecture"|"arch") echo "7" ;;
                "documentation"|"doc") echo "8" ;;
                "specification"|"spec") echo "9" ;;
                "design") echo "10" ;;
                "plan") echo "11" ;;
                "process"|"proc") echo "12" ;;
                "review"|"rev") echo "13" ;;
                "tutorial"|"tut") echo "14" ;;
                "guide") echo "15" ;;
                "kanban") echo "16" ;;
                "tech-debt"|"debt") echo "17" ;;
                "migration"|"mig") echo "18" ;;
                *) echo "99" ;;  # OTHER
            esac
            ;;
    esac
}

# Основная функция для использования в других модулях
create_id_for_new_document() {
    local level="$1"
    local type="$2"
    local parent_id="$3"
    
    # Определяем иерархию
    local hierarchy=$(determine_hierarchy "$parent_id" "$level")
    
    # Определяем номер типа
    local type_number=$(determine_type_number "$type" "$level")
    
    # Генерируем ID
    generate_new_id "$level" "$hierarchy" "$type_number" "$parent_id"
}
EOF
```

**Шаг 3: Создать патч для `document-creator.sh`**

```bash
# Создаем патч для обновления document-creator.sh
cat > /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/update-document-creator.patch << 'EOF'
--- document-creator.sh.original	2026-02-04 00:00:00
+++ document-creator.sh	2026-02-04 00:00:01
@@ -1,4 +1,7 @@
 #!/bin/bash
+
+source "$SCRIPT_DIR/id-generator-v2.sh"
+
 # Модуль реального создания документов - ТОЛЬКО внешние шаблоны
 
 # Найти файл документа по ID в frontmatter
@@ -76,36 +79,16 @@
     local parent_name=""
     local parent_file=""
     
-    # 1. Определяем ID
+    # 1. Определяем ID по новой системе
     if [ "$level" = "N" ]; then
         # Неиерархический документ
         doc_id=$(generate_non_hierarchical_id "$type")
     else
-        # Иерархический документ
-        if [ -z "$parent_id" ] || [ "$level" -eq 1 ]; then
-            # Мастер-документ
-            doc_id=$(find_free_master_id)
-            echo "🆔 Сгенерирован ID: $doc_id"
-        else
-            # Дочерний документ
-            parent_file=$(find_document_by_id "$parent_id")
-            if [ -z "$parent_file" ]; then
-                echo "❌ Ошибка: Родительский документ с ID '$parent_id' не найден!"
-                return 1
-            fi
-            
-            parent_name=$(get_name_from_frontmatter "$parent_file")
-            if [ -z "$parent_name" ]; then
-                parent_name=$(basename "$parent_file" .md | sed "s/^${parent_id} - //")
-            fi
-            
-            # Генерируем свободный ID
-            child_suffix=$(find_free_child_id "$parent_id")
-            doc_id="${parent_id}-${child_suffix}"
-            echo "🆔 Сгенерирован ID: $doc_id (свободный: $child_suffix)"
-        fi
+        # Используем новую систему генерации ID
+        doc_id=$(create_id_for_new_document "$level" "$type" "$parent_id")
+        echo "🆔 Сгенерирован ID по новой системе: $doc_id"
     fi
-    
+
     # 2. Форматируем теги
     local tags_yaml=$(format_tags_yaml "$tags" "$type")
     echo "🏷️  Сформированные теги:"
@@ -113,7 +96,8 @@
     
     # 3. Создаем имя файла в правильном формате
     local short_type=$(get_short_type "$type")
-    local filename="${doc_id} ${short_type} - ${name}.md"
+    local slug=$(echo "$name" | tr ' ' '_' | tr '[:upper:]' '[:lower:]' | tr -cd '[:alnum:]_')
+    local filename="${doc_id}_${short_type}_${slug}.md"
     
     echo "📄 Создаю документ: $filename"
     
EOF
```

## 🚀 **Быстрое решение для тестирования:**

Давайте создадим упрощенную версию прямо сейчас для тестирования:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# 1. Создаем простой id-generator для теста
cat > test-new-id-generator.sh << 'EOF'
#!/bin/bash
# Тест новой системы генерации ID

# Простая функция для демонстрации
generate_test_id() {
    local level="$1"
    local type="$2"
    local parent_id="$3"
    
    case "$level" in
        1) echo "1-010000-1" ;;  # PROJ
        2) echo "2-010400-1" ;;  # LINE  
        3) echo "3-010401-1" ;;  # COMP
        4) 
            if [ -n "$parent_id" ]; then
                hierarchy=$(echo "$parent_id" | cut -d'-' -f2)
                echo "4-${hierarchy}-0100-1"  # TASK
            else
                echo "4-010400-0100-1"
            fi
            ;;
        5)
            if [ -n "$parent_id" ]; then
                hierarchy=$(echo "$parent_id" | cut -d'-' -f2)
                task_num=$(echo "$parent_id" | cut -d'-' -f3 | cut -c1-4)
                echo "5-${hierarchy}-${task_num}01-1"  # SOL
            else
                echo "5-010400-010001-1"
            fi
            ;;
        *) echo "ERROR" ;;
    esac
}

# Тест
echo "🧪 ТЕСТ НОВОЙ ГЕНЕРАЦИИ ID:"
echo "=========================="
echo "1. Уровень 1 (PROJ): $(generate_test_id 1 "project" "")"
echo "2. Уровень 2 (LINE): $(generate_test_id 2 "line" "")"
echo "3. Уровень 3 (COMP): $(generate_test_id 3 "component" "2-010400-1")"
echo "4. Уровень 4 (TASK): $(generate_test_id 4 "task" "3-010401-1")"
echo "5. Уровень 5 (SOL):  $(generate_test_id 5 "solution" "4-010400-0100-1")"
EOF

chmod +x test-new-id-generator.sh
./test-new-id-generator.sh
```

**Что вам проще:**
1. **Создать полностью новый модуль `id-generator-v2.sh`** и обновить `document-creator.sh`
2. **Встроить простую логику прямо в `document-creator.sh`** для быстрого исправления
3. **Создать альтернативный скрипт создания** только для новых документов

**Рекомендую вариант 2** - быстрое исправление с минимальными изменениями. Вы согласны?

---

## 👤 **Kirill**

Пойдем по первому варианту

---

## 🤖 **DeepSeek**

Отлично! Идем по первому варианту - создаем полностью новый модуль `id-generator-v2.sh` и обновляем систему.

## 🚀 **Шаг 1: Создаем новый модуль id-generator-v2.sh**

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# Создаем новый модуль
cat > ./uch-scripts/id-generator-v2.sh << 'EOF'
#!/bin/bash
# id-generator-v2.sh - Генерация ID по системе 3-010402-1
# Для создания новых документов в существующей системе

# ============================================================================
# КОНФИГУРАЦИЯ
# ============================================================================

# Иерархии из существующей системы
HIERARCHY_UCH_PROJECT="010000"    # Проект UCH
HIERARCHY_DOCS_LINE="010400"      # Линия документации
HIERARCHY_BLOG_LINE="010100"      # Линия блога
HIERARCHY_STUDIO_LINE="010200"    # Линия студии
HIERARCHY_NUMBERING="010401"      # Компонент системы нумерации

# ============================================================================
# ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
# ============================================================================

# Преобразовать тип в номер по системе 3-010402-1
get_type_number() {
    local type="$1"
    local level="$2"
    
    # Приводим к lowercase для сравнения
    type_lower=$(echo "$type" | tr '[:upper:]' '[:lower:]')
    
    # Уровневые типы (номера 1-6)
    case "$level" in
        1)
            # Уровень 1: Проекты/Стратегия
            case "$type_lower" in
                "project"|"prod"|"proj") echo "1" ;;
                "vision") echo "2" ;;
                "strategy"|"strat") echo "3" ;;
                "roadmap"|"road") echo "4" ;;
                "business"|"bus") echo "5" ;;
                "brand") echo "6" ;;
                *) echo "1" ;; # По умолчанию PROJ
            esac
            ;;
        2)
            # Уровень 2: Линии развития
            case "$type_lower" in
                "line") echo "1" ;;
                "platform"|"plat") echo "2" ;;
                "service"|"serv") echo "3" ;;
                "tool") echo "4" ;;
                "library"|"lib") echo "5" ;;
                "application"|"app") echo "6" ;;
                *) echo "1" ;; # По умолчанию LINE
            esac
            ;;
        3)
            # Уровень 3: Компоненты
            case "$type_lower" in
                "component"|"comp") echo "1" ;;
                "module"|"mod") echo "2" ;;
                "system"|"sys") echo "3" ;;
                "api") echo "4" ;;
                "database"|"db") echo "5" ;;
                "infrastructure"|"infra") echo "6" ;;
                *) echo "1" ;; # По умолчанию COMP
            esac
            ;;
        4)
            # Уровень 4: Задачи
            case "$type_lower" in
                "task") echo "1" ;;
                "feature"|"feat") echo "2" ;;
                "research"|"res") echo "3" ;;
                "test") echo "4" ;;
                "improvement"|"improv") echo "5" ;;
                "refactoring"|"ref") echo "6" ;;
                *) echo "1" ;; # По умолчанию TASK
            esac
            ;;
        5)
            # Уровень 5: Решения
            case "$type_lower" in
                "solution"|"sol") echo "1" ;;
                "code") echo "2" ;;
                "bug") echo "3" ;;
                "algorithm"|"alg") echo "4" ;;
                "configuration"|"conf") echo "5" ;;
                "script") echo "6" ;;
                *) echo "1" ;; # По умолчанию SOL
            esac
            ;;
        6)
            # Уровень 6: Отчеты
            case "$type_lower" in
                "report") echo "1" ;;
                "metric") echo "2" ;;
                "analytics"|"analyt") echo "3" ;;
                "log") echo "4" ;;
                "backup") echo "5" ;;
                "audit") echo "6" ;;
                *) echo "1" ;; # По умолчанию REPORT
            esac
            ;;
        *)
            # Общие типы (номера 7-99) - для любого уровня
            case "$type_lower" in
                "architecture"|"arch") echo "7" ;;
                "documentation"|"doc") echo "8" ;;
                "specification"|"spec") echo "9" ;;
                "design") echo "10" ;;
                "plan") echo "11" ;;
                "process"|"proc") echo "12" ;;
                "review"|"rev") echo "13" ;;
                "tutorial"|"tut") echo "14" ;;
                "guide") echo "15" ;;
                "kanban") echo "16" ;;
                "tech-debt"|"debt") echo "17" ;;
                "migration"|"mig") echo "18" ;;
                *) echo "99" ;; # OTHER
            esac
            ;;
    esac
}

# Определить иерархию по контексту
determine_hierarchy() {
    local parent_id="$1"
    local level="$2"
    local type="$3"
    
    # Если есть родитель - берем его иерархию
    if [ -n "$parent_id" ]; then
        echo "$parent_id" | cut -d'-' -f2
        return 0
    fi
    
    # Если нет родителя - определяем по уровню и типу
    case "$level" in
        1)
            # Уровень 1: всегда проект UCH
            echo "$HIERARCHY_UCH_PROJECT"
            ;;
        2)
            # Уровень 2: линии развития
            case "$type" in
                "line"|"documentation") echo "$HIERARCHY_DOCS_LINE" ;;
                "blog"|"platform") echo "$HIERARCHY_BLOG_LINE" ;;
                "studio"|"audio") echo "$HIERARCHY_STUDIO_LINE" ;;
                *) echo "$HIERARCHY_DOCS_LINE" ;; # По умолчанию
            esac
            ;;
        3|4|5|6)
            # Уровни 3-6: по умолчанию документация
            echo "$HIERARCHY_DOCS_LINE"
            ;;
        *)
            echo "$HIERARCHY_DOCS_LINE" # Фолбэк
            ;;
    esac
}

# Найти следующий доступный номер в иерархии
find_next_available_number() {
    local level="$1"
    local hierarchy="$2"
    local pattern_type="$3"  # "single" или "double"
    
    case "$level" in
        1|2|3)
            # Уровни 1-3: ищем максимальный номер типа X в X-XXXXXX-X
            find . -name "${level}-${hierarchy}-*.md" -type f ! -path "*/backup-*" 2>/dev/null | \
                sed "s/.*${level}-${hierarchy}-//" | cut -d'_' -f1 | \
                while read id_part; do
                    # Извлекаем номер (последние цифры)
                    echo "$id_part" | grep -o '[0-9]*$'
                done | sort -n | tail -1
            ;;
        4)
            # Уровень 4: ищем максимальный 4-значный номер в 4-XXXXXX-XXXX-X
            find . -name "4-${hierarchy}-*.md" -type f ! -path "*/backup-*" 2>/dev/null | \
                sed "s/.*4-${hierarchy}-//" | cut -d'-' -f1 | \
                sort -n | tail -1
            ;;
        5)
            # Уровень 5: ищем максимальный 2-значный номер решения
            # Нужен parent_id чтобы знать задачу
            echo "01"  # По умолчанию
            ;;
        6)
            # Уровень 6: ищем максимальный 3-значный номер отчета
            find . -name "6-${hierarchy}-*.md" -type f ! -path "*/backup-*" 2>/dev/null | \
                sed "s/.*6-${hierarchy}-//" | cut -d'-' -f1 | \
                sort -n | tail -1
            ;;
        *)
            echo "1"
            ;;
    esac
}

# ============================================================================
# ОСНОВНЫЕ ФУНКЦИИ ГЕНЕРАЦИИ
# ============================================================================

# Генерация ID для нового документа
generate_id() {
    local level="$1"
    local type="$2"
    local parent_id="$3"
    
    # 1. Определяем иерархию
    local hierarchy=$(determine_hierarchy "$parent_id" "$level" "$type")
    
    # 2. Определяем номер типа
    local type_number=$(get_type_number "$type" "$level")
    
    # 3. Определяем следующий доступный номер
    local next_number=""
    
    case "$level" in
        1|2|3)
            # Уровни 1-3: X-XXXXXX-X
            local last_num=$(find_next_available_number "$level" "$hierarchy" "single")
            if [ -z "$last_num" ]; then
                next_number="1"
            else
                next_number=$((last_num + 1))
            fi
            echo "${level}-${hierarchy}-${next_number}${type_number}"
            ;;
        4)
            # Уровень 4: X-XXXXXX-XXXX-X
            local last_task=$(find_next_available_number "4" "$hierarchy" "double")
            if [ -z "$last_task" ]; then
                next_number="0100"
            else
                next_number=$(printf "%04d" $((last_task + 100)))
            fi
            echo "4-${hierarchy}-${next_number}-${type_number}"
            ;;
        5)
            # Уровень 5: X-XXXXXX-XXXX-X (наследует номер задачи)
            if [ -z "$parent_id" ]; then
                echo "Ошибка: для уровня 5 нужен parent_id (задача)"
                return 1
            fi
            
            # Извлекаем номер задачи из parent_id
            local task_number=$(echo "$parent_id" | cut -d'-' -f3)
            local solution_num="01"  # По умолчанию первое решение
            
            # Можно улучшить: искать существующие решения для этой задачи
            echo "5-${hierarchy}-${task_number}${solution_num}-${type_number}"
            ;;
        6)
            # Уровень 6: X-XXXXXX-XXXX-X
            local last_report=$(find_next_available_number "6" "$hierarchy" "triple")
            if [ -z "$last_report" ]; then
                next_number="001"
            else
                next_number=$(printf "%03d" $((last_report + 1)))
            fi
            echo "6-${hierarchy}-${next_number}-${type_number}"
            ;;
        *)
            echo "Ошибка: Неизвестный уровень $level"
            return 1
            ;;
    esac
}

# Генерация имени файла в правильном формате
generate_filename() {
    local id="$1"
    local type="$2"
    local name="$3"
    local level="$4"
    
    # Преобразуем тип в короткий формат
    local short_type=""
    case "$type" in
        "project"|"prod"|"proj") short_type="PROD" ;;
        "line") short_type="LINE" ;;
        "component"|"comp") short_type="COMP" ;;
        "task") short_type="TASK" ;;
        "feature"|"feat") short_type="FEAT" ;;
        "solution"|"sol") short_type="SOL" ;;
        "code") short_type="CODE" ;;
        "architecture"|"arch") short_type="ARCH" ;;
        "documentation"|"doc") short_type="DOC" ;;
        "specification"|"spec") short_type="SPEC" ;;
        "report") short_type="REPORT" ;;
        "metric") short_type="METRIC" ;;
        "snapshot"|"snap") short_type="SNAP" ;;
        "idea") short_type="IDEA" ;;
        *) short_type="DOC" ;; # Фолбэк
    esac
    
    # Преобразуем имя в slug
    local slug=$(echo "$name" | \
        tr '[:upper:]' '[:lower:]' | \
        tr ' ' '_' | \
        sed 's/[^a-zA-Z0-9_]//g' | \
        sed 's/__*/_/g' | \
        sed 's/^_//' | sed 's/_$//')
    
    # Формируем имя файла
    if [ "$level" -eq 6 ]; then
        # Уровень 6: с датой
        local date_part=$(date +%Y%m%d)
        echo "${id}_${short_type}_${date_part}.md"
    else
        # Уровни 1-5: со slug
        echo "${id}_${short_type}_${slug}.md"
    fi
}

# ============================================================================
# ТЕСТОВЫЕ ФУНКЦИИ
# ============================================================================

# Протестировать генерацию
test_id_generation() {
    echo "🧪 ТЕСТ ГЕНЕРАЦИИ ID ПО СИСТЕМЕ 3-010402-1"
    echo "=========================================="
    
    echo "1. Проект UCH (уровень 1, PROJ):"
    id1=$(generate_id 1 "project" "")
    echo "   ID: $id1"
    echo "   Имя файла: $(generate_filename "$id1" "project" "Universal Creative Hub" 1)"
    
    echo ""
    echo "2. Линия документации (уровень 2, LINE):"
    id2=$(generate_id 2 "line" "")
    echo "   ID: $id2"
    echo "   Имя файла: $(generate_filename "$id2" "line" "Documentation System" 2)"
    
    echo ""
    echo "3. Компонент системы нумерации (уровень 3, COMP):"
    id3=$(generate_id 3 "component" "2-010400-1")
    echo "   ID: $id3"
    echo "   Имя файла: $(generate_filename "$id3" "component" "Numbering System" 3)"
    
    echo ""
    echo "4. Задача аудита (уровень 4, TASK):"
    id4=$(generate_id 4 "task" "3-010401-1")
    echo "   ID: $id4"
    echo "   Имя файла: $(generate_filename "$id4" "task" "Аудит несоответствий" 4)"
    
    echo ""
    echo "5. Решение (уровень 5, SOL):"
    id5=$(generate_id 5 "solution" "4-010400-0100-1")
    echo "   ID: $id5"
    echo "   Имя файла: $(generate_filename "$id5" "solution" "Алгоритм корректировки" 5)"
    
    echo ""
    echo "✅ Тест завершен"
}
EOF

# Делаем исполняемым
chmod +x ./uch-scripts/id-generator-v2.sh

echo "✅ Создан новый модуль: uch-scripts/id-generator-v2.sh"
```

## 🚀 **Шаг 2: Тестируем новый генератор**

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# Запускаем тест
echo "🔧 ТЕСТИРУЕМ НОВЫЙ ГЕНЕРАТОР ID:"
./uch-scripts/id-generator-v2.sh test_id_generation
```

## 🚀 **Шаг 3: Обновляем document-creator.sh для использования нового генератора**

```bash
# Создаем backup текущего document-creator.sh
cp ./uch-scripts/document-creator.sh ./uch-scripts/document-creator.sh.backup

# Создаем обновленную версию
cat > ./uch-scripts/document-creator-v2.sh << 'EOF'
#!/bin/bash
# Модуль реального создания документов - с НОВОЙ системой генерации ID

# Загружаем новый генератор ID
if [ -f "$SCRIPT_DIR/id-generator-v2.sh" ]; then
    source "$SCRIPT_DIR/id-generator-v2.sh"
else
    echo "❌ Ошибка: Модуль id-generator-v2.sh не найден"
    exit 1
fi

# Найти файл документа по ID в frontmatter
find_document_by_id() {
    local target_id="$1"
    
    for file in *.md; do
        if [ ! -f "$file" ]; then
            continue
        fi
        
        if head -20 "$file" | grep -q '^id:[[:space:]]*"'"$target_id"'"'; then
            echo "$file"
            return 0
        fi
    done
    
    return 1
}

# Получить имя документа из поля name в frontmatter
get_name_from_frontmatter() {
    local file="$1"
    
    if [ ! -f "$file" ]; then
        return 1
    fi
    
    head -20 "$file" | awk '/^name:[[:space:]]*"/ {
        gsub(/^name:[[:space:]]*"/, "", $0)
        gsub(/"$/, "", $0)
        print $0
        exit
    }'
}

# Получить сокращенный тип для имени файла
get_short_type() {
    local type="$1"
    
    case "$type" in
        "project"|"proj") echo "PROD" ;;
        "line") echo "LINE" ;;
        "component"|"comp") echo "COMP" ;;
        "module") echo "MOD" ;;
        "epic") echo "EPIC" ;;
        "task") echo "TASK" ;;
        "feature"|"feat") echo "FEAT" ;;
        "bug") echo "BUG" ;;
        "snapshot"|"snap") echo "SNAP" ;;
        "solution"|"sol") echo "SOL" ;;
        "subtask") echo "SUBTASK" ;;
        "code_block"|"code") echo "CODE" ;;
        "decision"|"dec") echo "DEC" ;;
        "idea") echo "IDEA" ;;
        "reference"|"ref") echo "REF" ;;
        "meeting") echo "MEET" ;;
        "architecture"|"arch") echo "ARCH" ;;
        "documentation"|"doc") echo "DOC" ;;
        "specification"|"spec") echo "SPEC" ;;
        "design") echo "DESIGN" ;;
        "plan") echo "PLAN" ;;
        *) echo "DOC" ;;
    esac
}

# Создать реальный документ с НОВОЙ системой ID
create_real_document() {
    local name="$1"
    local level="$2"
    local type="$3"
    local parent_id="$4"
    local tags="$5"
    
    local current_date=$(get_current_date)
    local doc_id=""
    local parent_name=""
    local parent_file=""
    
    # 1. Определяем ID по НОВОЙ системе
    if [ "$level" = "N" ]; then
        # Неиерархический документ (используем старую функцию)
        if command -v generate_non_hierarchical_id &> /dev/null; then
            doc_id=$(generate_non_hierarchical_id "$type")
        else
            # Фолбэк
            case "$type" in
                "idea") doc_id="Z-$(date +%Y%m%d%H%M%S)" ;;
                "meeting") doc_id="M-$(date +%Y%m%d)" ;;
                *) doc_id="Z-$(date +%Y%m%d%H%M%S)" ;;
            esac
        fi
        echo "🆔 Сгенерирован неиерархический ID: $doc_id"
    else
        # Иерархический документ - используем НОВУЮ систему
        if ! command -v generate_id &> /dev/null; then
            echo "❌ Ошибка: Функция generate_id не найдена"
            echo "   Убедитесь что id-generator-v2.sh загружен"
            return 1
        fi
        
        # Получаем имя родителя если есть
        if [ -n "$parent_id" ] && [ "$level" -gt 1 ]; then
            parent_file=$(find_document_by_id "$parent_id")
            if [ -z "$parent_file" ]; then
                echo "⚠️  Предупреждение: Родительский документ с ID '$parent_id' не найден!"
                echo "   Создаю документ без информации о родителе"
            else
                parent_name=$(get_name_from_frontmatter "$parent_file")
                if [ -z "$parent_name" ]; then
                    parent_name=$(basename "$parent_file" .md | sed "s/^[^_]*_//" | sed "s/^[^_]*_//")
                fi
            fi
        fi
        
        # Генерируем ID по новой системе
        doc_id=$(generate_id "$level" "$type" "$parent_id")
        if [ $? -ne 0 ] || [ -z "$doc_id" ]; then
            echo "❌ Ошибка генерации ID"
            return 1
        fi
        echo "🆔 Сгенерирован ID по новой системе: $doc_id"
    fi
    
    # 2. Форматируем теги (используем существующую функцию)
    local tags_yaml=""
    if command -v format_tags_yaml &> /dev/null; then
        tags_yaml=$(format_tags_yaml "$tags" "$type")
        if [ -n "$tags_yaml" ]; then
            echo "🏷️  Сформированные теги:"
            echo "$tags_yaml"
        fi
    else
        # Простой фолбэк
        if [ -n "$tags" ]; then
            tags_yaml="tags:"
            IFS=',' read -ra TAG_ARRAY <<< "$tags"
            for tag in "${TAG_ARRAY[@]}"; do
                tags_yaml="$tags_yaml"$'\\n'$'  - "'"${tag// /}"'"'
            done
        fi
    fi
    
    # 3. Создаем имя файла в ПРАВИЛЬНОМ формате
    local filename=""
    if command -v generate_filename &> /dev/null; then
        filename=$(generate_filename "$doc_id" "$type" "$name" "$level")
    else
        # Фолбэк: простой формат
        local short_type=$(get_short_type "$type")
        local slug=$(echo "$name" | tr ' ' '_' | tr '[:upper:]' '[:lower:]' | tr -cd '[:alnum:]_')
        filename="${doc_id}_${short_type}_${slug}.md"
    fi
    
    echo "📄 Создаю документ: $filename"
    
    # 4. Выбираем шаблон
    if [ "$level" = "N" ]; then
        # Неиерархический документ
        if [ ! -f "T-NONHIER.md" ]; then
            echo "❌ Ошибка: Не найден шаблон для неиерархических документов T-NONHIER.md"
            return 1
        fi
        
        create_from_template "$filename" "$doc_id" "$name" "N" "$type" \
            "" "" "$tags_yaml" "$current_date" "T-NONHIER.md"
    else
        # Иерархический документ
        local template_file=""
        if [ "$level" -eq 1 ]; then
            template_file="T-MASTER.md"
        else
            template_file="T-CHILD.md"
        fi
        
        if [ ! -f "$template_file" ]; then
            echo "❌ Ошибка: Не найден шаблон $template_file"
            return 1
        fi
        
        create_from_template "$filename" "$doc_id" "$name" "$level" "$type" \
            "$parent_id" "$parent_name" "$tags_yaml" "$current_date" "$template_file"
    fi
    
    if [ $? -eq 0 ]; then
        echo "✅ Документ успешно создан: $filename"
        
        # Обновить родительский документ (если есть)
        if [ -n "$parent_file" ] && [ "$level" -gt 1 ]; then
            if command -v update_parent_document &> /dev/null; then
                update_parent_document "$parent_file" "$filename"
            fi
        fi
        
        return 0
    else
        echo "❌ Ошибка при создании документа"
        return 1
    fi
}

# Функция создания из шаблона (без изменений)
create_from_template() {
    local filename="$1"
    local doc_id="$2"
    local name="$3"
    local level="$4"
    local type="$5"
    local parent_id="$6"
    local parent_name="$7"
    local tags_yaml="$8"
    local current_date="$9"
    local template_file="${10}"
    
    echo "📋 Использую шаблон: $template_file"
    
    # Читаем шаблон
    local template_content=$(cat "$template_file")
    
    # Заменяем все переменные {{var}} на значения
    template_content=${template_content//\{\{id\}\}/$doc_id}
    template_content=${template_content//\{\{name\}\}/$name}
    template_content=${template_content//\{\{type\}\}/$type}
    template_content=${template_content//\{\{level\}\}/$level}
    template_content=${template_content//\{\{status\}\}/planning}
    template_content=${template_content//\{\{created\}\}/$current_date}
    template_content=${template_content//\{\{updated\}\}/$current_date}
    template_content=${template_content//\{\{author\}\}/$USER}
    
    # Обрабатываем родительскую информацию
    local parent_footer=""
    if [ -n "$parent_id" ] && [ -n "$parent_name" ] && [ "$template_file" = "T-CHILD.md" ]; then
        parent_footer="Родитель: ${parent_id}"
    fi
    
    # Создаем документ
    echo "$template_content" > "$filename"
    
    # Добавляем теги если есть
    if [ -n "$tags_yaml" ]; then
        # Вставляем теги в frontmatter - после строки author:
        local in_frontmatter=false
        local author_found=false
        local temp_file="${filename}.tmp"
        
        while IFS= read -r line || [ -n "$line" ]; do
            echo "$line" >> "$temp_file"
            
            if [[ "$line" == "---" ]]; then
                if [ "$in_frontmatter" = false ]; then
                    in_frontmatter=true
                else
                    in_frontmatter=false
                fi
            elif [ "$in_frontmatter" = true ] && [[ "$line" == author:* ]] && [ "$author_found" = false ]; then
                echo "$tags_yaml" >> "$temp_file"
                author_found=true
            fi
        done < "$filename"
        
        mv "$temp_file" "$filename"
    fi
    
    # Добавляем родительскую информацию если есть
    if [ -n "$parent_footer" ]; then
        echo "" >> "$filename"
        echo "---" >> "$filename"
        echo "$parent_footer" >> "$filename"
    fi
    
    echo "   📝 Заполнен шаблон, добавлены теги"
    return 0
}
EOF

# Заменяем старый document-creator.sh новым
mv ./uch-scripts/document-creator-v2.sh ./uch-scripts/document-creator.sh
chmod +x ./uch-scripts/document-creator.sh

echo "✅ Обновлен document-creator.sh для использования новой системы генерации ID"
```

## 🚀 **Шаг 4: Обновляем main.sh для загрузки нового модуля**

```bash
# Добавляем загрузку нового генератора в main.sh
if ! grep -q "id-generator-v2.sh" ./uch-scripts/main.sh; then
    # Добавляем после загрузки других модулей
    sed -i '' '/source.*utils.sh/a\
if [ -f "$SCRIPT_DIR/id-generator-v2.sh" ]; then\
    source "$SCRIPT_DIR/id-generator-v2.sh"\
    MODULES_LOADED=$((MODULES_LOADED + 1))\
fi' ./uch-scripts/main.sh
    
    echo "✅ Обновлен main.sh для загрузки id-generator-v2.sh"
else
    echo "⚠️  main.sh уже загружает id-generator-v2.sh"
fi
```

## 🚀 **Шаг 5: Тестируем всю систему**

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔧 ЗАПУСК ТЕСТА ВСЕЙ СИСТЕМЫ:"
echo "============================="

# 1. Тест генератора
echo "1. ТЕСТ ГЕНЕРАТОРА ID:"
./uch-scripts/id-generator-v2.sh test_id_generation

echo -e "\n2. ТЕСТ СОЗДАНИЯ ДОКУМЕНТА (СУХОЙ ТЕСТ):"

# Создаем тестовый скрипт
cat > test-new-system.sh << 'EOF'
#!/bin/bash
# Тест новой системы

SCRIPT_DIR="./uch-scripts"
source "$SCRIPT_DIR/utils.sh"
source "$SCRIPT_DIR/id-generator-v2.sh"
source "$SCRIPT_DIR/document-creator.sh"

echo "🧪 ТЕСТ СОЗДАНИЯ ДОКУМЕНТОВ:"

# Тест 1: Генерация ID
echo "Тест 1.1: Проект (уровень 1)"
id1=$(generate_id 1 "project" "")
echo "   ID: $id1"
echo "   Имя файла: $(generate_filename "$id1" "project" "Test Project" 1)"

echo ""
echo "Тест 1.2: Задача (уровень 4)"
id2=$(generate_id 4 "task" "3-010401-1")
echo "   ID: $id2"
echo "   Имя файла: $(generate_filename "$id2" "task" "Test Task" 4)"

echo ""
echo "Тест 1.3: Архитектура (общий тип, уровень 2)"
id3=$(generate_id 2 "architecture" "")
echo "   ID: $id3"
echo "   Имя файла: $(generate_filename "$id3" "architecture" "Test Architecture" 2)"

# Тест 2: Проверка функций
echo ""
echo "Тест 2: Вспомогательные функции"
echo "   Тип 'task' на уровне 4: $(get_type_number "task" 4)"
echo "   Тип 'arch' на уровне 2: $(get_type_number "arch" 2)"
echo "   Тип 'doc' на уровне 3: $(get_type_number "doc" 3)"

EOF

chmod +x test-new-system.sh
./test-new-system.sh

echo -e "\n3. ПРОВЕРКА СОВМЕСТИМОСТИ:"
echo "   Убедитесь что create.sh и другие модули работают с новым document-creator.sh"
```

## 🎯 **Шаг 6: Финальная проверка**

```bash
echo "🎯 ФИНАЛЬНАЯ ПРОВЕРКА СИСТЕМЫ:"
echo "=============================="

# Проверяем что все модули загружаются
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
./uch-create-modular.sh
```

**Если система запускается без ошибок - новая система генерации ID готова!**

## 📝 **Что мы сделали:**

1. ✅ Создали `id-generator-v2.sh` - новый модуль генерации ID по системе `3-010402-1`
2. ✅ Обновили `document-creator.sh` для использования новой системы
3. ✅ Обновили `main.sh` для загрузки нового модуля
4. ✅ Сохранили обратную совместимость (старые функции остались)

**Теперь при создании новых документов через модульную систему будут генерироваться правильные ID!**

**Запустите тестовые команды и проверьте, что система работает.**

---

## 👤 **Kirill**

кажется старое меню
kirillkravcov@MacBook-Air uch-docs % ./uch-create-modular.sh
=== Запуск модульной системы UCH ===
=== UCH CREATE: МОДУЛЬНАЯ СИСТЕМА ===
Версия: 1.1.0

⚠️  main.sh уже загружает id-generator-v2.sh
✅ Загружено модулей: 7

=== ГЛАВНОЕ МЕНЮ ===
1 - Создать документ (авто-ID)
2 - Создать по имени (ручной ID)
3 - Пакетное создание
4 - Операции с документами (замена/смещение)
5 - Проверить систему
6 - Протестировать утилиты
q - Выход

Ваш выбор (1-6/q): 1

📝 СОЗДАНИЕ ДОКУМЕНТА

=== ВЫБОР УРОВНЯ ДОКУМЕНТА ===
1 - Мастер-документ (уровень 1, корневой)
2 - Дочерний документ (уровень 2+)
3 - Неиерархический документ (идеи, ссылки, встречи)

Ваш выбор (1-3): 2

Введите ID родительского документа (например: 00 или 00-01):

---

## 🤖 **DeepSeek**

Да, система все еще использует **старый формат ID** в меню и подсказках! Нужно обновить подсказки и интерфейс для работы с **новым форматом ID**.

## 🔧 **Проблемы, которые нужно исправить:**

1. **Подсказки в меню** показывают старый формат (`00 или 00-01`)
2. **Система запрашивает родительский ID в старом формате**
3. **Пользователь не знает, какой формат ID использовать**

## 🚀 **Исправляем подсказки и интерфейс:**

### **Шаг 1: Обновляем create.sh (главное меню создания)**

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

# Создаем обновленную версию create.sh
cat > ./uch-scripts/create-v2.sh << 'EOF'
#!/bin/bash
# Модуль создания документов - обновленный для новой системы ID

# Загружаем модуль создания документов если есть
if [ -f "$SCRIPT_DIR/document-creator.sh" ]; then
    source "$SCRIPT_DIR/document-creator.sh"
fi

# Загружаем новый генератор ID
if [ -f "$SCRIPT_DIR/id-generator-v2.sh" ]; then
    source "$SCRIPT_DIR/id-generator-v2.sh"
fi

# Улучшенная функция создания документа (с новыми подсказками)
create_document_improved() {
    echo ""
    echo "📝 СОЗДАНИЕ ДОКУМЕНТА (НОВАЯ СИСТЕМА ID)"
    echo ""
    
    # 1. Выбор уровня
    echo "=== ВЫБОР УРОВНЯ ДОКУМЕНТА ==="
    echo "1 - Проект (уровень 1, корневой) - например: 1-010000-1"
    echo "2 - Линия/Компонент (уровень 2-3) - например: 2-010400-1, 3-010401-1"
    echo "3 - Задача/Решение (уровень 4-5) - например: 4-010400-0100-1, 5-010400-010001-1"
    echo "4 - Отчет (уровень 6) - например: 6-010400-001-1"
    echo "5 - Неиерархический документ (идеи, встречи, ссылки)"
    echo ""
    read -p "Ваш выбор (1-5): " level_choice
    
    local level=""
    local parent_id=""
    local level_name=""
    
    case $level_choice in
        1)
            level=1
            level_name="Проект (уровень 1)"
            echo "Выбран: $level_name"
            echo "Пример ID: 1-010000-1 (PROD), 1-010000-7 (ARCH)"
            ;;
        2)
            # Уровень 2 или 3
            echo ""
            echo "=== ВЫБОР КОНКРЕТНОГО УРОВНЯ ==="
            echo "1 - Линия развития (уровень 2) - например: 2-010400-1"
            echo "2 - Компонент системы (уровень 3) - например: 3-010401-1"
            read -p "Ваш выбор (1-2): " sub_choice
            
            case $sub_choice in
                1) level=2; level_name="Линия (уровень 2)" ;;
                2) level=3; level_name="Компонент (уровень 3)" ;;
                *) 
                    echo "❌ Неверный выбор"
                    return 1
                    ;;
            esac
            
            echo ""
            echo "Выбран: $level_name"
            read -p "Введите ID родительского документа (например: 1-010000-1 для уровня 2, 2-010400-1 для уровня 3): " parent_id
            
            if [ -z "$parent_id" ]; then
                echo "⚠️  ID родителя не указан. Будет создан документ без родителя."
            else
                echo "Родительский документ: $parent_id"
            fi
            ;;
        3)
            # Уровень 4 или 5
            echo ""
            echo "=== ВЫБОР КОНКРЕТНОГО УРОВНЯ ==="
            echo "1 - Задача (уровень 4) - например: 4-010400-0100-1"
            echo "2 - Решение (уровень 5) - например: 5-010400-010001-1"
            read -p "Ваш выбор (1-2): " sub_choice
            
            case $sub_choice in
                1) level=4; level_name="Задача (уровень 4)" ;;
                2) level=5; level_name="Решение (уровень 5)" ;;
                *) 
                    echo "❌ Неверный выбор"
                    return 1
                    ;;
            esac
            
            echo ""
            echo "Выбран: $level_name"
            read -p "Введите ID родительского документа (например: 3-010401-1 для задачи, 4-010400-0100-1 для решения): " parent_id
            
            if [ -z "$parent_id" ]; then
                echo "⚠️  Внимание: Для уровня $level рекомендуется указать родительский ID."
                read -p "Продолжить без родителя? (y/n): " confirm
                if [[ ! "$confirm" =~ ^[Yy]$ ]]; then
                    return 1
                fi
            else
                echo "Родительский документ: $parent_id"
            fi
            ;;
        4)
            level=6
            level_name="Отчет (уровень 6)"
            echo "Выбран: $level_name"
            echo "Пример ID: 6-010400-001-1 (REPORT)"
            
            echo ""
            read -p "Введите ID родительского документа (опционально, например: 2-010400-1): " parent_id
            ;;
        5)
            level="N"
            level_name="Неиерархический документ"
            echo "Выбран: $level_name"
            echo "Пример ID: Z-20260204123000 (IDEA), M-20260204 (MEETING)"
            ;;
        *)
            echo "❌ Неверный выбор"
            return 1
            ;;
    esac
    
    # 2. Название документа
    echo ""
    read -p "Введите название документа: " name
    if [ -z "$name" ]; then
        echo "❌ Название не может быть пустым"
        return 1
    fi
    
    # 3. Выбор типа
    echo ""
    echo "=== ВЫБОР ТИПА ДОКУМЕНТА ==="
    
    if [ -f "$SCRIPT_DIR/types.sh" ]; then
        source "$SCRIPT_DIR/types.sh"
        
        if [ "$level" = "N" ]; then
            echo "Типы для неиерархических документов:"
            echo "1 - idea (Идея/концепция)"
            echo "2 - reference (Ссылка/ресурс)"
            echo "3 - meeting (Встреча/обсуждение)"
            echo "4 - note (Заметка)"
            echo ""
            read -p "Выберите тип (1-4): " type_choice
            
            case $type_choice in
                1) type="idea" ;;
                2) type="reference" ;;
                3) type="meeting" ;;
                4) type="note" ;;
                *) type="idea" ;;
            esac
        else
            show_type_menu_for_level "$level"
            type=$(select_type_by_number "$level")
        fi
    else
        # Фолбэк
        type=$(get_default_type_for_level "$level")
    fi
    
    echo "Выбран тип: $type"
    
    # 4. Теги
    echo ""
    read -p "Введите теги через запятую (необязательно): " tags
    
    # 5. Сводка
    echo ""
    echo "📋 СВОДКА:"
    echo "  Уровень: $level ($level_name)"
    echo "  Название: $name"
    echo "  Тип: $type"
    if [ -n "$parent_id" ]; then
        echo "  Родитель: $parent_id"
    fi
    if [ -n "$tags" ]; then
        echo "  Теги: $tags"
    fi
    
    # 6. Подтверждение
    echo ""
    read -p "Создать документ? (y/n): " confirm
    if [[ ! "$confirm" =~ ^[Yy]$ ]]; then
        echo "❌ Отменено"
        return 1
    fi
    
    # 7. Создание
    echo ""
    if create_real_document "$name" "$level" "$type" "$parent_id" "$tags"; then
        echo "✅ Документ успешно создан!"
    else
        echo "❌ Ошибка при создании документа"
        return 1
    fi
}

# Функция для совместимости
create_batch_documents() {
    echo "Пакетное создание временно недоступно в новой системе"
    echo "Используйте обычное создание документов"
    read -p "Нажмите Enter чтобы вернуться в меню..."
}
EOF

# Заменяем старый create.sh
mv ./uch-scripts/create-v2.sh ./uch-scripts/create.sh
chmod +x ./uch-scripts/create.sh

echo "✅ Обновлен create.sh с новыми подсказками"
```

### **Шаг 2: Обновляем manual-mode.sh (ручное создание с ID)**

```bash
# Обновляем manual-mode.sh для работы с новым форматом ID
cat > ./uch-scripts/manual-mode-v2.sh << 'EOF'
#!/bin/bash
# Режим создания по имени (ручной ID) - обновленный для новой системы

# Создание документа с ручным указанием ID
create_document_manual() {
    echo "📝 СОЗДАНИЕ ПО ИМЕНИ (РУЧНОЙ ID - НОВЫЙ ФОРМАТ)"
    echo ""
    
    # 1. Ручной ID
    echo "=== ВВОД ID ДОКУМЕНТА ==="
    echo "Примеры правильных ID:"
    echo "  • 1-010000-1      (Проект, уровень 1)"
    echo "  • 2-010400-7      (Архитектура линии, уровень 2)"
    echo "  • 3-010401-1      (Компонент, уровень 3)"
    echo "  • 4-010400-0100-1 (Задача, уровень 4)"
    echo "  • 5-010400-010001-1 (Решение, уровень 5)"
    echo "  • Z-20260204120000 (Идея, неиерархический)"
    echo ""
    read -p "Введите ID документа: " manual_id
    
    if [ -z "$manual_id" ]; then
        echo "❌ ID не может быть пустым"
        return 1
    fi
    
    # Проверяем формат ID (новая система)
    if ! echo "$manual_id" | grep -qE '^([1-6]-[0-9]{6}-[0-9]+(-[0-9]+)?|Z-[0-9]{14}|M-[0-9]{8}|R-[0-9A-F]{6})$'; then
        echo "⚠️  Предупреждение: ID не соответствует стандартному формату"
        echo "   Форматы: X-XXXXXX-X, X-XXXXXX-XXXX-X, Z-YYYYMMDDHHMMSS, M-YYYYMMDD, R-HEXCODE"
        read -p "Продолжить? (y/n): " confirm
        if [[ ! "$confirm" =~ ^[Yy]$ ]]; then
            return 1
        fi
    fi
    
    # Проверяем не занят ли ID
    if find . -maxdepth 1 -name "${manual_id}_*.md" -type f | grep -q .; then
        echo "❌ Ошибка: Документ с ID '$manual_id' уже существует!"
        echo "   Существующие файлы:"
        find . -maxdepth 1 -name "${manual_id}_*.md" -type f | head -3
        return 1
    fi
    
    # 2. Определяем уровень из ID
    local level=""
    if [[ "$manual_id" =~ ^[1-6]- ]]; then
        level=$(echo "$manual_id" | cut -d'-' -f1)
        echo "Определен уровень: $level"
    elif [[ "$manual_id" =~ ^Z- ]]; then
        level="N"
        echo "Определен уровень: N (неиерархический, идея)"
    elif [[ "$manual_id" =~ ^M- ]]; then
        level="N"
        echo "Определен уровень: N (неиерархический, встреча)"
    elif [[ "$manual_id" =~ ^R- ]]; then
        level="N"
        echo "Определен уровень: N (неиерархический, ссылка)"
    else
        echo "⚠️  Не удалось определить уровень из ID"
        read -p "Введите уровень вручную (1-6 или N): " level
    fi
    
    # 3. Название документа
    echo ""
    read -p "Введите название документа: " name
    if [ -z "$name" ]; then
        echo "❌ Название не может быть пустым"
        return 1
    fi
    
    # 4. Выбор типа
    echo ""
    if [ -f "$SCRIPT_DIR/types.sh" ]; then
        source "$SCRIPT_DIR/types.sh"
        show_type_menu_for_level "$level"
        type=$(select_type_by_number "$level")
    else
        type=$(get_default_type_for_level "$level")
    fi
    
    # 5. Теги
    echo ""
    read -p "Введите теги через запятую (необязательно): " tags
    
    # 6. Родитель (если есть в ID и уровень > 1)
    local parent_id=""
    if [[ "$manual_id" =~ ^[2-6]- ]] && [[ "$level" =~ ^[2-6]$ ]]; then
        echo ""
        read -p "Введите ID родительского документа (если есть): " parent_id
    fi
    
    # 7. Сводка
    echo ""
    echo "📋 СВОДКА:"
    echo "  ID: $manual_id"
    echo "  Уровень: $level"
    echo "  Название: $name"
    echo "  Тип: $type"
    if [ -n "$parent_id" ]; then
        echo "  Родитель: $parent_id"
    fi
    if [ -n "$tags" ]; then
        echo "  Теги: $tags"
    fi
    
    # 8. Подтверждение
    echo ""
    read -p "Создать документ? (y/n): " confirm
    if [[ ! "$confirm" =~ ^[Yy]$ ]]; then
        echo "❌ Отменено"
        return 1
    fi
    
    # 9. Создание с ручным ID
    echo ""
    if create_real_document "$name" "$level" "$type" "$parent_id" "$tags"; then
        # Переименовываем файл чтобы использовать ручной ID
        # Находим созданный файл (последний созданный)
        latest_file=$(ls -t *.md | head -1)
        if [ -f "$latest_file" ]; then
            # Генерируем правильное имя файла с ручным ID
            short_type=$(get_short_type "$type")
            slug=$(echo "$name" | tr ' ' '_' | tr '[:upper:]' '[:lower:]' | tr -cd '[:alnum:]_')
            correct_name="${manual_id}_${short_type}_${slug}.md"
            
            if [ "$latest_file" != "$correct_name" ]; then
                mv "$latest_file" "$correct_name"
                echo "✅ Переименован в: $correct_name"
                
                # Обновляем ID в frontmatter если нужно
                sed -i '' "s/^id:.*/id: \"$manual_id\"/" "$correct_name"
            fi
            
            echo "✅ Документ создан с ручным ID: $manual_id"
        else
            echo "✅ Документ создан"
        fi
    else
        echo "❌ Ошибка при создании документа"
        return 1
    fi
}
EOF

# Заменяем старый manual-mode.sh
mv ./uch-scripts/manual-mode-v2.sh ./uch-scripts/manual-mode.sh
chmod +x ./uch-scripts/manual-mode.sh

echo "✅ Обновлен manual-mode.sh для новой системы ID"
```

### **Шаг 3: Тестируем обновленную систему**

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔧 ТЕСТИРУЕМ ОБНОВЛЕННУЮ СИСТЕМУ:"
echo "================================"

# Запускаем обновленную систему
./uch-create-modular.sh
```

## 🎯 **Что мы обновили:**

1. ✅ **create.sh**: Новое меню с правильными примерами ID
2. ✅ **manual-mode.sh**: Поддержка нового формата ID при ручном вводе
3. ✅ **Подсказки**: Показывают примеры правильных ID (`1-010000-1`, `4-010400-0100-1`, etc.)

**Теперь при выборе "1 - Создать документ (авто-ID)" система будет:**
- Показывать правильные примеры ID
- Запрашивать ID в новом формате
- Генерировать документы с правильными ID

**Попробуйте создать тестовый документ через обновленное меню!**

---

## 👤 **Kirill**

kirillkravcov@MacBook-Air uch-docs % ./uch-create-modular.sh
=== Запуск модульной системы UCH ===
=== UCH CREATE: МОДУЛЬНАЯ СИСТЕМА ===
Версия: 1.1.0

/Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs/uch-scripts/main.sh: line 138: syntax error: unexpected end of file
kirillkravcov@MacBook-Air uch-docs %

---

## 🤖 **DeepSeek**

Ошибка синтаксиса в `main.sh`! Давайте проверим и исправим:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🔍 ПРОВЕРЯЕМ main.sh НА ОШИБКИ:"
echo "=============================="

# Проверяем синтаксис
bash -n ./uch-scripts/main.sh

# Смотрим последние строки файла
echo -e "\n📋 ПОСЛЕДНИЕ 20 СТРОК main.sh:"
tail -20 ./uch-scripts/main.sh

# Смотрим строку 138
echo -e "\n🔍 СТРОКА 138 И ОКРЕСТНОСТИ:"
sed -n '130,150p' ./uch-scripts/main.sh
```

Скорее всего проблема в том, что мы добавили загрузку `id-generator-v2.sh` и сломали синтаксис. Давайте исправим `main.sh`:

```bash
# Создаем исправленную версию main.sh
cat > ./uch-scripts/main-v2.sh << 'EOF'
#!/bin/bash
# Главный скрипт UCH Document System - исправленная версия

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "=== UCH CREATE: МОДУЛЬНАЯ СИСТЕМА ==="
echo "Версия: 1.1.0 (НОВАЯ СИСТЕМА ID)"
echo ""

# Подключаем модули
MODULES_LOADED=0

# Базовые утилиты
if [ -f "$SCRIPT_DIR/utils.sh" ]; then
    source "$SCRIPT_DIR/utils.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

# Типы документов
if [ -f "$SCRIPT_DIR/types.sh" ]; then
    source "$SCRIPT_DIR/types.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

# Новый генератор ID (приоритетный)
if [ -f "$SCRIPT_DIR/id-generator-v2.sh" ]; then
    source "$SCRIPT_DIR/id-generator-v2.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
    echo "✅ Загружен новый генератор ID (v2)"
fi

# Создание документов
if [ -f "$SCRIPT_DIR/create.sh" ]; then
    source "$SCRIPT_DIR/create.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

if [ -f "$SCRIPT_DIR/document-creator.sh" ]; then
    source "$SCRIPT_DIR/document-creator.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

# Режимы работы
if [ -f "$SCRIPT_DIR/manual-mode.sh" ]; then
    source "$SCRIPT_DIR/manual-mode.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

if [ -f "$SCRIPT_DIR/batch-mode.sh" ]; then
    source "$SCRIPT_DIR/batch-mode.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

# Операции с документами
if [ -f "$SCRIPT_DIR/replace-shift.sh" ]; then
    source "$SCRIPT_DIR/replace-shift.sh"
    MODULES_LOADED=$((MODULES_LOADED + 1))
fi

echo "✅ Загружено модулей: $MODULES_LOADED"
echo ""

# Главное меню
show_main_menu() {
    echo "=== ГЛАВНОЕ МЕНЮ (НОВАЯ СИСТЕМА ID) ==="
    echo "1 - Создать документ (авто-ID по новой системе)"
    echo "2 - Создать по имени (ручной ID - новый формат)"
    echo "3 - Пакетное создание"
    echo "4 - Операции с документами (замена/смещение)"
    echo "5 - Проверить систему"
    echo "6 - Протестировать утилиты"
    echo "q - Выход"
    echo ""
    read -p "Ваш выбор (1-6/q): " choice
    
    case $choice in
        1) 
            create_document_improved
            echo ""
            show_main_menu 
            ;;
        2)
            create_document_manual
            echo ""
            show_main_menu
            ;;
        3)
            create_batch_documents
            echo ""
            show_main_menu
            ;;
        4)
            if command -v show_simple_operations_menu &> /dev/null; then
                show_simple_operations_menu
            elif command -v show_document_operations_menu &> /dev/null; then
                show_document_operations_menu
            else
                echo "❌ Функция операций с документами не найдена"
            fi
            echo ""
            show_main_menu
            ;;
        5)
            echo "✅ СИСТЕМА:"
            echo "📁 Директория: $SCRIPT_DIR"
            echo "📅 Дата: $(get_current_date 2>/dev/null || echo 'N/A')"
            echo "📦 Модулей: $MODULES_LOADED"
            echo "🆔 Система ID: НОВАЯ (3-010402-1)"
            
            # Проверяем новый генератор
            if command -v generate_id &> /dev/null; then
                echo "   Генератор ID: ✅ Загружен"
                echo "   Пример ID: $(generate_id 1 "project" "" 2>/dev/null || echo 'Ошибка')"
            else
                echo "   Генератор ID: ❌ Не загружен"
            fi
            
            echo ""
            show_main_menu
            ;;
        6)
            echo "🧪 ТЕСТ УТИЛИТ:"
            echo "- Текущая дата: $(get_current_date 2>/dev/null || echo 'Ошибка')"
            
            if command -v find_free_master_id &> /dev/null; then
                echo "- Старый master ID: $(find_free_master_id 2>/dev/null || echo 'N/A')"
            fi
            
            if command -v generate_id &> /dev/null; then
                echo "- Новый ID для проекта: $(generate_id 1 "project" "" 2>/dev/null || echo 'Ошибка')"
                echo "- Новый ID для задачи: $(generate_id 4 "task" "3-010401-1" 2>/dev/null || echo 'Ошибка')"
            fi
            
            echo "- Тип по умолчанию для уровня 3: $(get_default_type_for_level 3 2>/dev/null || echo 'N/A')"
            echo ""
            show_main_menu
            ;;
        q|Q) 
            echo "Выход" 
            exit 0 
            ;;
        *) 
            echo "❌ Неверный выбор" 
            show_main_menu 
            ;;
    esac
}

# Проверяем аргументы
if [ $# -eq 0 ]; then
    show_main_menu
else
    echo "⚠️  Командный режим пока не поддерживается"
    show_main_menu
fi
EOF

# Заменяем старый main.sh
mv ./uch-scripts/main-v2.sh ./uch-scripts/main.sh
chmod +x ./uch-scripts/main.sh

echo "✅ Исправлен main.sh"

# Проверяем синтаксис
echo "🔍 ПРОВЕРКА СИНТАКСИСА:"
bash -n ./uch-scripts/main.sh && echo "✅ Синтаксис правильный"

# Тестируем загрузку
echo -e "\n🔧 ТЕСТ ЗАГРУЗКИ МОДУЛЕЙ:"
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs
./uch-create-modular.sh --help 2>&1 | head -20
```

Если все еще есть ошибка, давайте создадим минимальную рабочую версию:

```bash
# Создаем минимальную рабочую версию на случай проблем
cat > ./uch-scripts/main-minimal.sh << 'EOF'
#!/bin/bash
# Минимальная рабочая версия main.sh

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "=== UCH CREATE: МИНИМАЛЬНАЯ ВЕРСИЯ ==="
echo "Версия: 1.1.0"
echo ""

# Только базовые модули
if [ -f "$SCRIPT_DIR/utils.sh" ]; then
    source "$SCRIPT_DIR/utils.sh"
    echo "✅ Загружен utils.sh"
fi

if [ -f "$SCRIPT_DIR/create.sh" ]; then
    source "$SCRIPT_DIR/create.sh"
    echo "✅ Загружен create.sh"
fi

if [ -f "$SCRIPT_DIR/document-creator.sh" ]; then
    source "$SCRIPT_DIR/document-creator.sh"
    echo "✅ Загружен document-creator.sh"
fi

echo ""
echo "Для использования запустите:"
echo "  create_document_improved   - Создать документ"
echo "  create_document_manual     - Создать с ручным ID"
echo ""
echo "Или используйте полную версию: ./uch-create-modular.sh"
EOF

chmod +x ./uch-scripts/main-minimal.sh
```

Теперь давайте попробуем запустить снова:

```bash
cd /Users/kirillkravcov/obsidian/my-digital-garden-content/uch-docs

echo "🚀 ЗАПУСК ОБНОВЛЕННОЙ СИСТЕМЫ:"
echo "=============================="
./uch-create-modular.sh
```

Если все еще есть ошибка, запустите минимальную версию для диагностики:

```bash
echo "🔧 ЗАПУСК МИНИМАЛЬНОЙ ВЕРСИИ ДЛЯ ДИАГНОСТИКИ:"
./uch-scripts/main-minimal.sh
```

**Выполните эти команды и покажите результат.** Мы найдем и исправим ошибку в `main.sh`.

---

